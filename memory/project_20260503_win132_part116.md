---
name: Win版#132 part 116 — NotebookLM #4 動画 (Multi-Agent Convergence) 暫定 MP4 埋め込み
description: 2026-05-03 Win版#132 part 116. NotebookLM f167dcc3 (Multi-Agent Convergence) を local pipeline で download → ffmpeg 圧縮 → web/assets/videos/ self-host → philosophy_page.dart に AI大学シリーズ #4 として埋め込み. GHA pipeline は 5 secrets 未設定で fail. Issue #1724 で恒久化依頼.
type: project
originSessionId: 9633496b-31a1-44ce-835a-a7c647f65288
---
# Win版#132 part 116 (2026-05-03)

## 軸: NotebookLM 動画 self-host fallback pattern 確立

User 要望「前と同じように、この動画をダウンロード、編集、サイト埋め込みしてください」(= NotebookLM `f167dcc3-ca79-4c47-91f3-7ef90a409025` Competitive AI Intelligence Report: The Multi-Agent Convergence).

## 完了

1. **GHA dispatch 試行** — `gh workflow run notebooklm-video-pipeline.yml` で f167dcc3 / 25423b84 / series=4 / slug=multi-agent-convergence
   - run [25271275372](https://github.com/kanta13jp1/my_web_app/actions/runs/25271275372) は `Input required and not supplied: token` で 9 秒 fail
   - `gh secret list` で **5 secrets 全未設定** 確認 (GITHUB_PAT / NOTEBOOKLM_STORAGE_STATE_JSON / ELEVENLABS_API_KEY / YOUTUBE_CLIENT_SECRET_JSON / YOUTUBE_TOKEN_JSON)

2. **local pipeline で fallback**:
   - `notebooklm use f167dcc3` → `notebooklm artifact list --json` で 10 artifact 確認 → 動画 artifact_id = `25423b84-f3c5-4cca-81d1-8d781d7b16b6` 特定
   - `notebooklm download video -a 25423b84... videos/source/multi-agent-convergence-source.mp4` → 47MB / 9:19 (559.67 秒)
   - `ffmpeg -i ... -c:v libx264 -crf 28 -preset slow -vf scale=1280:720 -c:a aac -b:a 96k` → 12MB / 180kbps / 720p
   - `cp videos/edited/...720p.mp4 web/assets/videos/multi-agent-convergence.mp4`

3. **philosophy_page.dart 拡張**:
   - `_Video` class に optional `String? mp4Url` field 追加 (= YouTube ID と self-host MP4 切替可能 / 後方互換維持)
   - `initState` の iframe 登録で `v.mp4Url ?? 'https://www.youtube.com/embed/${v.id}'` 切替
   - `_videoMeta` の TextButton で button label を `mp4Url != null ? 'MP4 で見る' : 'YouTube で見る'` 切替
   - AI大学シリーズ #4 として `_Video(id: 'multi-agent-convergence', mp4Url: '/assets/videos/multi-agent-convergence.mp4', ...)` 追加
   - `dart format` + `flutter analyze` 両方 pass (No issues found)

4. **.gitignore 例外追加**:
   - 既存 `videos/` rule (line 107) が `web/assets/videos/` も catch していた
   - `!web/assets/videos/` + `!web/assets/videos/**` で carve-out (= 本番サーブする self-host MP4 のみ通す)

5. **commit + push**:
   - `ddfe640f1` feat(philosophy): NotebookLM #4 動画埋め込み (3 files / +22 / -3)
   - origin/main へ rebase + push 成功

6. **Issue #1724 起票** (P1 / automation):
   - 5 secrets 設定後 → workflow 再 dispatch → YouTube ID 取得 → philosophy_page.dart の id 置換 + mp4Url 削除 + 12MB MP4 削除 (= repo size 復元)

## 制約遭遇 (= 恒久化 backlog)

- **GHA pipeline 5 secrets 全未設定** — User の手動 `gh secret set` 必要 (= Issue #1724)
- **ElevenLabs Scribe 字幕未生成** — local では API key なしで実行不可 (= secrets 設定後に同 workflow で生成)
- **intro/outro card 未生成** — `videos/make_cards.py` は PIL + Noto Sans JP で local 実行可能だが、本セッション scope 外で skip (= 同上)
- **D variant (cinematic) 未生成** — 同上理由で原版 source video 圧縮版のみ

## 学び

1. **NotebookLM fallback pipeline pattern 確立** — GHA secrets 不足時に **local 4-step (download → ffmpeg 圧縮 → self-host → embed)** で 1 セッション完結可能を実証. 「品質 < 配信即時性」trade-off で先に動画を見せて後で YouTube 化する pattern.
2. **`_Video.mp4Url` 拡張は後方互換維持** — 既存 8 entry に変更不要. 新規 entry のみ MP4 切替. 「破壊しない拡張」第 N 例.
3. **.gitignore 例外 pattern** — `videos/` blanket ignore + `!web/assets/videos/` carve-out の典型例. Win版#96 の video-use 出力 ignore と本番配信 directory の共存を実現.
4. **Firebase Hosting で MP4 直接配信** — 12MB であれば Firebase Hosting の限度内. 動画 self-host の中間 staging に有効. ただし bandwidth 課金 + repo bloat があるので **YouTube 化は急務**.

## 次回 candidate

- [#1724](https://github.com/kanta13jp1/my_web_app/issues/1724) GHA secrets 5 件設定 + workflow 再 dispatch + YouTube ID 置換 (P1)
- (前 part 115 起票分) [#1700-1707](https://github.com/kanta13jp1/my_web_app/issues/1700) 8 件 (NotebookLM cross-check + AI tool 2026-05 fleet 反映)
- 残 NotebookLM list (= 2026-04-30 70+ notebook) を全 12 軸 docs/ にどう配分するか方針化 (= part 115 で 8 本だけ Issue 化済 / 残 60+ 本)
