---
name: Win版#132 part 121 — DUP_OPEN 9 件 cleanup + ISSUE-PRECHECK rule 追加
description: 2026-05-03 Win版#132 part 121. User 同一要望 10 度目 (Phase 6 自律 cycle 第 10 例). part 120 で実装した crosscheck script を full scan (limit 100) で実行 → DUP_OPEN 15 件中、bc58b50b (= 意図的多 Issue) と #1734 (= batch Issue) を除く 9 件を programmatic 一括 close + canonical Issue にコメント merge note 追記 + inject-rules.txt に [ISSUE-PRECHECK] rule 追加 (= 起票前 cross-check 必須).
type: project
---

# Win版#132 part 121 (2026-05-03)

## 軸: cross-check tool dogfood + 重複起票の 構造的防止 rule 追加

User 同一要望 **10 度目**. Phase 6「定常自律実行」第 10 例. 本 part の特徴: **part 120 で作った tool を即 part 121 で dogfood 実行** + **発見された fleet hygiene 問題に rule で構造対策**.

## 完了

1. **bc58b50b reaffirm** — `notebook_count=97` / `harness_found=true`.

2. **Full cross-check scan** (= `scripts/notebooklm_issue_crosscheck.py --limit 100 --json`):
   - **TRUE_GAP**: 53 (= part 120 limit=25 時の 1 → 限界拡張で 53 件浮上 / 多くは小規模 notebook で Issue 起票不要 candidates)
   - **DUP_OPEN**: 15 (= part 120 の 9 → 6 件追加検出 / うち bc58b50b=13 は意図的、#1734=6 件は batch Issue 内包)
   - **OK**: 29

3. **DUP_OPEN 9 件 programmatic 一括 close**:
   - close pattern: `close → canonical (notebook)`
   - `1768 → 1731 (9b2e686f)` Notion-Style Comments
   - `1769 → 1730 (6deda071)` Claude Design Plugin
   - `1795, 1812 → 1783 (9b8885ef)` SaaS Operations
   - `1819 → 1784 (1aced136)` Claude Code Masterclass
   - `1815 → 1785 (f985728b)` Devin Documentation
   - `1813 → 1801 (c60da02b)` Strategic Intelligence Scoreboard
   - `1821 → 1810 (d83954af)` Competitor Discovery Report
   - `1820 → 1808 (0dbe8df1)` Gemini Code Assist Quotas
   - 各 close 時に **両方向コメント追記**: closing Issue に "merged to #N" / canonical に "merged from #N"
   - PS#3 / PS#5 / Win 並列起票分を Win territory で集約

4. **`~/.claude/hooks/inject-rules.txt` に `[ISSUE-PRECHECK]` rule 追加**:
   - notebooklm 起源 → `gh issue list --search "<8文字notebook_id> in:title" --state all` で既存有無確認
   - 1+ open Issue ヒット → 既存 Issue にコメント追記で merge (新規起票しない)
   - 0 ヒット → 起票OK
   - 違反検知 = part 120 cron が daily 04:00 JST 検出
   - bc58b50b 等の playbook ID は意図的に複数 Issue OK (= ID prefix 共通だが内容別)

## commit + push (本 part 後段)

- 予定:
  - `~/.claude/hooks/inject-rules.txt` (= local file / コミット対象外 / 各 instance home dir で同期必要)
  - `memory/project_20260503_win132_part121.md`
  - ROADMAP entry

## 制約 / 学び

1. **「tool 作った直後セッションで dogfood」pattern 第 1 例** — part 120 で `notebooklm_issue_crosscheck.py` 完成 → part 121 で full scan + 9 close 実行. 「実装 → 即 dogfood → cleanup」cycle.
2. **inject-rules.txt は home dir だけにある** — 各 instance が自分の `~/.claude/hooks/inject-rules.txt` を持つ. 規律拡散には **cross-instance-pr 必要** (= part 119 と同 pattern). 本 part rule 追加分も後続 instance に告知 推奨.
3. **TRUE_GAP 53 件は cleanup ではなく triage backlog** — Issue を作るほどでない / 既に対応済 / 個別 review 必要. 全数チェック script を別途用意すれば lifecycle 管理可能 (= 次 part 候補 / cleanup_unrelated_notebooks.py).
4. **gh issue close + comment 2 段階 atomic でない** — close 後 comment が失敗する稀 race condition あり. 本 part は 9/9 成功確認済だが、将来は GraphQL mutation で 1 transaction 化候補.
5. **「bc58b50b 13 重複は OK」判断** — 同 ID prefix を含む 13 Issue は意図的 (= 各 application / dogfood / sub-task 別). 一律 dedup ではなく **「同 notebook + 同 scope」の二軸 dedup が必要** (= part 118 持ち越し dedup 精度 と統合).

## Phase 6 進化観察

- **「実装 → 自走化 → validation → 同類問題自動化 → dogfood cleanup → rule 構造化」6 phase 同セッション完結 pattern**
- part 117 codex-session-safety + part 118 session-residuals + part 120 notebooklm-crosscheck + 本 part rule 追加 = **fleet hygiene infra 4 layer 完成**
- Issue cleanup 9 件 = 1 セッションで Issue queue 整理 +0 起票 (= 純粋 cleanup)

## 次回 candidate

- inject-rules.txt の `[ISSUE-PRECHECK]` rule を全 instance に cross-instance-pr で告知
- TRUE_GAP 53 件の triage classifier script (= cleanup 不要 / 既対応 / 要 Issue を分類)
- bc58b50b の 13 重複の「同 scope dedup」(= playbook 全体 vs 個別 application Issue)
- `gh issue close + comment` の atomic 化 (= GraphQL mutation 1 trans)

## 連続 dogfood

49 part 連続 (= part 73-121). Phase 6 第 10 例 / 「tool 作直後 dogfood」pattern 第 1 例 / 同セッション 6 phase 完結 pattern 第 1 例.
