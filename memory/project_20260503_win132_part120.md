---
name: Win版#132 part 120 — NotebookLM × Issue Cross-Check 自動化 (manual triage の Phase 2 自走化)
description: 2026-05-03 Win版#132 part 120. User 同一要望 9 度目 (Phase 6 自律 cycle 第 9 例). part 115/117/119 で 3 回繰り返した manual triage を本 part で完全自動化. scripts/notebooklm_issue_crosscheck.py 新規 (~250 行 / dependency-free / TRUE_GAP + DUP_OPEN 分類) + .github/workflows/notebooklm-issue-crosscheck.yml (daily 04:00 JST + Issue #1647 comment + TRUE_GAP ≥3 で Issue 自動作成). 初回 smoke test (= 25 notebook) で TRUE_GAP 1 + DUP_OPEN 9 検出 = 4 instance 並列重複の構造的問題 surface.
type: project
---

# Win版#132 part 120 (2026-05-03)

## 軸: manual triage の Phase 2 自走化 + 4 instance 並列重複問題 surface

User 同一要望 **9 度目**. Phase 6「定常自律実行」第 9 例. 本 part の特徴: **part 115/117/119 で 3 回繰り返した manual NotebookLM triage を本 part で完全自動化** = 「3 回手動 → 4 回目で自動化」観察 pattern.

## 完了

1. **bc58b50b 既適用 reaffirm** — `notebook_count=97` / `harness_found=true`.

2. **`scripts/notebooklm_issue_crosscheck.py` 新規** (~250 行 / dependency-free):
   - `notebooklm list --json` から全 notebook を fetch
   - 各 notebook の **8 文字 ID** を `gh issue list --search "<id> in:title"` で検索
   - 結果を 3 分類: **TRUE_GAP** (= Issue 無し / 手動 triage 必須) / **DUP_OPEN** (= 2+ open Issue / cleanup 候補) / **OK**
   - `--json` / `--gaps-only` / `--dup-only` / `--comment-on-issue <N>` flag 対応
   - `--limit` safety cap (default 200)

3. **`.github/workflows/notebooklm-issue-crosscheck.yml` 新規**:
   - daily 04:00 JST cron (= 全 instance wrap-up + residuals-sync 後)
   - workflow_dispatch で `limit` / `comment_target` 切替
   - `NOTEBOOKLM_STORAGE_STATE_JSON` secret 不在時は warning + exit (= Issue #1724 と同 secret 依存)
   - TRUE_GAP ≥3 件検出時 → 専用 Issue 自動作成 (dedup 24h)

4. **初回 smoke test** (limit 25):
   - **TRUE_GAP 1**: `f167dcc3` Multi-Agent Convergence (= 実は part 116 で動画化 + #1704 で戦略 batch covered だが、Issue title に 8 文字 ID 含まないため検出漏れ — script 限界明示)
   - **DUP_OPEN 9**: bc58b50b (12 重複!) / 9b2e686f / 6deda071 / 9b8885ef / 1aced136 / f985728b / c60da02b / d83954af / 0dbe8df1
   - **fleet hygiene problem surface**: 4 instance (= Win/PS#3/PS#5/Codex) が並列で同 notebook を Issue 化 → 規律不在
   - bc58b50b は Issue 12 件 (= playbook 直適用 + AI tool 言及 + dogfood 等) で title 共通 8 文字含むため意図的に多い

## commit + push (本 part 後段)

- 予定:
  - `scripts/notebooklm_issue_crosscheck.py`
  - `.github/workflows/notebooklm-issue-crosscheck.yml`
  - `memory/project_20260503_win132_part120.md`
  - ROADMAP entry

## 制約 / 学び

1. **「3 回手動 → 4 回目で自動化」pattern 観察** — part 115 (= 8 本) + part 117 (= 2 本) + part 119 (= 7 本) で計 17 本を 3 セッションかけて手動 triage. **この時点で「自動化すべき」signal 明確化**. 本 part で着地. 次回からこの判断 latency を縮める指標.
2. **title-search dedup の限界** — Issue title が必ずしも 8 文字 ID を含まない (= e.g. #1704 戦略 batch / #1750 multi-notebook batch). **embedding similarity / body content scan** が必要 (= part 118 持ち越し dedup 精度向上 と統合).
3. **DUP_OPEN 9 件は fleet 規律問題** — 同 notebook を 4 instance が並列で Issue 化. **memo commit pattern cross-instance-pr (= part 119)** + **本 part の cross-check 自動化** で「重複 surface → cleanup」cycle 確立. 各重複 Issue 主候補 (= 番号最小) を残して残り close する作業を後続 instance に handoff 推奨.
4. **dependency-free script pattern 第 4 例** — `ai_tool_watch.py` + `codex_session_check.py` + `session_residuals_to_issue.py` + 本 `notebooklm_issue_crosscheck.py`. stdlib + subprocess (gh + notebooklm CLI) のみで動作. GHA / local / Codex / Claude Code 全環境互換.

## Phase 6 進化観察

- 「**実装 → 自走化分離 → dispatch validation → 同類問題の自動化**」4 phase 同セッション完結 pattern 第 1 例
- part 117 (codex-session-safety) + part 118 (session-residuals) + part 120 (notebooklm-crosscheck) で **3 つの定常 cron 自動化 infra** が完結
- 各 cron が tracking Issue #1647 / #1422 に comment して fleet 全体に visibility 提供

## 次回 candidate (= 全 GitHub Issue 化候補 / 一部既出)

- DUP_OPEN 9 件の cleanup (= 重複 Issue close 作業 / Win or Codex#2 担当)
- title-search dedup の限界対応 (= body 文中 ID scan + embedding similarity)
- 4 instance 並列重複防止のための **「Issue 起票前 cross-check 必須」rule** を inject-rules.txt に追加 (= cross-instance-pr 候補)
- notebooklm-issue-crosscheck.yml の初回 cron 自走確認 (= 明日 04:00 JST 後)

## 連続 dogfood

48 part 連続 (= part 73-120). Phase 6 第 9 例 / template Phase 1→2 dogfood 第 10 例 / dependency-free script 第 4 例 / 「3 回手動 → 自動化」pattern 第 1 例 / 同セッション 4 phase 完結 pattern 第 1 例.
