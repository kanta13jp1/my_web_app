name: my_web_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.0.0
  cupertino_icons: ^1.0.6
  intl: ^0.19.0                    # 追加：日付フォーマット
  flutter_markdown: ^0.7.4+1       # 追加：マークダウンレンダリング
  url_launcher: ^6.3.1             # 追加：リンククリック対応
  markdown: ^7.2.2                 # 追加：マークダウンパース
  highlight: ^0.7.0                # 追加：シンタックスハイライト

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true