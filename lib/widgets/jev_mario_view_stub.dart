import 'package:flutter/material.dart';

class JevMarioView extends StatelessWidget {
  const JevMarioView({super.key});

  @override
  Widget build(BuildContext context) => const Center(
    child: Text('Jev Mario LabはWebブラウザでご利用ください。'),
  );
}
