name: Lumen Path prototype checks
on:
  workflow_dispatch:
  push:
    branches: [codex/lumen-path-20260912]
    paths: ['web/labs/lumen-path/**', 'test/e2e/lumen_path.spec.ts', '.github/workflows/lumen-path-checks.yml']
permissions: {}
concurrency:
  group: lumen-path-${{ github.ref }}
  cancel-in-progress: true
jobs:
  prototype:
    permissions:
      contents: read
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          persist-credentials: false
      - uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version: '22'
      - run: node --test web/labs/lumen-path/model.test.mjs
      - run: node --check web/labs/lumen-path/app.mjs
      - run: npm ci --ignore-scripts
      - run: npx playwright install --with-deps chromium
      - name: Candidate browser tests
        shell: bash
        run: |
          python3 -m http.server 8765 --bind 127.0.0.1 --directory web &
          server_pid=$!
          trap 'kill "$server_pid"' EXIT
          for attempt in $(seq 1 20); do
            if curl --fail --silent http://127.0.0.1:8765/labs/lumen-path/ > /dev/null; then break; fi
            sleep 1
          done
          E2E_BASE_URL=http://127.0.0.1:8765 npx playwright test test/e2e/lumen_path.spec.ts --workers=1 --retries=0
      - uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6
        if: always()
        with:
          name: lumen-path-browser-evidence
          path: |
            test-results/
            playwright-report/
          retention-days: 7
