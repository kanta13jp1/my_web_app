name: LUMEN PATH production recording only
on:
  workflow_dispatch:
    inputs:
      production_sha:
        description: Reviewed production commit
        required: true
        type: string
permissions: {}
concurrency:
  group: lumen-recording-${{ github.ref }}
  cancel-in-progress: false
jobs:
  record:
    permissions:
      contents: read
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: '22'
      - run: npm ci --ignore-scripts
      - run: npx playwright install --with-deps chromium
      - run: sudo apt-get update && sudo apt-get install -y ffmpeg fonts-noto-cjk
      - name: Record actual public production interaction
        env:
          EXPECTED_PRODUCTION_SHA: ${{ inputs.production_sha }}
        run: node scripts/lumen-record.mjs
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        if: always()
        with:
          name: lumen-production-recording
          path: lumen-recording/
          retention-days: 14
