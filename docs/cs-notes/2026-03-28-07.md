# CS チェック 2026-03-28 07:00

## Step 0: Edge Functions UI 連携チェック

全 Edge Function について `lib/` 内の invoke 呼び出しを確認。

除外対象以外の全関数 (`ai-assistant`, `ai-search`, `ai-suggest-tags`, `analyze-reality`,
`development-achievements`, `generate-daily-challenges`, `get-admin-users`,
`get-competitor-features`, `get-growth-roadmap-progress`, `get-home-dashboard`,
`get-support-tickets`, `growth-achievement-summary`, `growth-acquisition-report`,
`growth-acquisition-signal`, `growth-command-center`, `growth-import-commit`,
`growth-import-preview`, `growth-referral`, `growth-share-signal`, `growth-weekly-digest`,
`local-election-intelligence`, `trigger-analysis`) は既に UI から呼び出し済み。

新規ページ作成: なし（全関数対応済み）

## 対応済み (FAQ返信)

なし（Supabase API がプロキシ制限により到達不可）

## 対応済み (バグ修正)

なし

## エスカレーション (要人間対応)

なし

## スキップ

- Step 1〜3: `get-support-tickets` / `reply-support-request` Edge Function への HTTP アクセスが Claude Code Web 環境のプロキシ制限 (403 host_not_allowed) により不可。ドメイン `smmkxxavexumewbfaqpy.supabase.co` は許可ホストリストに未掲載。

## Step 4: GitHub PR レビュー

オープン PR: 0件 → スキップ

## Step 5: コード品質レビュー

直近3コミットの変更Dartファイル:
- `lib/data/home_tool_catalog.dart`
- `lib/main.dart`
- `lib/pages/home_page.dart`
- `lib/pages/work_menu_page.dart`
- `lib/services/home_tool_usage_service.dart`

レビュー結果: 問題なし
- セキュリティ: SQL injection / XSS / 認証バイパスなし
- パフォーマンス: 特記事項なし（`_entries` getter は静的データのため再構築コスト軽微）
- ダミーデータ混入: なし
- TODO/FIXME コメント: なし
- flutter analyze: Flutter が環境にインストールされていないため実行不可

GitHub Issue 発行: なし

## インフラ異常

- Supabase Edge Functions (`smmkxxavexumewbfaqpy.supabase.co`): プロキシ制限により到達不可 (403 host_not_allowed)
- Firebase Hosting (`my-web-app-b67f4.web.app`): プロキシ制限により到達不可 (curl exit 56)

> ※ Claude Code Web 環境のエグレス制限が原因。実際のサービス障害ではない可能性が高い。
> 次回のインフラチェックは GitHub Actions (`daily-report.yml`) のヘルスチェック結果を参照することを推奨。
