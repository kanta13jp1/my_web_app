# CS チェック 2026-03-30 00:00

## Step 0: Edge Functions UI 連携チェック

全 Edge Function の UI 連携を確認。

- `get-public-memo-ogp` / `public-memo-share`: `public_memo_service.dart` で URL エンドポイントとして使用済み（invoke 不要）
- `get-competitor-monitoring`: `competitor_monitoring_card.dart` で invoke 済み
- その他全関数: `lib/` 内で invoke 呼び出しまたはサービス経由で連携済み

**結論**: 新規 UI ページ作成不要。変更なし。

---

## 対応済み (FAQ返信)

なし（Supabase API プロキシ制限により取得不可）

## 対応済み (バグ修正)

なし

## エスカレーション (要人間対応)

なし

## スキップ (投票0・重複など)

- **Step 1 サポートチケット取得**: Claude Code Web 環境のプロキシ制限により Supabase API (`get-support-tickets`) への接続が拒否された。チケット取得不可。

---

## Step 4: GitHub PR レビュー

オープン PR: **0件** — レビュースキップ。

---

## Step 5: コード品質レビュー

直近コミット `04ca188` で変更されたファイルをレビュー:

- `lib/pages/landing_page.dart`: デッドコード除去のみ。セキュリティ・パフォーマンス問題なし。
- `.github/workflows/deploy-prod.yml`: CI/CD 設定更新。シークレット参照は適切。
- `supabase/functions/note-comments/index.ts`: **セキュリティ問題を検出**

### 発見した問題

**[CRITICAL] note-comments: JWT署名未検証による認証バイパス脆弱性**

- ファイル: `supabase/functions/note-comments/index.ts` (行 29〜42)
- `getUserIdFromJwt()` 関数がJWTのペイロードをBase64デコードするのみで署名を検証していない
- 攻撃者が偽造JWTで任意ユーザーのコメントを閲覧・投稿・削除できる
- GitHub Issue 発行: **kanta13jp1/my_web_app#249**

---

## Step 6: インフラヘルスチェック

Claude Code Web 環境のプロキシ制限により以下の外部エンドポイントへの接続が拒否:

- `https://smmkxxavexumewbfaqpy.supabase.co/functions/v1/get-home-dashboard` → プロキシ拒否 (HTTP 403)
- `https://my-web-app-b67f4.web.app/` → プロキシ拒否 (exit code 56)

※ プロキシ制限によるもので、実際のインフラ障害ではない可能性が高い。
※ GitHub Actions `deploy-prod.yml` が正常に動作していることを確認済み（最新コミット `04ca188` でデプロイ完了）。
