# CS チェック 2026-03-26 18:00

## 対応済み (FAQ返信)
- なし

## 対応済み (バグ修正)
- なし

## エスカレーション (要人間対応)
- なし

## スキップ (投票0・重複など)
- なし

## インフラ異常

### Supabase Edge Functions 接続不可
- `GET https://smmkxxavexumewbfaqpy.supabase.co/functions/v1/get-support-tickets` → **接続失敗 (proxy 403 host_not_allowed)**
- `GET https://smmkxxavexumewbfaqpy.supabase.co/functions/v1/get-home-dashboard` → **接続失敗 (proxy 403 host_not_allowed)**

### Firebase Hosting 接続不可
- `GET https://my-web-app-b67f4.web.app/` → **接続失敗 (proxy 403 host_not_allowed)**

### 原因
実行環境 (Claude Code Remote) のエグレスプロキシが `*.supabase.co` および `*.web.app` ドメインを許可リストに含んでいないため、外部APIへの接続がブロックされました。

### 対応
- サポートチケットの取得・返信処理はスキップ (Step 1〜3)
- GitHub PR レビュー: オープンPR **0件** → スキップ (Step 4)
- インフラヘルスチェック: 上記理由により確認不可 → 本セクションに記録

### 推奨アクション
1. Claude Code Schedule を実行する環境で `*.supabase.co` および `*.web.app` を許可リストに追加するか、別の実行環境 (e.g. GitHub Actions, Supabase Cron) でこのタスクを実行してください。
2. または、サービスキーを環境変数 `SUPABASE_SERVICE_KEY` に設定したうえで、GitHub Actions ワークフローとして cs-check を移行することを検討してください。
