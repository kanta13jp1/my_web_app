# CS チェック 2026-03-28 06:00

## Step 0: Edge Function UI 連携チェック
全 Edge Function (34件) に lib/ 内での invoke 呼び出しまたはステータス管理UIが確認済み。
新規ページ作成不要。

## 対応済み (FAQ返信)
- なし (Supabase API がプロキシ制限により 403 で接続不可)

## 対応済み (バグ修正)
- なし

## エスカレーション (要人間対応)
- なし

## スキップ (投票0・重複など)
- チケット取得 API (get-support-tickets) へのアクセスがプロキシ制限 (host_not_allowed) により不可。
  実サービス障害ではなく Claude Code Web 環境のプロキシ制限によるもの。

## GitHub PR レビュー
- オープン PR: 0件。レビュー対象なし。

## コードレビュー結果 (直近3コミット変更ファイル)
対象ファイル:
- lib/pages/user_manual_page.dart
- lib/services/local_election_reality_service.dart
- lib/widgets/election_regional_kpi_chart.dart
- test/services/local_election_reality_service_test.dart

**レビュー結果: 重大な問題なし**

軽微な指摘 (GitHub Issue 発行不要レベル):
- `election_regional_kpi_chart.dart:150` の `_buildLegend` 内の色見本 `Container` に
  `Semantics` ラベルが未設定。隣接テキスト (`現職維持目標` / `新人擁立目標`) がスクリーンリーダーで
  読まれるため実用上の問題は低い。
- セキュリティ問題・パフォーマンス問題・ダミーデータ混入・TODO/FIXME コメントは検出されず。

## インフラヘルスチェック
- `get-home-dashboard`: プロキシ制限により疎通確認不可 (環境制限、実サービス障害ではない)
- `my-web-app-b67f4.web.app`: プロキシ制限により疎通確認不可 (環境制限、実サービス障害ではない)
- 実障害の証跡なし。GitHub Actions 等での別途確認を推奨。
