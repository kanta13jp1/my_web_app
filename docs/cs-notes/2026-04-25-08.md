# CS チェックノート 2026-04-25-08:00

## 実行サマリー

| 項目 | 結果 |
|---|---|
| 実行時刻 | 2026-04-25 08:xx UTC (17:xx JST) |
| サポートチケット | アクセス不可 (ネットワーク制限 / HTTP 403) |
| オープンPR | 2件 (#701, #704) — bootstrap docs のみ、問題なし |
| auto-review Issues | 0件 |
| workflow-failure Issues | 1件 → **自動修復完了** (Issue #718) |
| Edge Function UI 連携 | 50関数 / 全件連携済み (新規追加なし) |
| インフラヘルス | ネットワーク制限でアクセス不可 |
| 日次レポート | 2026-04-25.md 生成済み ✓ |

---

## Step 0: Edge Function UI 連携チェック

- `supabase/functions/` 実関数数: **50** (90未満 → デプロイOK)
- `edge_function_summary_card.dart` の `_functions` リストと照合 → **全件登録済み**
- 新規連携対象なし → スキップ

---

## Step 1-2: サポートチケット

- `get-support-tickets` EF: **アクセス不可** (プロキシ制限 / HTTP 403)
- ネットワーク制限によりSupabase EF呼び出し不可 → チケット0件扱い

---

## Step 3: CSノート記録

このファイル自体が記録。

---

## Step 4: GitHub PR レビュー

オープンPR: **2件**

| PR | タイトル | 状態 | レビュー結果 |
|---|---|---|---|
| #704 | auto: issue #514 アプリバージョン表示 + 更新プロンプト | Draft | bootstrap のみ — セキュリティ/Lint 問題なし |
| #701 | auto: issue #660 Scale EGP ファインチューニング | Draft | bootstrap のみ — セキュリティ/Lint 問題なし |

---

## Step 5: コード品質レビュー

- flutter analyze: ローカル実行不可（WEB版環境）
- 直近変更 .dart ファイル: `lib/pages/project_gantt_page.dart` (ラベル変更のみ)
- 新規 GitHub Issue 作成: なし

---

## Step 6: GitHub Issue 自動修復

### 6a: auto-review ラベル Issues
→ **0件** (対応なし)

### 6b: Edge Function UI導線チェック Issues
→ 該当なし

### 6c: workflow-failure Issues
→ **1件** (Issue #718 — Deploy to Production 失敗)

**修復内容:**
- エラー原因: migration `20260425203000_wbs_remove_all_instances_add_codex.sql` が migration `20260425210000` との DB 状態コンフリクトで失敗した可能性
- auto-repair の 14 桁バージョン検出にかからない SQL エラー（ALTER COLUMN NOT NULL / constraint 競合）
- 修正: `.github/workflows/deploy-prod.yml` の repair リストに `20260425203000` を追加
- commit: `8749a45` / Issue #718 → **クローズ済み**

---

## Step 7: Schedule タスク健全性

### 7a: 直近コミット (2時間以内, Claude Schedule)
```
05a0ff0 自動: CS チェック 2026-04-25-07:00
```
→ 07:00 CS チェック正常実行済み ✓

### 7b: 日次タスク実行確認
- `docs/daily-reports/2026-04-25.md`: **生成済み** ✓
- CS チェック: 05:00, 06:00, 07:00 UTC 実行済み ✓

### 7c: cs-notes パターン分析
- 直近CSノート: workflow-failure 1件 → 本セッションで修復済み
- 繰り返しエラーパターン: なし

### 7d: schedule_task_runs
- Supabase REST API: ネットワーク制限でアクセス不可

---

## Step 8-9: インフラヘルスチェック

| エンドポイント | 結果 |
|---|---|
| `get-home-dashboard` EF | ネットワーク制限でアクセス不可 (HTTP 403) |
| `https://my-web-app-b67f4.web.app/` | ネットワーク制限でアクセス不可 (HTTP 403) |
| `guitar-recording-studio` EF | ネットワーク制限でアクセス不可 (HTTP 403) |

→ WEB版インスタンス環境からの外部アクセス制限のため正確な判定不可。

---

## Step 10: schedule_task_runs ログ

- Supabase POST: ネットワーク制限でスキップ

---

## エスカレーション

なし

## 今回の発見・特記事項

- **workflow-failure Issue #718 自動修復完了**: migration 20260425203000 の repair エントリを deploy-prod.yml に追加 → 次回 deploy でクリーン再適用される
- PR #701/#704: github-issue-fix.yml が自動生成した bootstrap ブランチ — CI通過後マージ待ち
- 最新コミット `dd2186e` まで fetch・rebase 完了

## 次回対応推奨

- 次回 deploy-prod が正常完了するか確認（migration 203000 repair 効果確認）
- Issue #696 (Slack+Notion READY): Win版インスタンスでの実装開始を検討
- PR #704/#701: CI 通過確認後マージ
