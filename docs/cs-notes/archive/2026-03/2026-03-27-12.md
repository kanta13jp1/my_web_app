# CS チェック 2026-03-27 12:00

## 対応済み (FAQ返信)
- なし (チケット取得不可 → 下記「実行環境制限」参照)

## 対応済み (バグ修正)
- なし

## エスカレーション (要人間対応)
- なし

## スキップ (投票0・重複など)
- なし

---

## 実行環境制限 (プロキシ host_not_allowed)

Claude Code の実行環境で外部アクセスが許可ホストリストで制限されており、
以下エンドポイントへの接続が `CONNECT tunnel failed, response 403 (host_not_allowed)` で遮断された。

| エンドポイント | ステップ | 結果 |
|---|---|---|
| `smmkxxavexumewbfaqpy.supabase.co/functions/v1/get-support-tickets` | Step 1 | 403 host_not_allowed |
| `smmkxxavexumewbfaqpy.supabase.co/functions/v1/reply-support-request` | Step 2 | 未実行 (Step1失敗のため) |
| `smmkxxavexumewbfaqpy.supabase.co/functions/v1/get-home-dashboard` | Step 5 | 未実行 (同上) |
| `my-web-app-b67f4.web.app/` | Step 5 | 403 (プロキシ制限) |

**対処**: ローカル環境または CI/CD 環境 (GitHub Actions) から cs-check を実行することを推奨。
Claude Code Web/Remote 実行環境ではプロキシが supabase.co / web.app への接続を許可していない。

## GitHub PR レビュー (Step 4)
- オープン PR: **0件** → スキップ

## インフラ異常
- `my-web-app-b67f4.web.app/` → HTTP 403 (プロキシによる接続遮断のため実インフラ異常の判定不可)
- Supabase Edge Functions → HTTP 403 (同上)
- **要確認**: 実環境からの手動ヘルスチェック推奨
