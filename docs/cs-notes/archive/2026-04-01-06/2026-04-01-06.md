# CS チェック 2026-04-01 06:00

## Step 0: Edge Function UI 自動連携

カード未登録の Edge Function を特定し、最大3件の UI ページを新規作成:

- `customer-feedback` → `/customer-feedback` → `CustomerFeedbackPage` 追加
- `address-book` → `/address-book` → `AddressBookPage` 追加
- `subscription-billing` → `/subscription-billing` → `SubscriptionBillingPage` 追加

`lib/main.dart` に import・route 追加、`edge_function_summary_card.dart` に `_FnDef` 3件追加。
カード未登録関数: 28件確認 (うち今回3件対応)

## 対応済み (FAQ返信)

- なし (Supabase エンドポイントがサンドボックスプロキシにより 403 でブロック)

## 対応済み (バグ修正)

- なし

## エスカレーション (要人間対応)

- なし

## スキップ (投票0・重複など)

- なし

## インフラ状況

- Supabase エンドポイント: サンドボックス環境のプロキシ制限 (host_not_allowed) により到達不可
  - `get-support-tickets`: 503 (proxy blocked)
  - `get-home-dashboard`: 503 (proxy blocked)
  - Firebase Hosting: 未確認 (同様の制限の可能性あり)
- GitHub PRs: gh CLI 未インストールのため確認不可
- flutter analyze: Flutter SDK 未インストールのため確認不可

## 備考

- 全外部 HTTP リクエストがサンドボックスネットワークポリシー (403 x-deny-reason: host_not_allowed) によりブロックされている
- コード変更のみ実施・コミット
