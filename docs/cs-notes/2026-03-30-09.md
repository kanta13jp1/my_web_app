# CS チェック 2026-03-30 09:00

## Step 0: Edge Function UI 連携チェック

全 Edge Function を確認した結果、すべての関数に UI 呼び出しが存在するか除外対象に該当することを確認。
新規ページの作成は不要。

| 確認結果 | 関数数 |
|---|---|
| UI invoke 済み | 27 関数 |
| 除外対象 (サーバーサイド専用) | 13 関数 |
| 新規ページ作成が必要 | 0 関数 |

## Step 1: サポートチケット取得

**結果**: ネットワーク到達不可 (Claude Code Web 環境のプロキシ制限)
- `get-support-tickets` Edge Function への接続失敗 (curl exit 56)
- チケット対応はスキップ

## 対応済み (FAQ返信)
- なし (ネットワーク制限によりスキップ)

## 対応済み (バグ修正)
- なし

## エスカレーション (要人間対応)
- なし

## スキップ (投票0・重複など)
- なし

## Step 4: GitHub PR レビュー

オープンPR: **0件** — レビュー不要

## Step 5: コードレビュー (直近3コミット変更ファイル)

対象ファイル:
- `lib/data/home_tool_catalog.dart`
- `lib/main.dart`
- `lib/pages/landing_page.dart`
- `lib/pages/team_workspace_page.dart`
- `lib/services/local_election_share_service.dart`

### `lib/pages/team_workspace_page.dart` レビュー結果

| 観点 | 結果 |
|---|---|
| セキュリティ | ✅ 問題なし (userId は auth から取得、Supabase クライアント経由でクエリ) |
| パフォーマンス | ✅ 問題なし (必要なデータのみ select、mounted チェック済み) |
| アクセシビリティ | ✅ 問題なし |
| TODO/FIXME | なし |
| ダミーデータ | なし |

### `lib/services/local_election_share_service.dart` レビュー結果

| 観点 | 結果 |
|---|---|
| セキュリティ | ✅ 問題なし (userId は auth から取得、null チェック済み) |
| パフォーマンス | ✅ 問題なし |
| アクセシビリティ | 対象外 (サービス層) |
| TODO/FIXME | なし |
| ダミーデータ | なし |

**GitHub Issue 発行**: 問題なし → スキップ

## Step 6: インフラヘルスチェック

**結果**: ネットワーク到達不可 (Claude Code Web 環境のプロキシ制限)
- `get-home-dashboard`: 接続失敗 (curl exit 56)
- `https://my-web-app-b67f4.web.app/`: 接続失敗 (curl exit 56)

※ 環境のネットワーク制限によるもので、実際のインフラ障害ではない可能性が高い。
  本番サービスの実際の状態は手動で確認することを推奨。

## インフラ異常 (環境制限)

| エンドポイント | ステータス | 備考 |
|---|---|---|
| `get-home-dashboard` | 接続不可 | Claude Web 環境プロキシ制限 |
| `https://my-web-app-b67f4.web.app/` | 接続不可 | Claude Web 環境プロキシ制限 |

実際の障害ではない可能性が高い。次回の GitHub Actions または手動確認を推奨。
