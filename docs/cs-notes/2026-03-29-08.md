# CS チェック 2026-03-29 08:00

## Step 0: Edge Function UI 連携チェック

全 Edge Function について UI invoke 呼び出しの有無を確認。

**結果: 全関数カバー済み — 新規ページ作成不要**

- `public-memo-share`: サーバーサイド専用 (OGP シェア URL リダイレクト) → UI 不要
- `get-public-memo-ogp`: `/public-memos` ページでカバー済み
- その他全関数: `lib/pages/` または `lib/widgets/` の invoke 呼び出しで対応済み

## Step 1: サポートチケット取得

**結果: API アクセス不可 (プロキシ制限)**

Supabase Edge Function (`get-support-tickets`) への接続が環境のプロキシにより 403 でブロックされたため、チケット取得不可。

## 対応済み (FAQ返信)
- なし (API 接続不可のため)

## 対応済み (バグ修正)
- なし

## エスカレーション (要人間対応)
- なし

## スキップ
- チケット取得 API 接続不可 (ホスト `smmkxxavexumewbfaqpy.supabase.co` がプロキシ許可リスト外)

## Step 4: GitHub PR レビュー

オープン PR: **0件** — スキップ

## Step 5: コードレビュー

直近3〜5コミットで変更された Dart ファイル: **0件** (すべて自動スケジュールの CS チェックコミットのみ)

`flutter` コマンドが PATH に存在しないため `flutter analyze` は実行不可。

## インフラ異常

以下のエンドポイントがプロキシ制限により 403:

- `https://smmkxxavexumewbfaqpy.supabase.co/functions/v1/get-home-dashboard` → **403 (プロキシブロック)**
- `https://my-web-app-b67f4.web.app/` → **403 (プロキシブロック)**

※ 環境の制限によるもの。本番インフラ側の異常ではない可能性が高い。
