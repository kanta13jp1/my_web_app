# CS チェック 2026-03-28 14:00

## Step 0: Edge Functions UI 連携チェック

全 Edge Function について UI 呼び出し (invoke / 直接 HTTP) の有無を確認。

- 新規 UI 連携が必要な未接続関数: **なし**
- note-comments: note_editor_page.dart で HTTP 直接呼び出し ✓
- memo-reactions: public_memo_detail_page.dart で HTTP 直接呼び出し ✓
- public-memo-share: サーバーサイド専用 (除外対象) ✓
- get-public-memo-ogp: public_memo_service.dart で URL 構築 ✓
- growth-import-preview / growth-import-commit: import_service.dart で invoke ✓

対応: **なし (全関数カバー済み)**

## 対応済み (FAQ返信)

なし — Supabase API へのアクセスがプロキシ制限 (host_not_allowed) により不可。
チケット0件扱いとして Step 4 以降へスキップ。

## 対応済み (バグ修正)

なし

## エスカレーション (要人間対応)

なし

## スキップ (プロキシ制限)

- get-support-tickets: 403 host_not_allowed (Claude Code Web 環境のプロキシ制限)

## Step 4: GitHub PR レビュー

オープン PR: **0件** — レビュー不要

## Step 5: コード品質レビュー

直近3コミットで変更されたファイル:
- `lib/pages/home_page.dart` — ReferralShareCard を ProfileCompletionBanner 直下に追加
- `lib/pages/note_editor_page.dart` — withOpacity → withValues(alpha:) 移行

確認観点:
- セキュリティ: 問題なし (`_delete()` / `_submit()` でトークン確認あり)
- パフォーマンス: 問題なし
- アクセシビリティ: 問題なし
- TODO/FIXME: なし
- ダミーデータ: なし

GitHub Issue 発行: **なし**

## インフラヘルスチェック

- `get-home-dashboard` Edge Function: プロキシ制限により接続不可 (host_not_allowed) — 実際のサービス障害ではなく環境制限
- `https://my-web-app-b67f4.web.app/`: プロキシ制限により接続不可 (host_not_allowed) — 実際のサービス障害ではなく環境制限

インフラ異常: **判定不可 (プロキシ制限)** — 実サービス障害の可能性は低い
