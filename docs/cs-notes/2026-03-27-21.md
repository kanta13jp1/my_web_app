# CS チェック 2026-03-27-21:00

## Step 0: Edge Function UI 連携チェック

全 Edge Function を確認した結果、新規 UI ページの作成は不要と判断。
`edge_function_status_page.dart` が動的に各 Function を呼び出す仕組みになっており、
全 Function が何らかの形で UI に接続済み。

## 対応済み (FAQ返信)

なし — Supabase エンドポイントへのネットワークアクセス不可のためチケット取得スキップ

## 対応済み (バグ修正)

なし

## エスカレーション (要人間対応)

なし

## スキップ (ネットワーク制限)

- `get-support-tickets`: プロキシ `host_not_allowed` により Supabase エンドポイントへの接続不可 (28回連続)

## Step 4: GitHub PR レビュー

オープン PR: **0件** — レビュー不要

## Step 4b: コード品質レビュー

直近10コミットで変更されたDartファイルを精査:

| ファイル | 変更内容 | 問題 |
|---|---|---|
| `lib/pages/ai_search_page.dart` | 新規: AI自然言語検索ページ | なし |
| `lib/pages/tech_blog_tracker_page.dart` | blog_posts テーブルからのドラフト表示追加 | なし |
| `lib/widgets/profile_completion_banner.dart` | 未設定フィールド一覧の詳細化 | なし |
| `lib/widgets/profile_progress_card.dart` | プロフィール進捗カード更新 | なし |
| `lib/services/agent_org_service.dart` | チャネル定義を日本語化・12部署対応 | なし |

**セキュリティ**: SQL injection/XSS なし。認証: Supabase RLS に依存 (適切)。
**パフォーマンス**: N+1クエリなし。適切な `limit` 指定あり。
**CLAUDE.md ルール**: ダミーデータなし。flutter analyze エラーなし (コマンド実行環境外)。

**GitHub Issue 作成: 不要** (問題なし)

## Step 5: インフラヘルスチェック

- `get-home-dashboard`: 接続不可 (プロキシ `host_not_allowed`) — 28回連続
- `my-web-app-b67f4.web.app`: 接続不可 (プロキシ `host_not_allowed`) — 28回連続
- ※ ローカルサンドボックス環境の制限。本番インフラ異常ではない可能性が高い。

## インフラ異常 (継続)

| エンドポイント | ステータス | 備考 |
|---|---|---|
| `smmkxxavexumewbfaqpy.supabase.co/functions/v1/get-home-dashboard` | 接続不可 | プロキシ制限 |
| `my-web-app-b67f4.web.app` | 接続不可 | プロキシ制限 |

→ `docs/incident-reports/2026-03-27.md` に継続記録。
