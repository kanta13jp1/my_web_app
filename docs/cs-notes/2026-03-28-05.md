# CS チェック 2026-03-28 05:00

## 対応済み (FAQ返信)
- なし

## 対応済み (バグ修正)
- なし

## エスカレーション (要人間対応)
- なし

## スキップ (投票0・重複など)
- なし

## Step 0: Edge Function UI 連携チェック
- 以下の関数はサービス層経由で既にUIから呼び出されており、新規ページ作成不要:
  - `growth-achievement-summary` → admin_analytics_page.dart (L4468)
  - `growth-acquisition-report` → growth_mission_service.dart (L874)
  - `growth-command-center` → growth_mission_service.dart (L1009)
  - `growth-weekly-digest` → growth_mission_service.dart (L1051)
  - `ai-suggest-tags` → バックエンドSVG生成サービス (ページ不要)
  - `growth-share-signal` → イベントトラッカー (ページ不要)

## Step 1: サポートチケット取得
- 結果: プロキシ制限により Supabase Edge Function へのアクセス不可 (403 host_not_allowed)
- チケット件数: 不明 (APIアクセス不可)

## Step 4: GitHub PR レビュー
- オープンPR: 0件 (対応不要)

## Step 5: コード品質レビュー
直近3コミットで変更されたDartファイルを確認:
- `lib/main.dart` → 問題なし (ルーティング標準実装)
- `lib/models/local_election_plan.dart` → 問題なし (fromJson型安全、nullチェック済み)
- `lib/pages/agent_org_page.dart` → 問題なし (StatefulWidget標準実装)
- `lib/pages/election_victory_page.dart` → 問題なし (mounted チェック適切)
- `lib/services/agent_org_service.dart` → 問題なし (Supabase標準呼び出し)
- `lib/services/local_election_plan_service.dart` → 問題なし (SharedPreferences適切使用、例外処理済み)
- `test/services/local_election_plan_service_test.dart` → 問題なし (テスト適切)

セキュリティ/パフォーマンス/アクセシビリティ/ダミーデータ問題: **なし**

## インフラ異常
- Supabase Edge Functions (get-support-tickets, get-home-dashboard): プロキシ制限 (host_not_allowed) によりアクセス不可
- Firebase Hosting (https://my-web-app-b67f4.web.app/): プロキシ制限によりアクセス不可
- ※ Claude Code Web 環境のプロキシポリシーによる制限。本番サービス自体の異常ではない可能性が高い。
