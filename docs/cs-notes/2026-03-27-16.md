# CS チェック 2026-03-27 16:00

## Step 0: Edge Function UI 連携チェック

全 Edge Function を確認した結果、新規UIページの作成は不要と判断。

| 関数 | 状態 |
|------|------|
| ai-assistant | ai_assistant_menu.dart 経由で呼び出し済み |
| ai-search | ai_service.dart 経由で呼び出し済み |
| ai-suggest-tags | ai_service.dart 経由で呼び出し済み |
| analyze-reality | real_world_danshari_page.dart / reality_check_page.dart 経由 |
| development-achievements | development_achievements_card.dart 経由 |
| generate-daily-challenges | daily_challenge_card.dart 経由 |
| generate-quote-image | daily_motivation_card.dart 経由 (直接URL) |
| get-admin-users | admin_analytics_page.dart 経由 |
| get-competitor-features | growth_roadmap_progress_card.dart 経由 |
| get-growth-roadmap-progress | growth_roadmap_progress_card.dart 経由 |
| get-home-dashboard | home_page.dart 経由 |
| get-public-memo-preview | public_memo_service.dart 経由 |
| share-quote | daily_motivation_card.dart 経由 (直接URL) |
| trigger-analysis | election_strategy_page.dart 経由 |
| growth-* 系 | growth_mission_service.dart / growth_acquisition_service.dart 経由 |
| daily-judgment | サーバー専用 (夜間スケジュール実行) → 除外 |
| health-check | サーバー専用 (インフラ監視) → 除外 |
| check-competitor-updates | サーバー専用 (スケジュール実行) → 除外 |
| schedule-daily-digest | サーバー専用 (Claude Schedule) → 除外 |
| get-support-tickets | サーバー専用 (CS自動化) → 除外 |
| reply-support-request | サーバー専用 (CS自動化) → 除外 |
| post-x-update | サーバー専用 (X自動投稿) → 除外 |
| notify-feature-request | サーバー専用 (メール通知) → 除外 |
| send-waitlist-notification | サーバー専用 (メール通知) → 除外 |
| agent-runtime-cycle | サーバー専用 → 除外 |
| get-ogp | 除外 (get-ogp は除外対象) |

## 対応済み (FAQ返信)

なし — ネットワーク制限により Supabase Edge Function へのアクセス不可
(プロキシが host_not_allowed エラーを返却: HTTP 403)

## 対応済み (バグ修正)

なし

## エスカレーション (要人間対応)

なし

## スキップ

- Step 1 (get-support-tickets): ネットワーク制限により API 呼び出し不可
- Step 5 (インフラヘルスチェック): ネットワーク制限により API 呼び出し不可

## GitHub PR レビュー

オープン PR: なし (指摘事項なし)

## インフラヘルスチェック

実行不可 — サンドボックス環境のネットワークプロキシが外部Supabase/Firebase へのアクセスをブロック。
次回は環境側の制限解除後に再実行してください。
