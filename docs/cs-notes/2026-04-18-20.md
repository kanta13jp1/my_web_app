# CS チェック 2026-04-18 20:00

## 対応済み (Edge Function UI自動連携)
- `affiliate-marketing` → /affiliate-marketing ページ接続済み
- `ai-image-generator` → /ai-image-generator ページ接続済み
- `ai-secretary` → /ai-secretary ページ接続済み
- edge_function_summary_card.dart に3件追加 (計34件 UI接続済み)

## サポートチケット対応
- API接続不可 (プロキシ制限403): チケット取得スキップ (0件扱い)

## インフラ異常
- **get-home-dashboard**: 403 (プロキシ制限、実環境での動作確認は別途必要)
- **guitar-recording-studio**: 403 (プロキシ制限)
- **Firebase Hosting (my-web-app-b67f4.web.app)**: 403 (プロキシ制限)
- ※ これらはWEB版のネットワーク制限によるもの。実サービスへの影響は不明。VSCode版での確認を推奨。

## 重大: Deploy to Production CI 連続失敗 (要対応)
- **発生中**: Issue #462〜#480 計20件以上が workflow-failure ラベルで蓄積中
- **根本原因の仮説**: `dart format --set-exit-if-changed .` が強制チェック (continue-on-error: false)
  - 自動cs-check / Schedule タスクが dart format を実行せずにコミットしている
  - 各コミットで format差分が発生 → CI ハードフェイル
- **直近の修正試み**: VSCode版#104 (commit 5a0f312) で prefer_const を warning に降格したが、
  CI はまだ失敗中 (run 24612912064)
- **推奨対応**: VSCode版またはWindows版で `dart format .` を実行してプッシュすることで
  フォーマット差分をゼロにしてCI を通す

## スキップ
- PR レビュー: オープンPR なし
- flutter analyze: WEB版では flutter コマンド利用不可

## GitHub Issues 状況
- オープン Issues: 41件 (20件以上が workflow-failure)
- Issue #479: Edge Function UI導線チェック 109件未接続 → 今回3件対応
- workflow-failure Issues (#462〜#480): CI修復は VSCode版に依頼推奨
