# インシデントレポート 2026-07-21 21:33 JST

**検出元**: scheduled task `infra-health-check` (毎時)
**本番影響**: **なし** (アプリ・DB・デプロイすべて正常)
**分類**: ローカル作業ツリーのファイル破損 (未コミット / 未 push)

---

## 1. 定常ヘルスチェック結果 — すべて正常

### Edge Function `health-check`

`GET https://smmkxxavexumewbfaqpy.supabase.co/functions/v1/health-check` → HTTP 200

```json
{
  "status": "healthy",
  "checks": {
    "env":                { "ok": true, "detail": "SUPABASE_URL + SERVICE_ROLE_KEY set" },
    "db_connectivity":    { "ok": true, "latencyMs": 1255 },
    "wbs_tasks":          { "ok": true },
    "ai_circuit_breaker": { "ok": true }
  },
  "timestamp": "2026-07-21T12:33:43.742Z"
}
```

- `verify_jwt = false` の公開エンドポイントのため service key なしで到達 (機微情報は返さない設計)。
- **観測**: `db_connectivity` = 1255ms。`ok=true` だが `head:true` の count クエリとしてはやや遅い。
  part340 の `agent_memories` statement timeout (8.12s) の前例があるため、次回以降も 1s 超が続くなら
  EXPLAIN ANALYZE を取ること (ブラインド index 追加は禁止 / `feedback_db_timeout_needs_explain_not_blind_index`)。
  今回は単発観測のため対応不要。

### Firebase Hosting

- <https://my-web-app-b67f4.web.app/> → 正常表示 (タイトル「自分株式会社 | AIが今日の最優先タスクを整理」)。
- `version.json` = `1.0.1728` / build 4901 / commit `70c2c055` / deployed 2026-07-21T07:57:42Z。
- `git merge-base --is-ancestor 70c2c055 origin/main` → **OK** (origin/main の祖先)。以降 4 commit 分のラグは通常範囲。
- GitHub Actions 直近 12 run はすべて success / skipped。失敗なし。

---

## 2. 検出した異常 — tracked ファイル 8 本が 0 バイトに切り詰められている

ヘルスチェックの副産物として発見。`.claude/settings.json` が 0 バイトだったことから全体を走査した。

| HEAD のサイズ | ファイル |
|---:|---|
| 41,302 B | `.github/workflows/deploy-prod.yml` |
| 16,383 B | `.github/workflows/ci.yml` |
| 12,309 B | `.claude/settings.json` |
| 10,811 B | `.claude/commands/wrap-up.md` |
| 9,268 B | `.github/workflows/deploy-staging.yml` |
| 7,086 B | `.github/workflows/horse-racing-update.yml` |
| 4,690 B | `.github/workflows/claude-session-hygiene-cron.yml` |
| 963 B | `.github/workflows/memory-search-sync.yml` |

合計 **約 103 KB が作業ツリー上で消失**。リポジトリ全体を `git ls-files` で走査した結果、
0 バイト化しているのはこの 8 本のみ (他は無傷)。

### 単一の自動処理による破損である根拠

8 本すべての mtime が **52 ミリ秒の窓に収まっている**:

```
03:30:25.579  .claude/commands/wrap-up.md
03:30:25.581  .claude/settings.json
03:30:25.627  .github/workflows/ci.yml
03:30:25.627  .github/workflows/deploy-prod.yml
03:30:25.627  .github/workflows/claude-session-hygiene-cron.yml
03:30:25.628  .github/workflows/deploy-staging.yml
03:30:25.630  .github/workflows/horse-racing-update.yml
03:30:25.631  .github/workflows/memory-search-sync.yml
```

人手の編集ではありえない。2026-07-21 03:30:25 JST に走った自動処理が
`.claude/` と `.github/workflows/` を対象に一括で空書き込みしたと判断する。
同じ git status に `D .claude/scheduled_tasks.lock` が出ており、同時刻帯の
scheduled task 系処理が関与している可能性がある (未特定)。

### 被害範囲は限定的 — origin/main は無傷

`origin/main` 上の同 8 ファイルはすべて完全な内容を保持している:

```
42,556 B  .github/workflows/deploy-prod.yml
17,413 B  .github/workflows/ci.yml
12,371 B  .claude/settings.json
11,113 B  .claude/commands/wrap-up.md
 9,497 B  .github/workflows/deploy-staging.yml
 7,133 B  .github/workflows/horse-racing-update.yml
 5,003 B  .github/workflows/claude-session-hygiene-cron.yml
 1,025 B  .github/workflows/memory-search-sync.yml
```

GitHub Actions は origin 上の定義で走るため、**本番デプロイ・CI に影響はない**
(実際に直近 run はすべて success)。破損は未コミットのローカル作業ツリーに閉じている。

### 残存リスク (これが本件の要点)

現在のブランチは `codex/issue-1215-hitl` で、この 8 本は `git status` 上 **`M` (modified)** として並んでいる。
ここで `git add -A` / `git commit -a` が走ると、**空の `deploy-prod.yml` と `ci.yml` がコミットされ、
マージされた時点で本番デプロイと CI が停止する**。着地前に必ず解消すること。

---

## 3. 本タスクでの対応方針 — report-only (修復コマンド未実行)

以下の理由から**自動修復は行わなかった**:

1. 現在のブランチが `codex/issue-1215-hitl` = 他インスタンス (Codex) の進行中 WIP。
   運用ルール `feedback_scheduled_task_codex_wip_observe_only`
   (= scheduled task が main 以外 + foreign WIP 上にいる場合は report-only) に該当する。
2. `.claude/settings.json` には `hooks` と `permissions` が含まれる。
   ユーザー不在の自動実行でパーミッション/フック設定を書き戻すのは適切でない。
3. 本番影響がゼロであり、緊急復旧の必要がない。

### 推奨復旧手順 (ユーザーまたは当該ブランチ担当が実行)

作業ツリーの 8 本は 0 バイトなので、**上書きで失われる内容は存在しない**。

```bash
git checkout origin/main -- \
  .github/workflows/deploy-prod.yml \
  .github/workflows/ci.yml \
  .github/workflows/deploy-staging.yml \
  .github/workflows/horse-racing-update.yml \
  .github/workflows/claude-session-hygiene-cron.yml \
  .github/workflows/memory-search-sync.yml \
  .claude/settings.json \
  .claude/commands/wrap-up.md
```

復旧後の検証:

```bash
# 8 本すべてが非 0 バイトであること
git ls-files -z | while IFS= read -r -d '' f; do
  [ -f "$f" ] && [ ! -s "$f" ] && echo "STILL EMPTY: $f"
done

# settings.json が有効な JSON であること
python -c "import json;json.load(open('.claude/settings.json',encoding='utf-8'))" && echo OK
```

なお `origin/main` 版はこのブランチの HEAD 版より新しい (例: deploy-prod.yml 41,302 → 42,556 B)。
このブランチ固有の変更を意図していた場合は `git checkout HEAD -- <path>` を使い分けること。

### 追って調べるべきこと

- 03:30:25 JST に何が走ったかの特定 (`.claude/scheduled_tasks.lock` の削除と同一処理か)。
  対象が `.claude/` + `.github/workflows/` に限られている点から、両ディレクトリを
  走査する種類の自動処理が候補。
- 再発した場合は同じ 52ms 窓パターンが出るため、mtime クラスタが判定に使える。

---

## 付記: ブランチ間の settings.json 差分 (軽微 / 本インシデントとは独立)

`.claude/settings.json` の `env` キーがブランチ間で異なる:

- `origin/main`: `MCP_TIMEOUT` / `MAX_MCP_OUTPUT_TOKENS` / `ENABLE_MCP_TOOL_SEARCH` / **`NOTEBOOKLM_HOME`** (4 個)
- 本ブランチ HEAD (`codex/issue-1215-hitl`): 上記のうち `NOTEBOOKLM_HOME` を欠く (3 個)

`CLAUDE.md` の「`NOTEBOOKLM_HOME=~/.notebooklm-gmail` を settings.json env で自動適用」という記述は
**origin/main に対しては正しい**。本ブランチが `NOTEBOOKLM_HOME` 追加より前に切られた
(あるいは意図せず落とした) だけと見られる。上記の推奨復旧手順は `origin/main` から取り直すため、
この差分も同時に解消される。
