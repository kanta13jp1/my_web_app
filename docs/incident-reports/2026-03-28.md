# インシデントレポート 2026-03-28

## 概要

**種別**: ネットワーク接続不可 (継続中 — 2日連続)
**影響範囲**: CS自動化スケジュール全体 (cs-check タスク)
**発生時刻**: 2026-03-27 00:00〜2026-03-28 00:00 (継続中)
**優先度**: 高 (CS自動化の外部通信が停止中)

## 発生状況

Claude Code Schedule のサンドボックス環境において、外部ネットワーク接続が引き続きブロックされています。
2026-03-27 のインシデントレポートから継続しており、累計 30 回以上のCSチェックが影響を受けています。

```
curl https://smmkxxavexumewbfaqpy.supabase.co/... → HTTP 403 (proxy: host_not_allowed)
curl https://my-web-app-b67f4.web.app/           → HTTP 403 (proxy: host_not_allowed)
```

プロキシのエグレス制御 (`allowed_hosts`) に `smmkxxavexumewbfaqpy.supabase.co` および
`my-web-app-b67f4.web.app` が含まれていないため、通信不可。

## 影響を受けた機能

| 機能 | 状態 |
|------|------|
| サポートチケット取得 (`get-support-tickets`) | 実行不可 |
| チケット返信 (`reply-support-request`) | 実行不可 |
| X 自動投稿 (`post-x-update`) | 実行不可 |
| インフラヘルスチェック (`get-home-dashboard`) | 実行不可 |
| Firebase Hosting 疎通確認 | 実行不可 |

## 影響なし

- GitHub PR レビュー (MCP 経由で動作確認済み)
- コード品質チェック
- CS ノート記録・コミット

## 対応状況

- 2026-03-27: 初回インシデントレポート作成
- 2026-03-28 00:00: 継続中として記録更新
- 2026-03-28 01:00: 継続中 (累計32回のCSチェックが影響) — self-heal記録
- 2026-03-28 03:00: 継続中 (累計33回のCSチェックが影響) — self-heal記録

## 推奨アクション (人間対応必要)

1. **Claude Code Schedule の実行環境設定を確認**: プロキシの `allowed_hosts` に以下のドメインを追加する:
   - `smmkxxavexumewbfaqpy.supabase.co`
   - `my-web-app-b67f4.web.app`
2. または Schedule 実行環境でネットワーク制限を解除する。
3. 手動で未対応チケットを Supabase ダッシュボードから確認することを推奨。
