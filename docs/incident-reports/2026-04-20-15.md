# Incident Report 2026-04-20 15:00 JST

## Summary
`health-check` Edge Function returns 404 — deleted during EF cleanup (#120), not redeployed.

## Findings

| Target | Status | Detail |
|--------|--------|--------|
| Firebase Hosting (https://my-web-app-b67f4.web.app/) | ✅ 200 OK | Flutter app loading normally |
| `health-check` EF | ❌ 404 NOT_FOUND | Deleted in commit `a6c092fb` (EF cleanup 第1弾) |
| SUPABASE_SERVICE_KEY | ⚠️ Not set in scheduler env | Authenticated EFs (`get-home-dashboard`, `development-achievements`) not testable |

## Root Cause
`health-check` EF was intentionally deleted in EF cleanup phase 1 (Win版#120) to reduce EF count below 50. The scheduled task `infra-health-check` still references it.

## Impact
- Infra health check scheduled task unable to verify DB/table health via `health-check` EF
- Firebase Hosting is healthy — user-facing app unaffected
- No user-visible downtime

## Recommended Action
1. Update `infra-health-check` SKILL.md to use `get-home-dashboard` EF as proxy health check (it queries DB and returns structured data)
2. Set `SUPABASE_SERVICE_KEY` in scheduled task environment so authenticated EFs can be tested
3. Or recreate a minimal `health-check` EF if deep DB health monitoring is needed

## Severity
LOW — app is operational, only monitoring coverage gap
