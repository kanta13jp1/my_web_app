# Incident Report: 2026-05-18 07:43 JST — Supabase project egress quota exceeded (P0)

## Summary

Automated infra health check (`infra-health-check` scheduled task) detected that the entire Supabase project `smmkxxavexumewbfaqpy` is restricted due to `exceed_egress_quota`. All API surfaces (Edge Functions, REST, Realtime, Storage) return HTTP 402 with a quota-violation message. Firebase Hosting serves the SPA shell normally (HTTP 200), but the app cannot fetch data — user-facing experience is a perpetual "Loading application…" state.

This is the **first observed occurrence** of an egress-quota restriction on this project (no prior `exceed_egress` matches in `docs/`).

## Findings

### Supabase project — RESTRICTED (HTTP 402)

- **Endpoint probed**: `GET https://smmkxxavexumewbfaqpy.supabase.co/functions/v1/health-check`
- **HTTP status**: `402 Payment Required`
- **Response body**:
  ```json
  {"message":"Service for this project is restricted due to the following violations: exceed_egress_quota. Please reach out to Supabase support at https://supabase.help immediately."}
  ```
- **Scope**: Project-wide (not just `health-check`). REST probe (`/rest/v1/`) returned `401` for missing apikey rather than data — consistent with full-project restriction enforced at the gateway.
- **Auth attempted**: Anonymous (no `SUPABASE_SERVICE_KEY` in scheduled-task env). Service key would not change the outcome — 402 is enforced before auth on restricted projects.
- **Impact**: All Edge Functions (~50 per EF-CAP-50), all DB reads/writes via PostgREST, Storage, Realtime → blocked. Cron-driven workflows that call back into Supabase will also fail.

### Firebase Hosting — OK

- **URL**: <https://my-web-app-b67f4.web.app/>
- **HTTP status**: `200` in 0.065 s
- **Page**: Title "自分株式会社 | AIが今日の最優先タスクを整理" renders; nav and marketing sections present.
- **User-visible degradation**: Footer shows "Loading application..." indefinitely because the Flutter SPA cannot reach Supabase. New visitors will see the LP but cannot sign in, view dashboards, or trigger any data action.

## Root cause (provisional)

Unknown without Supabase dashboard access. Plausible contributors (verify before acting):

1. **Recent EF burst**: part 218 shipped 47 new issues including multi-provider AI work; part 219/225 cron added blog-draft EFs; recent days have heavy EF traffic.
2. **Loop or runaway cron**: a scheduled task may be polling Storage / large query repeatedly. `[AUTO-REPLY]` and `[CONCURRENCY]` rules guard against this but coverage may have a gap.
3. **Asset / Storage egress**: monthly_asset_reports schema landed 2026-05-16 (`20260516000000_monthly_asset_reports_schema.sql`) — if reports include large attachments, downloads could spike egress.
4. **Free-tier monthly quota rollover**: Supabase free tier ~5 GB/month egress. If the project is on free tier and crossed the threshold, automatic restriction triggers.

## Recommended actions (CEO / human-required — cannot auto-fix from CLI)

1. **Open Supabase dashboard** → Project → Settings → Usage → Egress. Confirm which bucket/endpoint is consuming the quota.
2. **Contact Supabase support** at <https://supabase.help> per the error message if the spike is unexpected, or upgrade plan / wait for monthly reset if expected.
3. **Triage cron / EF traffic**: review recent additions for runaway loops. Check `dispatch_automation_hub` reference memory for known offenders.
4. **Add egress monitor**: extend `scripts/schedule_resilience_watch.py` (or similar) to alert *before* 402 lands — e.g. poll usage API daily and warn at 80%.
5. **Status banner**: while restricted, frontend should detect 402 from Supabase and show a maintenance notice instead of the silent "Loading application..." state. File a follow-up issue.

## Status

- **Detection**: 2026-05-18 07:43 JST (automated)
- **Status**: **OPEN — P0** (production outage; user-facing data layer down)
- **Auto-resolution attempted**: None — outside CLI's reach (billing / Supabase support action required).
- **Next check**: next hourly `infra-health-check` run; will continue to report 402 until restriction is lifted.

## Evidence

```
$ curl -s -o /tmp/health.json -w "HTTP_%{http_code}\n" \
    https://smmkxxavexumewbfaqpy.supabase.co/functions/v1/health-check
HTTP_402

$ cat /tmp/health.json
{"message":"Service for this project is restricted due to the following violations: exceed_egress_quota. Please reach out to Supabase support at https://supabase.help immediately."}

$ curl -s -o /dev/null -w "FB_HTTP=%{http_code} time=%{time_total}s\n" \
    https://my-web-app-b67f4.web.app/
FB_HTTP=200 time=0.065107s
```
