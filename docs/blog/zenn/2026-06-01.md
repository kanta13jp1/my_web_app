---
title: "Flutter Webで負債管理ワークフローを自動化した話 — ビリング確認漏れをゼロにする設計"
emoji: "💳"
type: "tech"
topics: [Flutter, Supabase, AI, ClaudeCode]
published: true
---

個人の資産・負債を一元管理する Flutter Web アプリ「自分株式会社」の開発を続けています。今回は資産管理画面に 2 つの新機能を追加しました。

1. **負債コントロールレビューパネル** — ビリング確認漏れ・支払い元未設定を一覧表示
2. **口座移動タスクへの実行メモ機能** — ATM 手数料や残高を記録

## 負債コントロールレビューパネル

ワークブック画面の上部に、レビューが必要な負債行を集約して表示するセクションを追加しました。

```dart
enum _DebtMasterReviewFilter { all, billingConfirmation, paymentSource }
```

レビュー対象がある場合はアンバー系、すべてクリアなら緑で表示されます。

```dart
final color = hasReviewTargets
    ? const Color(0xFFD97706)  // 要確認
    : const Color(0xFF0D9488); // クリア
```

「推定支払額を一括確定」ボタンも追加し、毎月の概算支払いをワンタップで確定できます。

```dart
void _confirmAllEstimatedPaymentAmounts(
  Iterable<AssetLiabilityDebtRow> rows,
) {
  final targets = rows.where((r) => r.paymentAmountEstimated).toList();
  if (targets.isEmpty) return;
  setState(() {
    for (final row in targets) {
      final amount = row.scheduledPaymentAmount.round();
      _monthlyPaymentOverrides[row.id] = amount.toDouble();
      _monthlyPaymentControllers[row.id]?.text = amount.toString();
    }
  });
  unawaited(_saveAssetLiabilityMonthlyState());
}
```

## 口座移動タスクへの実行メモ機能

```dart
Future<void> _showTransferTaskMemoDialog(
  AssetLiabilityTransferTask task,
) async {
  final controller = TextEditingController(text: task.completionMemo);
  final nextMemo = await showDialog<String>(
    context: context,
    builder: (context) => AlertDialog(
      title: const Text('口座移動タスクの実行メモ'),
      content: TextField(
        controller: controller,
        minLines: 3, maxLines: 6,
        decoration: const InputDecoration(
          hintText: '例: ATM手数料110円。移動後の残高は84,000円。',
        ),
      ),
    ),
  );
  if (nextMemo == null) return;
  setState(() {
    _transferTasks = [
      for (final t in _transferTasks)
        if (t.id == task.id) t.copyWith(completionMemo: nextMemo) else t,
    ]..sort(_compareTransferTasksByDueDate);
  });
  unawaited(_saveAssetLiabilityMonthlyState());
}
```

## 詰まったポイント: 差分検知の更新忘れ

```dart
// 修正: completionMemo も比較対象に追加
left.completedAt?.toIso8601String() != right.completedAt?.toIso8601String() ||
left.completionMemo != right.completionMemo
```

## まとめ

Flutter Web の `StatefulWidget` + Supabase 永続化で「個人の業務フロー自動化」を実現しています。

本アプリは https://my-web-app-b67f4.web.app/ で公開中です。
