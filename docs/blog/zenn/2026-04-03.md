---
title: Flutter WebでブラウザだけのギターDAW+2FA+IoT管理を1日で実装した話 — Supabase RLS×MediaRecorder実践
emoji: 🎸
type: tech
topics: [flutter, supabase, dart, webdev, ai]
published: true
---

# はじめに — 2026-04-03 [Zenn]

「自分株式会社」は、Notion・Evernote・MoneyForward・Slack・GitHub など **21社の競合SaaSを1つに統合** したAI生産性プラットフォームです。Flutter Web + Supabase で構築し、毎日機能を追加しながら公開しています。

今日（2026-04-03）の主な実装：

- **ギター録音スタジオ完成** — クロスブラウザ対応マイク権限ハンドリング・WAV出力・共有リンク
- **2FA（二要素認証）管理ページ** — TOTP設定・信頼済みデバイス・バックアップコード
- **IoTデバイス管理** — 部屋別グリッド・自動化ルール
- **アクセス制御・パスワード金庫・Podcast Manager** など新規ページ6件

---

# クロスブラウザ対応マイク権限ハンドリング

Flutter Webでマイク録音の最大の壁は **ブラウザごとに異なる権限エラー** です。

```dart
try {
  final stream = await _requestMicrophone();
} catch (e) {
  final msg = e.toString().toLowerCase();
  if (msg.contains('notallowederror') || msg.contains('permission denied')) {
    _showPermissionGuide(PermissionErrorType.notAllowed);
  } else if (msg.contains('notfounderror') || msg.contains('devicenotfound')) {
    _showPermissionGuide(PermissionErrorType.notFound);
  } else if (msg.contains('notsupportederror')) {
    _showPermissionGuide(PermissionErrorType.notSupported);
  }
}
```

エラーを `NotAllowed / NotFound / NotSupported` の3種に分類し、**iOS Safari / Android Chrome / デスクトップChrome / Firefox** 向けの解決手順を表示します。

# 共有リンクのクリップボードコピー

```dart
if (_isPublic && _savedRecordingId != null) {
  final shareUrl =
      'https://my-web-app-b67f4.web.app/guitar-studio/$_savedRecordingId';
  await Clipboard.setData(ClipboardData(text: shareUrl));
  _showSnackBar('共有リンクをコピーしました！');
}
```

`flutter/services.dart` の `Clipboard` はFlutter Webでもそのまま動作します。

# Supabase guitar_recordings テーブル + RLS

```sql
CREATE TABLE guitar_recordings (
  id        uuid DEFAULT gen_random_uuid() PRIMARY KEY,
  user_id   uuid REFERENCES auth.users(id) ON DELETE CASCADE NOT NULL,
  title     text NOT NULL,
  is_public boolean DEFAULT false,
  created_at timestamptz DEFAULT now()
);

ALTER TABLE guitar_recordings ENABLE ROW LEVEL SECURITY;

-- 自分のデータは全操作可能
CREATE POLICY "owner_all" ON guitar_recordings
  FOR ALL USING (auth.uid() = user_id);

-- 公開データは全ユーザー参照可
CREATE POLICY "public_select" ON guitar_recordings
  FOR SELECT USING (is_public = true);
```

SNS系機能の王道パターンである **owner_all + public_select の2層RLS** を採用。

# 2FA管理ページ

`supabase_flutter` パッケージの MFA API を使用：

- `enrollMFA()` でTOTP QRコード表示
- `auth.mfa_factors` テーブルで信頼済みデバイス管理
- バックアップコード生成・全セッション強制ログアウト

# IoTデバイス管理 — 部屋別グリッド

```dart
final devicesByRoom = <String, List<IotDevice>>{};
for (final device in _devices) {
  devicesByRoom.putIfAbsent(device.room, () => []).add(device);
}
```

自動化ルールはEdge Function `home-iot-automation` に委譲。

# 詰まったポイント

## MediaRecorder の型エラー

```
The argument type 'String?' can't be assigned to parameter type 'String'.
```

```dart
// 修正前
final mimeType = _getSupportedMimeType();
// 修正後
final mimeType = _getSupportedMimeType() ?? 'audio/webm';
```

## RLSポリシーの順序

必ず `ENABLE ROW LEVEL SECURITY` → `CREATE POLICY` の順番で記述すること。

## `updated_at` トリガーの重複

`CREATE OR REPLACE TRIGGER`（PostgreSQL 14+）または `DROP TRIGGER IF EXISTS` で対処。

# まとめ

1. クロスブラウザのマイク権限エラーは3種類に分類してOS・ブラウザ別の解決策を提示
2. Supabase RLSの2層設計（owner_all + public_select）はSNS系機能の王道
3. Flutter Web の Clipboard API はネイティブと同じ書き方で使える
4. `flutter analyze 0エラー` 維持が毎日の品質ゲート

**サービスURL**: https://my-web-app-b67f4.web.app/

#FlutterWeb #Supabase #buildinpublic
