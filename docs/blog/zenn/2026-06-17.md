---
title: "Flutter Webで定期固定費を「コード不要」で登録・管理する仕組みを作った話"
emoji: "💰"
type: "tech"
topics: ["Flutter", "Supabase", "AI", "ClaudeCode", "個人開発"]
published: true
---

## はじめに

「家賃・光熱費・通信費など毎月かかる固定費を、コードを1行も書かずにUIから登録して、資金繰りの見込み残高に自動反映したい」

自分の場合、Flutter Webで作っている自分株式会社アプリ（https://my-web-app-b67f4.web.app/）の資産管理ページで、家賃/KDDI/水道/ガスの固定費が約8箇所にハードコードされていました。新しい固定費が増えるたびにコードを修正してデプロイする必要があり、明らかにスケールしない設計でした。

今週、この問題を根本から解決するデータ駆動モデルとUIを実装しました。

## 実装方法

### Step 1: データモデル設計 (PR #3452)

まずハードコードされた固定費をデータとして扱えるように、`AssetRecurringFixedCost` モデルを定義しました。

```dart
class AssetRecurringFixedCost {
  final String id;
  final String name;       // 家賃、KDDI、水道 など
  final double amount;     // 月額
  final int transferDay;   // 振替日 (1〜28)
  final String period;     // monthly / bimonthly など
  final String? sourceAccount; // 振替元口座
}
```

ポイントは `appliesToMonth(DateTime month)` メソッドです。毎月の場合は常に `true`、隔月の場合は基準月から計算して `true/false` を返します。

永続化には `AssetRecurringFixedCostStore` を作り、`asset_recurring_fixed_costs_v1` というキーでローカルに保存しつつ、Supabase の `asset_pref_mirror` テーブルに全量 upsert する設計にしました。

### Step 2: 資金繰りへの自動計上

`buildWorkbook(recurringFixedCosts:)` で当月に該当する固定費を自動的に utility 負債として計上します。二重計上防止のロジックが重要です。

```dart
// 二重計上防止
final alreadyExists = liabilities.any(
  (l) => l.name == cost.name || l.id == cost.id,
);
if (!alreadyExists) {
  _addLiability(cost);
}
```

### Step 3: UIウィジェットの実装 (PR #3457)

資産管理ページが肥大化しないよう、UIは独立ウィジェットとして切り出しました。

- **`RecurringFixedCostCard`**: 一覧表示 + 追加/編集/削除ボタン
- **`RecurringFixedCostEditorDialog`**: 名称/月額/振替日/周期/振替元の入力フォーム + バリデーション

起動時のロードロジックはローカル優先・サーバーフォールバック方式を採用：

```dart
final local = await RecurringFixedCostStore.load();
if (local.isEmpty) {
  final mirror = await _fetchMirrorFromSupabase();
  setState(() => _recurringFixedCosts = mirror);
} else {
  setState(() => _recurringFixedCosts = local);
}
```

## 詰まったポイント

### 1. ミラー同期の「ローカル優先 vs サーバー優先」問題

端末Aでデータをローカルに保存し、端末Bで初回起動した際にどちらを優先するか悩みました。「ローカルに登録があれば上書きしない安全側」という方針にしました。

### 2. 寛容な `fromJson` パース

サーバーから返ってくるデータが必ずしも正しい形式とは限りません。以下のケースを黙って破棄する実装にしました：
- 名称が空 / 金額が0以下 / 振替日が1〜28の範囲外

### 3. テスト専用注入

```dart
RecurringFixedCostCard(
  debugInitialRecurringFixedCosts: [
    AssetRecurringFixedCost(name: '家賃', amount: 80000, ...),
  ],
)
```

`debugInitialRecurringFixedCosts` パラメータで、本番コードに影響を与えずにWidgetTestが書けます。

## まとめ

| 機能 | PR | 内容 |
|------|-----|------|
| データ駆動モデル | #3452 | ハードコード廃止・UIから登録可能に |
| UIウィジェット | #3457 | カード + エディタダイアログ + 端末間ミラー同期 |
| キャッシュフロー予測v2 | #3419 | ホライゾン切替 + 安全証拠金ライン |
| 予算実績比較 | #3421 | カテゴリ別予実管理 |
| 更新バナー即時確認 | #3451 | 起動直後 + 前面復帰時にバージョンチェック |

「データ駆動 + ミラー同期」の設計パターンを使うことで、コードを修正せずにビジネスロジックを変更できる柔軟性が生まれます。

実際のアプリは https://my-web-app-b67f4.web.app/ で動作確認できます。

#FlutterWeb #Supabase #buildinpublic
