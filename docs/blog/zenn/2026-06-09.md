---
title: "Supabase Edge Function に多段階承認ワークフローを実装した話 — manager / finance / legal / CEO の4ステップ設計"
emoji: "🔐"
type: "tech"
topics: ["Flutter", "Supabase", "AI", "ClaudeCode", "TypeScript"]
published: true
---

## はじめに

自分株式会社 (Flutter Web + Supabase) の Social Commerce Hub に、**割引・特典付与の多段階承認ワークフロー**を追加しました。

割引や特権付与は損失に直結するため、承認フローの設計は慎重に行う必要があります。今回は Supabase Edge Function (Deno) に純粋関数として実装し、テスト 6/6 パス・後方互換を維持した形で本番投入しました。

本番環境: https://my-web-app-b67f4.web.app/

## 実装の全体像

### 承認ステップの設計

割引承認には最大 4 ステップを設けています:

| ステップ | 担当 | 発動条件 |
|----------|------|----------|
| Step 1 | Manager | 任意の割引申請 |
| Step 2 | Finance | 割引率が閾値超 or 無償提供あり |
| Step 3 | Legal | 特権付与 (privilege grant) を含む場合 |
| Step 4 | CEO | 大口割引 or 複数条件該当 |

### 純粋関数として実装

```typescript
// supabase/functions/_shared/multi_step_approval_workflow.ts

export function buildMultiStepApprovalWorkflow(
  request: DiscountApprovalRequest
): ApprovalWorkflow {
  const steps: ApprovalStep[] = [];

  steps.push({ role: "manager", status: "pending" });

  if (request.discount_rate > FINANCE_THRESHOLD || request.free_offer) {
    steps.push({ role: "finance", status: "pending" });
  }

  if (request.privilege_grants?.length > 0) {
    steps.push({ role: "legal", status: "pending" });
  }

  if (request.discount_rate > CEO_THRESHOLD) {
    steps.push({ role: "ceo", status: "pending" });
  }

  return { steps, current_step_index: 0, status: "held_for_approval" };
}

export function applyApprovalStepDecision(
  workflow: ApprovalWorkflow,
  decision: StepDecision
): ApprovalWorkflow {
  const updated = { ...workflow };
  updated.steps[workflow.current_step_index].status = decision.approved
    ? "approved"
    : "rejected";

  if (decision.approved) {
    updated.current_step_index += 1;
    if (updated.current_step_index >= updated.steps.length) {
      updated.status = "approved";
    }
  } else {
    updated.status = "rejected";
  }

  updated.audit_events = [
    ...(workflow.audit_events ?? []),
    { ...decision, timestamp: new Date().toISOString() },
  ];

  return updated;
}
```

### 後方互換フォールバック

既存の `discount_approval.request` イベントには `approval_workflow` フィールドがないため、null チェックで分岐させています:

```typescript
if (!request.approval_workflow) {
  return handleLegacySingleStepDecision(request, decision);
}
const updatedWorkflow = applyApprovalStepDecision(
  request.approval_workflow,
  decision
);
```

## テスト戦略 (Deno Test)

純粋関数設計のおかげでモック不要で 6 ケースを網羅:

```bash
deno test supabase/functions/_shared/multi_step_approval_workflow_test.ts
# running 6 tests from ...
# ok | 6 passed | 0 failed
```

カバレッジ:
- 基本フロー (manager のみ)
- Finance ステップ追加
- Legal ステップ追加
- CEO ステップ追加
- 途中 reject
- 後方互換 (approval_workflow なし)

## 詰まったポイント

### 純粋関数の設計判断

Edge Function はステートレスなため、ワークフロー状態は DB に持たせる必要があります。関数を副作用なしの純粋関数にすることで:

- **Deno Test でモック不要** → テストが高速かつシンプル
- **再利用性** → 他の Edge Function からも呼び出し可能
- **型安全** → TypeScript の型推論が最大限効く

### GHA concurrency の追加

同一ブランチへの連続 push でキューが詰まる問題も今週解消しました:

```yaml
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
```

## 今週の並行作業: インフラ運用体制の整備

コードだけでなく、運用ドキュメントも整備しました:

- **On-call / incident-response SOP** — SEV1〜3 分類・エスカレーション設計
- **本番監視 Runbook** — 10 本の GHA cron の cadence と異常時 escalation
- **ブランドガイドライン / プレスリリース v1** — MVP ローンチ準備

## まとめ

| 分類 | 内容 |
|------|------|
| feat | 多段階承認ワークフロー (4ステップ / 320L / テスト 6/6) |
| feat | On-call Incident Response SOP v1 |
| docs | 本番監視 Runbook / ブランドガイドライン等 |
| ci | GHA 冗長フロー最適化 |

次は割引承認の Flutter UI と Supabase Realtime によるリアルタイム状態反映に取り組みます。

#FlutterWeb #Supabase #buildinpublic
