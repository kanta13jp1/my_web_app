---
title: "Flutter WebでiOS対応ギターレコーディングスタジオを作った — Web Audio API + WAV + AIコーチ"
emoji: "🎸"
type: "tech"
topics: ["Flutter", "Supabase", "WebAudioAPI", "ClaudeCode", "AI"]
published: true
---

## はじめに

「Notion・Evernote・Slack・Amazon など 21 社の機能を1つに無料で提供する」というビジョンで開発している **自分株式会社** (Flutter Web + Supabase) で、**ブラウザだけで動くギターレコーディングスタジオ** を実装しました。

最大のハードルは **iOS Safari での録音ファイル再生互換性** です。`MediaRecorder` が出力する WebM/Opus は iPhone では再生できません。**Web Audio API + 自前 WAV エンコーダ** で解決した実装を紹介します。

https://my-web-app-b67f4.web.app/

---

## 実装した機能

| 機能 | 技術 |
|---|---|
| ブラウザ録音 | MediaRecorder API |
| iOS 対応 WAV 出力 | Web Audio API + 16bit PCM 変換 |
| メトロノーム | AudioContext + BPM 可変 |
| コード辞典 | フィンガーポジション表示 |
| AI ギターコーチ | Claude API + 練習履歴分析 |
| 練習統計 | Supabase PostgreSQL |

---

## MediaRecorder の mimeType 自動検出

iOS Safari は `audio/webm;codecs=opus` 非対応です。優先順位をつけて自動選択します。

```dart
String _getSupportedMimeType() {
  const types = [
    'audio/webm;codecs=opus',
    'audio/webm',
    'audio/mp4',
    'audio/ogg;codecs=opus',
    '',
  ];
  for (final type in types) {
    if (type.isEmpty) return type;
    // dart:js_interop — JSBoolean → bool 変換が必要
    if (MediaRecorderJS.isTypeSupported(type).toDart) return type;
  }
  return '';
}
```

:::message alert
`MediaRecorder.isTypeSupported` の戻り値は `JSBoolean` です。`.toDart` を忘れると型エラーになります。
:::

---

## WAV エンコーダ (16bit PCM)

録音した Blob を `decodeAudioData` でデコードし、WAV ヘッダーを付与します。

```typescript
// Supabase Edge Function (Deno)
function encodeWAV(samples: Float32Array, sampleRate: number): ArrayBuffer {
  const buffer = new ArrayBuffer(44 + samples.length * 2);
  const view = new DataView(buffer);

  writeString(view, 0, 'RIFF');
  view.setUint32(4, 36 + samples.length * 2, true);
  writeString(view, 8, 'WAVE');
  writeString(view, 12, 'fmt ');
  view.setUint32(16, 16, true);
  view.setUint16(20, 1, true);   // PCM
  view.setUint16(22, 1, true);   // モノラル
  view.setUint32(24, sampleRate, true);
  view.setUint32(28, sampleRate * 2, true);
  view.setUint16(32, 2, true);
  view.setUint16(34, 16, true);  // 16bit
  writeString(view, 36, 'data');
  view.setUint32(40, samples.length * 2, true);

  let offset = 44;
  for (const s of samples) {
    const clamped = Math.max(-1, Math.min(1, s));
    view.setInt16(offset, clamped < 0 ? clamped * 0x8000 : clamped * 0x7FFF, true);
    offset += 2;
  }
  return buffer;
}
```

iOS Safari・Android Chrome・Windows Edge すべてで再生確認済みです。

---

## AI ギターコーチ

練習データを Supabase に蓄積し、Claude API で分析・アドバイスを生成します。

```typescript
// supabase/functions/guitar-studio/index.ts
case 'ai_analyze': {
  const { data: stats } = await supabase
    .from('guitar_practice_stats')
    .select('*')
    .eq('user_id', userId)
    .order('practice_date', { ascending: false })
    .limit(7);

  const analysis = await callClaude(`
    直近7日間の練習データ: ${JSON.stringify(stats)}
    1. 練習パターン分析 (良い点・改善点)
    2. 今日の練習メニュー (3項目)
    3. 来週の目標
    を日本語で提案してください。
  `);
  return new Response(JSON.stringify({ analysis }), { status: 200 });
}
```

---

## 詰まったポイント

### 1. `req.json()` の空ボディクラッシュ (192 Edge Functions 一括修正)

GET リクエストで `await req.json()` を呼ぶとクラッシュします。

```typescript
// OK: try/catch でフォールバック
let body: Record<string, unknown> = {};
try {
  if (req.method !== 'GET') body = await req.json();
} catch { /* 空ボディは許容 */ }
```

### 2. `JSAny?` 型エラー

```dart
// NG: JSAny (non-nullable)
external JSAny someJsFunc();

// OK: JSAny? (nullable)
external JSAny? someJsFunc();
```

### 3. Migration 重複 version

本番 DB に適用済みのマイグレーションを再 apply しようとした際に重複エラー。
`supabase migration repair --status applied <version>` で解消しました。

---

## アーキテクチャ

```
Flutter Web
  └─ /guitar-studio
       ├─ MediaRecorder → Blob
       ├─ AudioContext (decodeAudioData) → WAV
       └─ HTTP → Supabase Edge Function (Deno)
                  ├─ recordings テーブル (CRUD)
                  ├─ guitar_practice_stats (統計)
                  └─ Claude API (AI コーチ)
```

---

## まとめ

Flutter Web + Web Audio API + Supabase で iOS 互換のギター録音スタジオを構築できました。`MediaRecorder` のフォーマット問題は Web Audio API の WAV 変換で回避するのがポイントです。

今後: リアルタイムエフェクト・AI チューナー・マルチトラックミックスを予定しています。

無料で試せます → https://my-web-app-b67f4.web.app/

#FlutterWeb #Supabase #buildinpublic #WebAudioAPI
