# クロスポスト統合版 2026-03-28

## 記事タイトル

Flutter Web × Supabase で「ログイン不要」の絵文字リアクション＋SNS OGP シェアを実装した話

---

## プラットフォーム別設定

### Qiita

- タグ: `Flutter` `Supabase` `FlutterWeb` `個人開発` `OGP`
- 限定公開: false
- LGTMボタン: 有効

### note

- マガジン: 「自分株式会社 開発ログ」
- ハッシュタグ: `#FlutterWeb` `#Supabase` `#buildinpublic` `#個人開発` `#ソーシャル機能`
- 有料設定: 無料

### はてなブログ

- カテゴリ: Flutter / Supabase / 個人開発
- タグ: flutter supabase ogp emoji-reactions build-in-public
- 公開: 即時

### Medium

- Publication: (Personal)
- Tags: Flutter, Supabase, Web Development, IndieHacking, BuildInPublic
- Language: Japanese (with English subtitle)
- English subtitle: "Building Login-Free Emoji Reactions + SNS OGP Share in Flutter Web × Supabase"

### dev.to

```yaml
---
title: Building Login-Free Emoji Reactions + SNS OGP Share in Flutter Web × Supabase
published: true
tags: flutter, supabase, webdev, buildinpublic
cover_image: https://my-web-app-b67f4.web.app/ogp.png
---
```

### Hashnode

- Series: 「自分株式会社 Build in Public」
- Tags: Flutter, Supabase, Web Development, Open Source, Indie Hacking
- Canonical URL: (Zenn 記事 URL を設定推奨)

### Substack

- Newsletter: 「自分株式会社 週次レポート」
- Section: 技術実装メモ
- Free/Paid: Free

### X Article (旧 Twitter)

140字ダイジェスト:

```text
公開メモに絵文字リアクション実装🔥
ログイン不要でIPハッシュ+UNIQUE制約でトグル実現。
Twitter OGP対応もEdge Functionで完成。
Flutter×Supabaseで作った自分株式会社👇
https://my-web-app-b67f4.web.app/
#FlutterWeb #Supabase #buildinpublic
```

### NOTION

- Database: 技術ブログ投稿管理
- Status: Draft → Ready to Publish
- Properties:
  - Title: Flutter Web × Supabase で「ログイン不要」の絵文字リアクション＋SNS OGP シェアを実装した話
  - Date: 2026-03-28
  - Tags: Flutter, Supabase, OGP, Emoji Reactions
  - URL: https://my-web-app-b67f4.web.app/

---

## 本文 (共通)

### はじめに

「自分株式会社」は Notion・Evernote・Slack など21競合の機能を1つに統合したAIライフマネジメントアプリです。Flutter Web + Supabase で開発しており、最近「公開メモ」機能のユーザーが SNS でシェアできる仕組みを追加しました。

シェアするだけでは面白くない。読んだ人がリアクションできる仕組みを、**ログイン不要**で実装しました。さらに同日、ノートコメント機能（CRUD）も実装し、メモ周りのソーシャル体験を一気に強化しました。

今回のポイントは3点です。

- **SNS OGP**: Twitter / LINE のカードに実際のメモ内容を表示する Edge Function
- **絵文字リアクション**: IP の SHA-256 ハッシュで重複防止、ログイン不要で 👍❤️🔥💡🎉 を押せる
- **ノートコメント**: Supabase Edge Function + Flutter UI で CRUD を完結させる

### OGP メタタグ付き HTML を返す Edge Function

公開メモの URL を `https://your-project.supabase.co/functions/v1/public-memo-share?id=xxx` にします。SNS クローラーがアクセスした場合は OGP タグ付き HTML を返し、ブラウザからのアクセスはアプリ URL へリダイレクトします。

```typescript
const isBot = /Twitterbot|facebookexternalhit|Slackbot|TelegramBot|WhatsApp|LINE/i
  .test(req.headers.get('user-agent') ?? '');

if (isBot) {
  return new Response(
    `<!DOCTYPE html><html><head>
      <meta property="og:title" content="${memo.title}" />
      <meta property="og:description" content="${memo.content.slice(0, 120)}" />
      <meta property="og:image" content="${ogpImageUrl}" />
      <meta name="twitter:card" content="summary_large_image" />
    </head><body></body></html>`,
    { headers: { 'Content-Type': 'text/html' } }
  );
} else {
  return Response.redirect(appUrl);
}
```

### memo_reactions テーブルの設計

ログイン不要のリアクションは IP の **SHA-256 ハッシュ**を使い、`UNIQUE (memo_id, emoji, ip_hash)` 制約をトグルとして活用します。

```sql
CREATE TABLE memo_reactions (
  id          uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  memo_id     uuid REFERENCES memos(id) ON DELETE CASCADE,
  emoji       text NOT NULL CHECK (emoji IN ('👍','❤️','🔥','💡','🎉')),
  ip_hash     text NOT NULL,
  created_at  timestamptz DEFAULT now(),
  UNIQUE (memo_id, emoji, ip_hash)
);
```

INSERT で UNIQUE 違反（エラーコード `23505`）が起きたら DELETE するだけです。

### Flutter の _MemoReactionsBar ウィジェット

`AnimatedContainer` + `Wrap` で押した感触のある UI を作りました。

```dart
@override
Widget build(BuildContext context) {
  return Wrap(
    spacing: 8,
    children: emojis.map((e) => AnimatedContainer(
      duration: const Duration(milliseconds: 200),
      decoration: BoxDecoration(
        color: myReactions.contains(e)
            ? Theme.of(context).colorScheme.primaryContainer
            : Colors.grey.shade100,
        borderRadius: BorderRadius.circular(20),
      ),
      child: GestureDetector(
        onTap: () => _toggle(e),
        child: Text('$e ${counts[e] ?? 0}'),
      ),
    )).toList(),
  );
}
```

### ノートコメント機能

RLS ポリシーで「認証ユーザーのみ投稿可・全員閲覧可」を実現しています。

```sql
CREATE POLICY "read_all"   ON note_comments FOR SELECT USING (true);
CREATE POLICY "insert_own" ON note_comments FOR INSERT
  WITH CHECK (auth.uid() = user_id);
CREATE POLICY "delete_own" ON note_comments FOR DELETE
  USING (auth.uid() = user_id);
```

### まとめ

今回の実装で「公開メモ → SNS シェア → リアクション → コメント」という小さなソーシャルループが完成しました。

技術的なポイントをまとめると:

- OGP は **Edge Function でボット判定 → HTML/リダイレクト振り分け**
- リアクションは **UNIQUE 制約 + INSERT 違反をトグルとして活用**
- コメントは **RLS ポリシーで認証ユーザーのみ投稿可・全員閲覧可**
- Flutter UI は **AnimatedContainer + Wrap** でシンプルかつ感触のある実装

URL: <https://my-web-app-b67f4.web.app/>

\#FlutterWeb \#Supabase \#buildinpublic
