# クロスポスト統合版 2026-04-24

**タイトル**: EF-CAP-50達成 — Supabase Edge Functionを85→50本に削減した「Hubパターン」戦略

---

## プラットフォーム別設定

### Qiita
```yaml
title: "Supabase Edge Functionを85→50本に削減した「Hubパターン」実践"
tags: ["Flutter", "Supabase", "Deno", "個人開発", "アーキテクチャ"]
```

### note
```
タイトル: Edge Functionが85本に膨れ上がった話 — Hubパターンで50本に削減した
マガジン: 自分株式会社 開発記録
タグ: Flutter, Supabase, 個人開発, アーキテクチャ
```

### はてなブログ
```yaml
title: "EF-CAP-50達成 — Supabase Edge Functionを85→50本に削減した「Hubパターン」戦略"
category: ["Flutter", "個人開発", "Supabase", "アーキテクチャ"]
```

### Medium
```yaml
title: "Hub Pattern: How I Reduced 85 Supabase Edge Functions Down to 50"
tags: ["supabase", "deno", "flutter", "architecture", "serverless"]
```

### dev.to
```yaml
title: "Hub Pattern: Reducing 85 Supabase Edge Functions to 50 in a Week"
tags: [supabase, deno, flutter, buildinpublic]
canonical_url: ""
```

### Hashnode
```yaml
title: "EF-CAP-50: The Hub Pattern for Supabase Edge Function Management"
tags: ["Supabase", "Deno", "Flutter", "Architecture", "個人開発"]
```

### Substack
```
Subject: 【開発週報】Edge Function 85本を50本に削減 — Hubパターンという答え
```

### X Article
```
タイトル: Supabase EF 85→50本削減した話 — Hubパターンとは
```

### NOTION
```
Page title: 2026-04-24 ブログ: EF-CAP-50達成 Hubパターン
Database: ブログ投稿管理
Status: Draft
```

---

## 本文 (統合版)

個人開発のライフマネジメントアプリ「自分株式会社」（Flutter Web + Supabase）を1人で作り続けている。Notion・Evernote・MoneyForward・Slack・X・Amazonなど21社の競合サービスを1つに統合するという、どう考えても無謀なプロダクトだ。

今週は開発上の大きなマイルストーンを達成した。「**EF-CAP-50**」——Supabase Edge Functionの本数を50本以下に抑えるというアーキテクチャ制約への到達だ。最大85本まで膨れ上がっていたEdge Functionを、35本もの削減に成功した。

### なぜEdge Functionが85本まで増えたのか

機能追加の初期フェーズでは「1機能 = 1 Edge Function」という素朴な実装が続いた。音声処理、動画会議管理、ポッドキャスト、音楽プレイリスト、スクリーンレコーダー……全て独立したEdge Functionとして実装されていた。

問題はSupabaseの無料プランのEF制限と、コールドスタートの頻発だ。使用頻度の低いEFはキャッシュから外れ、毎回数百msのコールドスタートが発生する。85本全てをウォームな状態に保つのはコスト的に不可能だった。

### Hubパターンの設計

解決策として「Hubパターン」を採用した。機能カテゴリごとに「Hub」EFを1本作り、その内部で `action` パラメータによってルーティングする設計だ。

```typescript
// media-hub/index.ts
Deno.serve(async (req) => {
  const { action, ...params } = await req.json();
  
  switch (action) {
    case 'podcast.episodes': return handlePodcastEpisodes(params);
    case 'playlist.list': return handlePlaylistList(params);
    case 'recording.start': return handleRecordingStart(params);
    case 'gallery.upload': return handleGalleryUpload(params);
  }
});
```

今週だけで以下のHubマイグレーションを完了させた:

| 統合前 (個別EF) | 統合後 (Hub) | 削減数 |
|----------------|-------------|-------|
| audio-effects-processor, screen-recorder, podcast-manager, music-playlist-manager, photo-gallery-manager | **media-hub** | 5→1 |
| carbon-tracker, travel-planner, appointment-scheduler | **lifestyle-hub** | 3→1 |
| social-scheduler, pet-care, recipe-manager | **schedule-hub** | 3→1 |
| gift-manager, wallet, loyalty, emergency, donation | **commerce-hub** | 5→1 |
| social-feed, workflow, event, address, notification, inventory, form, sitemap, memo | **core-hub** | 9→1 |

合計: 25本のEFを5本のHubに統合。EF数 **85→50本**を達成。

### notification.broadcast_release の実装

`core-hub` に新アクションを追加。新機能リリース時に全ユーザーへのプッシュ通知を一括送信できる。

```typescript
case 'notification.broadcast_release': {
  const { version, highlights } = params;
  const users = await supabase.from('profiles').select('id, push_token');
  await Promise.all(users.data.map(u => sendPush(u.push_token, {
    title: `v${version} リリース`,
    body: highlights.join(' / ')
  })));
  return new Response(JSON.stringify({ sent: users.data.length }));
}
```

### AI大学が185社に到達

今週追加したAIプロバイダー10社:

- **vLLM** (UC Berkeley/PagedAttention/⭐35k) — LLM推論エンジン
- **Ragas** (RAG評価/4メトリクス) — RAG品質保証
- **AutoGen** (Microsoft Research/⭐40k) — マルチエージェント
- **Agno** (旧phidata/Memory+RAG) — エージェントビルダー
- **Haystack** (deepset/⭐17k) — 本番NLPパイプライン
- **ZenML** (MLOps/@pipeline) — ML実験管理
- **Cursor** ($9.9B/a16z) — AI-firstエディタ
- **Lovable** (vibe-coding) — AIアプリビルダー
- **Bolt.new** (WebContainers) — インブラウザ開発
- **Microsoft Copilot** (Bing AI統合) — MSのAIアシスタント

AI大学は**185社**到達。最終目標200社まであと15社。

### 詰まったポイント

**Deno lintの `no-duplicate-case`**: Hub化で1ファイルに多数のcaseを書くと重複を見落としやすい。`media-hub` でpodcastのcaseが2箇所に存在してCIが失敗。

**マイグレーション番号衝突**: 複数インスタンス並行開発のため同日の番号が衝突する。repair listで追跡・修正。

**Deno lint ≠ flutter analyze**: EFのDenoコードは `deno lint` 必須。WEB版インスタンスはローカルCLI不可なのでVSCode版にcross-instance-prで検証依頼。

### まとめ

Hubパターンのメリットは3つ:
1. **コールドスタート削減** — EF本数減少でウォームな状態が保ちやすい
2. **デプロイ単位の集約** — 関連機能を一度のデプロイで更新できる
3. **凝集度向上** — 同じドメインのコードが一箇所に集まる

「1機能 = 1 EF」という初期設計の負債から学んだのは「共通のライフサイクルを持つ機能は同じ場所に置く」というシンプルな原則。適切なHub境界を引くことがスケーラブルなサーバーレスアーキテクチャの鍵だった。

本番URL: https://my-web-app-b67f4.web.app/

---

## ハッシュタグセット

**日本語系**: #FlutterWeb #Supabase #個人開発 #buildinpublic #AI開発 #ClaudeCode #アーキテクチャ
**英語系**: #FlutterWeb #Supabase #buildinpublic #ClaudeCode #serverless #deno #devlog
**Qiita/Zenn**: Flutter, Supabase, Deno, 個人開発, アーキテクチャ
**dev.to/Hashnode**: supabase, deno, flutter, buildinpublic, serverless
