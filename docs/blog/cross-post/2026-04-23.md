# クロスポスト統合版 2026-04-23

**タイトル**: Claude Quota超過でも開発を止めない — 自分株式会社のMulti-AI Fallback設計

---

## プラットフォーム別設定

### Qiita
```yaml
title: "Claude Quota超過でも開発を止めない — Multi-AI Fallback設計の実践"
tags: ["Flutter", "Supabase", "ClaudeCode", "個人開発", "GitHub Actions"]
```

### note
```
タイトル: AIサービス依存リスクをCircuit Breakerパターンで解消した話
マガジン: 自分株式会社 開発記録
タグ: Flutter, AI, 個人開発
```

### はてなブログ
```yaml
title: "Claude Quota超過でも開発を止めない — 自分株式会社のMulti-AI Fallback設計"
category: ["Flutter", "個人開発", "AI開発"]
```

### Medium
```yaml
title: "Building Resilient AI-Driven Development: Multi-AI Fallback for Flutter Web"
tags: ["flutter", "supabase", "claude", "ai", "devlog"]
```

### dev.to
```yaml
title: "Multi-AI Fallback: Never Let Claude Quota Stop Your Flutter Development"
tags: [flutter, supabase, ai, buildinpublic]
canonical_url: ""
```

### Hashnode
```yaml
title: "Claude Quota超過でも開発を止めない — Multi-AI Fallback設計"
tags: ["Flutter", "AI", "Supabase", "個人開発"]
```

### Substack
```
Subject: 【開発週報】Claude quota超過でも止まらない体制を作った
```

### X Article
```
タイトル: Claude quota超過対策 — Multi-AI Fallbackを作った話
```

### NOTION
```
Page title: 2026-04-23 ブログ: Multi-AI Fallback設計
Database: ブログ投稿管理
Status: Draft
```

---

## 本文 (統合版)

個人開発のライフマネジメントアプリ「自分株式会社」（Flutter Web + Supabase）を1人で作り続けている。

今週のテーマは「**Claude Code quota超過でも開発を止めない体制づくり**」。AI駆動開発を本格化させるにつれ、単一AIへの依存リスクが顕在化してきた。

### なぜMulti-AI Fallbackが必要か

Claude Codeは優秀だが、個人のMax planには月次quota上限がある。実際に今月、上限に達して数時間開発が止まった。Anthropic APIの障害統計を見ると週1回程度のインシデントが記録されており、「AIが落ちたら開発も止まる」という脆弱な体制だった。

**設計目標**:
- Claude Code token消費を月50%削減
- 他AIで代替可能なタスク率70%以上
- Anthropic API停止時も48時間以内に開発継続可能

### タスク別AI振り分けマトリクス

Claude Codeは「CEO/アーキテクト専任」として、判断・統合・memory管理だけを担当。実装タスクはGitHub Copilot / Gemini Code Assist / OpenAI Codexに分散させる。

| タスク | Primary AI | Claude Codeの役割 |
|--------|-----------|----------------|
| 行補完 | GitHub Copilot | 不使用 |
| 500行超リファクタリング | Gemini Code Assist | 事前レビューのみ |
| SQL/アルゴリズム | OpenAI Codex | 仕様確認のみ |
| PR自動レビュー | Claude API (Gemini fallback) | 不使用 |
| アーキテクチャ判断 | **Claude Code 独占** | 主役 |

### Circuit Breakerパターンの実装

GHAスケジュールタスクにCircuit Breakerを実装。Claude APIが不安定な時はGemini 1.5 Flashに自動フォールバック。

```yaml
- name: Claude API circuit breaker check
  id: circuit_check
  run: |
    FAIL_RATE=$(python scripts/check_circuit.py --window 1h)
    if [ "$FAIL_RATE" -gt "50" ]; then
      echo "circuit_open=true" >> $GITHUB_OUTPUT
    fi

- name: Gemini PR review (fallback)
  if: steps.circuit_check.outputs.circuit_open == 'true'
  run: gemini review $PR_NUMBER
```

### 詰まったポイント

**環境変数名ミス**: `GOOGLE_AI_API_KEY` ではなく `GEMINI_API_KEY` が正しい名前。確認に2時間かかった。

**Dart lint エラー**: 他AIのコードに `avoid_dynamic_calls` 違反が混入。他AIのコードは必ず `flutter analyze` を通してからcommitするルールを整備した。

### 今週の成果

- AI大学が **152社**到達（Hume AI・Glean追加）
- 9本のデッドEdge FunctionをHubに統合（EF上限50本対策）
- AppLifecycleObserverでpresence heartbeat制御実装
- `docs/AI_FALLBACK_RUNBOOK.md` 整備

### まとめ

Circuit Breakerパターン＋タスク別AI振り分け＋インスタンス間メッセージパッシングの3本柱で、Claude quota超過・API停止・インスタンス間コンフリクトという3リスクを同時解消できた。

本番URL: https://my-web-app-b67f4.web.app/

---

## ハッシュタグセット

**日本語系**: #FlutterWeb #Supabase #個人開発 #buildinpublic #AI開発 #ClaudeCode
**英語系**: #FlutterWeb #Supabase #buildinpublic #ClaudeCode #MultiAI #circuitbreaker #devlog
**Qiita/Zenn**: Flutter, Supabase, ClaudeCode, 個人開発, GitHubActions
**dev.to/Hashnode**: flutter, supabase, ai, buildinpublic, devlog
