# クロスポスト統合版 2026-06-09

## 共通タイトル

**Supabase Edge Function に多段階承認ワークフローを実装した話 — manager / finance / legal / CEO の4ステップ設計**

## 共通本文

自分株式会社 (Flutter Web + Supabase) の Social Commerce Hub に、**割引・特典付与の多段階承認ワークフロー**を追加しました。

割引や特権付与は損失に直結するため、承認フローの設計は慎重に行う必要があります。今回は Supabase Edge Function (Deno) に純粋関数として実装し、テスト 6/6 パス・後方互換を維持した形で本番投入しました。

**本番環境**: https://my-web-app-b67f4.web.app/

### 承認ステップの設計

| ステップ | 担当 | 発動条件 |
|----------|------|----------|
| Step 1 | Manager | 任意の割引申請 |
| Step 2 | Finance | 割引率が閾値超 or 無償提供あり |
| Step 3 | Legal | 特権付与 (privilege grant) を含む場合 |
| Step 4 | CEO | 大口割引 or 複数条件該当 |

### 純粋関数として実装

```typescript
export function buildMultiStepApprovalWorkflow(
  request: DiscountApprovalRequest
): ApprovalWorkflow {
  const steps: ApprovalStep[] = [];
  steps.push({ role: "manager", status: "pending" });
  if (request.discount_rate > FINANCE_THRESHOLD || request.free_offer) {
    steps.push({ role: "finance", status: "pending" });
  }
  if (request.privilege_grants?.length > 0) {
    steps.push({ role: "legal", status: "pending" });
  }
  if (request.discount_rate > CEO_THRESHOLD) {
    steps.push({ role: "ceo", status: "pending" });
  }
  return { steps, current_step_index: 0, status: "held_for_approval" };
}
```

### 後方互換フォールバック

```typescript
if (!request.approval_workflow) {
  return handleLegacySingleStepDecision(request, decision);
}
const updatedWorkflow = applyApprovalStepDecision(
  request.approval_workflow, decision
);
```

### テスト結果

```bash
deno test supabase/functions/_shared/multi_step_approval_workflow_test.ts
# ok | 6 passed | 0 failed
```

### 今週の並行作業

- On-call / incident-response SOP v1 (SEV1〜3 分類・エスカレーション)
- 本番監視 Runbook (10 本 GHA cron 一元管理)
- ブランドガイドライン / マーケティング / プレスリリース v1
- GHA 冗長フロー最適化 (concurrency 追加 2 件)

---

## プラットフォーム別設定

### Qiita

- タグ: `Flutter` `Supabase` `TypeScript` `個人開発` `AI`
- 絵文字なし、コードブロック重視

### note

- タグ: `FlutterWeb` `Supabase` `個人開発` `buildinpublic` `AI駆動開発`
- 冒頭に「今週の振り返り」として感情的なリード文を追加推奨
- 画像: スクリーンショット or フロー図を追加推奨

### はてなブログ

- カテゴリ: プログラミング / Flutter / 個人開発
- タグ: FlutterWeb Supabase TypeScript 個人開発 AI駆動開発
- はてなブックマークコメント欄向けに「まとめ」を充実させること

### Medium

- Tags: Flutter, Supabase, TypeScript, Software Engineering, Indie Hacking
- Publication: Towards Dev or Level Up Coding への投稿検討
- 英語版タイトル: "Building a Multi-Step Approval Workflow in Supabase Edge Functions"

### dev.to

- Tags: flutter, supabase, typescript, webdev
- シリーズ: "Building in Public — 自分株式会社"
- Cover image 推奨

### Hashnode

- Tags: Flutter, Supabase, TypeScript, IndieHacking
- Publication: Personal blog (jibun-inc.hashnode.dev)
- Newsletter 連携推奨

### Substack

- Section: 技術日記
- 有料読者向け追記: 承認ロジックの詳細 + DB スキーマ設計の背景
- CTA: 本番環境リンク + フォロー促進

### X Article (Twitter/X)

- 文字数: 〜800字以内に圧縮
- ハッシュタグ: #FlutterWeb #Supabase #buildinpublic #個人開発
- スレッド形式でポイントを分割投稿も可

### NOTION

- データベース: Blog Posts
- ステータス: Draft → Review → Published
- プロパティ: Platform (multi-select), Published Date, URL
- 本文はそのまま貼り付け可能 (Markdown 対応)

---

*共通フッター*

本番環境: https://my-web-app-b67f4.web.app/  
#FlutterWeb #Supabase #buildinpublic #個人開発 #AI駆動開発
