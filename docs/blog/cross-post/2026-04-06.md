# Flutter WebでAI駆動の競合モニタリングUIを作った話 — 2026-04-06

> **クロスポスト用統合版** — 各プラットフォームのタグ・フォーマット対応済み

---

## プラットフォーム別タグ設定

| プラットフォーム | タグ・カテゴリ |
|---|---|
| **Qiita** | `Flutter` `Supabase` `AI` `ClaudeCode` `個人開発` |
| **note** | `FlutterWeb` `Supabase` `個人開発` `AI` `buildinpublic` |
| **はてなブログ** | Flutter, Supabase, AI, 個人開発, ClaudeCode |
| **Medium** | `flutter` `supabase` `ai` `claude-code` `build-in-public` |
| **dev.to** | `flutter`, `supabase`, `ai`, `buildinpublic` |
| **Hashnode** | Flutter Web, Supabase, AI Automation, Claude Code |
| **Substack** | AIプロダクト開発, Flutter Web, 個人開発 |
| **X Article** | #FlutterWeb #Supabase #buildinpublic #ClaudeCode |
| **NOTION** | カテゴリ: Tech Blog / ステータス: Draft |

---

## 本文

「自分株式会社」は、Notion・Evernote・MoneyForward・Slack・X など21の競合SaaSの機能を1つに統合することを目指すAIライフマネジメントアプリです。Flutter Web + Supabase で構築し、https://my-web-app-b67f4.web.app/ で公開中です。

今回のセッション(VSCode#6・PS#25・PS#26)では、3つの機能を実装しました:

1. **競合モニタリングカードに「今すぐチェック」ボタン追加** — UIから手動トリガーを可能に
2. **バイラル共有カードの強化** — リファラルリンク共有の使いやすさ改善
3. **cs-check自己修復強化** — Claude ScheduleによるCS対応の完全自動化

---

### 競合モニタリングカード「今すぐチェック」ボタン

Claude Schedule は毎日07:00 JSTに `check-competitor-updates` Edge Function を呼び出し、14社の可用性を自動確認します。今回はUIから手動トリガーできるボタンを追加しました。

```dart
ElevatedButton.icon(
  onPressed: _isChecking ? null : _triggerCompetitorCheck,
  icon: _isChecking
      ? const SizedBox(width: 16, height: 16,
          child: CircularProgressIndicator(strokeWidth: 2))
      : const Icon(Icons.refresh),
  label: Text(_isChecking ? 'チェック中...' : '今すぐチェック'),
),
```

```dart
Future<void> _triggerCompetitorCheck() async {
  setState(() => _isChecking = true);
  try {
    final response = await supabase.functions.invoke(
      'check-competitor-updates',
      body: {'manual': true},
    );
    setState(() => _results = response.data);
  } finally {
    setState(() => _isChecking = false);
  }
}
```

`_isChecking` フラグで二重送信を防止し、ローディング中は `CircularProgressIndicator` を表示します。

---

### バイラル共有カード強化

`share_plus` パッケージを使い、リファラルリンクをネイティブ共有シート経由でSNSへ送信できるようにしました。

```dart
await Share.share(
  '自分株式会社を使ってみて！AIが予定・メモ・家計を全部管理してくれる。\n'
  'https://my-web-app-b67f4.web.app/?ref=${user.id}',
  subject: 'AIライフマネジメントアプリ',
);
if (!mounted) return;
ScaffoldMessenger.of(context).showSnackBar(
  const SnackBar(content: Text('共有しました！')),
);
```

`await` の後に `mounted` チェックを挟むことで `use_build_context_synchronously` 警告を解消しています。

---

### cs-check自己修復強化

Claude Scheduleが `public_gallery` Edge Functionのバグを自動修正した実例:

```typescript
// 修正前: クエリパラメータを先に参照していた
const sortBy = url.searchParams.get('sortBy') ?? 'created_at';

// 修正後: bodyを優先、クエリパラメータをフォールバックに
const body = await req.json().catch(() => ({}));
const sortBy = body.sortBy ?? url.searchParams.get('sortBy') ?? 'created_at';
const limit = Number(body.limit ?? url.searchParams.get('limit') ?? 20);
const offset = Number(body.offset ?? url.searchParams.get('offset') ?? 0);
```

Claude Scheduleがバグを検出→コード修正→`flutter analyze` / `deno lint` 確認→自動コミット→ユーザーへ「修正しました」の返信まで行います。

---

### 詰まったポイント

#### `use_build_context_synchronously` 警告

Flutter 3.x以降では async gap の後に BuildContext を使うと警告になります。`mounted` チェックを必ず入れましょう。

#### 21競合の記述統一

直接競合14社 + 間接競合7社 = 21社として、CLAUDE.md・各画面・Supabaseデータを統一しました。数字の不一致はユーザーの信頼を損ないます。

---

### まとめ

今回の実装で「自分株式会社」は以下が強化されました:

- **競合監視のリアルタイム化**: 手動トリガーが可能に
- **バイラル成長の基盤**: リファラル共有のUX改善
- **完全自動CS対応**: バグ検出→修正→通知までClaudeが自動化

Flutter Web + Supabase + Claude Scheduleの組み合わせで、少人数（実質1人+Claude）での運用を実現しています。

ぜひサービスを試してみてください:  
URL: https://my-web-app-b67f4.web.app/

---

**Qiita用フッター:**
この記事は自動生成ブログ下書きシステム (Claude Schedule) で作成されました。

**note用フッター:**
毎日コツコツ #buildinpublic で開発記録を公開しています。

**Medium用フッター (英語):**
Building in public with Flutter Web + Supabase + Claude Code. Follow along at https://my-web-app-b67f4.web.app/

**dev.to用フッター (英語):**
Check out the live app: https://my-web-app-b67f4.web.app/ | Built with #flutter #supabase #claudecode

#FlutterWeb #Supabase #buildinpublic #ClaudeCode #AI #個人開発
