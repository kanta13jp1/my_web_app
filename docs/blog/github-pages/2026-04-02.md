---
layout: post
title: "Flutter WebでiOS対応ギターレコーディングスタジオを作った — Web Audio API + WAV + AIコーチ"
date: 2026-04-02
categories: [flutter, supabase, web-audio-api, ai]
tags: [FlutterWeb, Supabase, WebAudioAPI, ClaudeCode, buildinpublic]
author: kanta13jp1
---

## はじめに

「Notion・Evernote・Slack・Amazon など 21 社の機能を1つに無料で提供する」ビジョンで開発している **自分株式会社** で、**ブラウザだけで動くギターレコーディングスタジオ** を実装しました。

最大のハードルは **iOS Safari での録音ファイル再生** です。`MediaRecorder` が出力する WebM/Opus は iPhone で再生できません。**Web Audio API + 16bit PCM WAV エンコーダ** で解決しました。

サービス: [https://my-web-app-b67f4.web.app/](https://my-web-app-b67f4.web.app/)

---

## 実装した機能

- **ブラウザ録音**: MediaRecorder API (mimeType 自動検出)
- **iOS 対応 WAV 出力**: Web Audio API + 自前 WAV エンコーダ
- **メトロノーム**: AudioContext + BPM 可変
- **コード辞典**: フィンガーポジション表示
- **AI ギターコーチ**: Claude API + 練習履歴分析
- **練習統計**: Supabase PostgreSQL

---

## MediaRecorder mimeType 自動検出

```dart
String _getSupportedMimeType() {
  const types = ['audio/webm;codecs=opus', 'audio/webm', 'audio/mp4', ''];
  for (final type in types) {
    if (type.isEmpty) return type;
    if (MediaRecorderJS.isTypeSupported(type).toDart) return type;
  }
  return '';
}
```

**注意**: `isTypeSupported` の戻り値は `JSBoolean` なので `.toDart` が必要です。

---

## WAV エンコーダ

```typescript
function encodeWAV(samples: Float32Array, sampleRate: number): ArrayBuffer {
  const buffer = new ArrayBuffer(44 + samples.length * 2);
  const view = new DataView(buffer);
  // RIFF/WAV ヘッダー書き込み (省略)
  let offset = 44;
  for (const s of samples) {
    const clamped = Math.max(-1, Math.min(1, s));
    view.setInt16(offset, clamped < 0 ? clamped * 0x8000 : clamped * 0x7FFF, true);
    offset += 2;
  }
  return buffer;
}
```

iOS Safari・Android Chrome・Windows Edge すべてで再生確認済みです。

---

## AI ギターコーチ (Claude API)

```typescript
const analysis = await callClaude(`
  直近7日間の練習データ: ${JSON.stringify(stats)}
  1. 練習パターン分析
  2. 今日の練習メニュー (3項目)
  3. 来週の目標
`);
```

---

## 詰まったポイント

1. **`req.json()` 空ボディクラッシュ** → try/catch でフォールバック (192 Edge Functions 一括修正)
2. **`JSAny?` 型エラー** → `external JSAny?` に変更
3. **Migration 重複 version** → `supabase migration repair` で解消

---

## まとめ

Flutter Web + Web Audio API + Supabase で iOS 互換のギター録音スタジオを構築できました。今後はリアルタイムエフェクト・AI チューナーを予定しています。

無料で試せます → [https://my-web-app-b67f4.web.app/](https://my-web-app-b67f4.web.app/)

\#FlutterWeb \#Supabase \#buildinpublic \#WebAudioAPI
