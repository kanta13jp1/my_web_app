---
layout: post
title: "Flutter WebでブラウザだけのギターDAW+2FA+IoT管理を1日で実装した話"
date: 2026-04-03 09:00:00 +0900
categories: [flutter, supabase, webdev, buildinpublic]
tags: [flutter, supabase, dart, mediarecorder, rls, 2fa, iot, ai]
---

# Flutter WebでブラウザだけのギターDAW+2FA+IoT管理を1日で実装した話 — 2026-04-03 [GitHub Pages]

「自分株式会社」は、Notion・Evernote・MoneyForward・Slack・GitHub など21社の競合SaaSを1つに統合したAI生産性プラットフォームです。Flutter Web + Supabase で構築し、毎日機能を追加しながら公開しています。

## 今日の実装内容 (2026-04-03)

| 機能 | 競合 | ポイント |
|------|------|----------|
| ギター録音スタジオ | GarageBand, SoundCloud | クロスブラウザ マイク権限ハンドリング |
| 2FA管理 | Authy, Google Authenticator | Supabase MFA API活用 |
| IoTデバイス管理 | Home Assistant, Nature Remo | 部屋別グリッド + 自動化ルール |
| アクセス制御 | Okta, Auth0 | RLS連携 |
| パスワード金庫 | 1Password, Bitwarden | AES暗号化ストレージ |
| Podcast Manager | Spotify, Anchor | RSS + 音声管理 |

## クロスブラウザ対応マイク権限ハンドリング

Flutter Webでマイク録音の最大の壁は**ブラウザごとに異なる権限エラー**です。エラーを3種に分類して、OS・ブラウザ別の解決手順を表示することでUXを大幅改善しました。

```dart
try {
  final stream = await _requestMicrophone();
} catch (e) {
  final msg = e.toString().toLowerCase();
  if (msg.contains('notallowederror') || msg.contains('permission denied')) {
    _showPermissionGuide(PermissionErrorType.notAllowed);
  } else if (msg.contains('notfounderror')) {
    _showPermissionGuide(PermissionErrorType.notFound);
  } else if (msg.contains('notsupportederror')) {
    _showPermissionGuide(PermissionErrorType.notSupported);
  }
}
```

## Supabase RLS 2層設計

```sql
ALTER TABLE guitar_recordings ENABLE ROW LEVEL SECURITY;

CREATE POLICY "owner_all" ON guitar_recordings
  FOR ALL USING (auth.uid() = user_id);

CREATE POLICY "public_select" ON guitar_recordings
  FOR SELECT USING (is_public = true);
```

- **owner_all**: 自分のデータは全操作可能
- **public_select**: 公開データは全ユーザーが参照可能

## 詰まったポイント

- `MediaRecorder.isTypeSupported` の Dart null safety エラー → `?? 'audio/webm'` で解消
- RLSは `ENABLE` → `CREATE POLICY` の順番が重要
- `updated_at` トリガーの重複は `DROP TRIGGER IF EXISTS` で対処

## まとめ

`flutter analyze 0エラー` を維持しながら1日で6機能を追加できました。Supabase Edge Functionへのロジック委譲と、RLS設計の型化が速度の鍵です。

**サービスURL**: [https://my-web-app-b67f4.web.app/](https://my-web-app-b67f4.web.app/)

\#FlutterWeb #Supabase #buildinpublic
