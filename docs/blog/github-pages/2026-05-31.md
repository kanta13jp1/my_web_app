---
layout: post
title: "FlutterWebで資産管理アプリにAI分析履歴機能を実装した話"
date: 2026-05-31 09:00:00 +0900
categories: [flutter, supabase, ai, development]
tags: [FlutterWeb, Supabase, ClaudeCode, AI, buildinpublic]
---

## はじめに

「自分株式会社」は Flutter Web + Supabase で構築した個人用統合管理アプリです。メモ・資産管理・AI大学・競合分析など30以上の機能を一つのアプリに集約し、Claude Code で毎日開発を続けています。

アプリURL: [https://my-web-app-b67f4.web.app/](https://my-web-app-b67f4.web.app/)

今週の主要な実装は **資産管理AI分析履歴の永続化** です。これまで資産管理ページで AI によるポートフォリオ分析を実行できましたが、分析結果はセッション内限りで消えてしまっていました。今回、Supabase PostgreSQL に分析履歴テーブルを作成し、Flutter から履歴を保存・参照できるようにしました。

---

## 実装内容

### 1. Supabaseマイグレーション: AI分析履歴テーブル

```sql
CREATE TABLE asset_management_ai_analysis_history (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  user_id UUID REFERENCES auth.users(id) ON DELETE CASCADE,
  analysis_type TEXT NOT NULL,
  prompt TEXT,
  result_json JSONB,
  model_used TEXT,
  created_at TIMESTAMPTZ DEFAULT now()
);

ALTER TABLE asset_management_ai_analysis_history ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Users can manage own history"
  ON asset_management_ai_analysis_history
  FOR ALL USING (auth.uid() = user_id);
```

JSONB型でAI分析結果を保存することで、後からフィールド単位のクエリが可能になります。

### 2. DartモデルとService

```dart
class AssetManagementAiAnalysisHistoryService {
  final SupabaseClient _client;

  Future<void> saveAnalysis({
    required String analysisType,
    required Map<String, dynamic> resultJson,
    String? modelUsed,
  }) async {
    await _client.from('asset_management_ai_analysis_history').insert({
      'user_id': _client.auth.currentUser!.id,
      'analysis_type': analysisType,
      'result_json': resultJson,
      'model_used': modelUsed ?? 'claude-sonnet-4-6',
    });
  }
}
```

### 3. CI最適化

```yaml
on:
  push:
    paths-ignore:
      - 'docs/**'
      - '**.md'
      - 'memory/**'
```

毎時実行される自動コミット（ヘルスモニター、AI大学更新など）による不要なCI実行をスキップします。

---

## まとめ

- Supabase JSONB + RLS でAI分析履歴を安全・柔軟に永続化
- Flutter の非同期初期化は `WidgetsBinding.instance.addPostFrameCallback` で後処理
- CI `paths-ignore` で自動コミットによるランナー消費を大幅節約

引き続き build in public で開発を続けます。アプリはこちら: [https://my-web-app-b67f4.web.app/](https://my-web-app-b67f4.web.app/)

`#FlutterWeb` `#Supabase` `#buildinpublic` `#ClaudeCode` `#AI`
