---
layout: post
title: "Flutter WebでWBSガントチャートを作り直した — 5インスタンス並行開発が生んだUI大改修"
date: 2026-04-20
categories: [flutter, supabase, buildinpublic]
tags: [Flutter, Supabase, ClaudeCode, AI, 個人開発, WBS, ガントチャート]
---

# Flutter WebでWBSガントチャートを作り直した [GitHub Pages]

個人開発のライフマネジメントアプリ「自分株式会社」（Flutter Web + Supabase）の今週の主な成果。

## 技術スタック

- **フロントエンド**: Flutter Web (Dart)
- **バックエンド**: Supabase (PostgreSQL + Deno Edge Functions)
- **開発体制**: 5インスタンス並行開発（VSCode/Windows/PowerShell/WEB/Mobile）

## 主な変更点

### WBSガントチャートUI

```dart
// 本日線オートスクロール
void _scrollToToday() {
  final dayOffset = DateTime.now().difference(startDate).inDays;
  final offset = dayOffset * _dayWidth - screenWidth / 2;
  _controller.animateTo(offset.clamp(0, maxExtent), ...);
}
```

- 未完了タスクフィルタ（FilterChipトグル）
- 本日線オートスクロール
- 計画vs実績バー表示（planned vs actual）
- インスタンス別フィルターチップ

### EF CRITICAL移行完了

```typescript
// ハブ化パターン（EF総数50本以下維持）
switch (action) {
  case 'chat.list': return handleChatList(body, supabase);
  case 'time.log':  return handleTimeLog(body, supabase);
}
```

chat / time / goal / habit / music の5EFをハブへ移行完了。CRITICAL残ゼロ達成。

### AI大学144社達成

Figure AI（Brett Adcock創業、$39B評価額、Helix VLA）を追加。

詳細: [自分株式会社](https://my-web-app-b67f4.web.app/)

\#FlutterWeb #Supabase #buildinpublic #個人開発 #ClaudeCode
