---
layout: post
title: "Flutter Web で友達招待コードを実装 — ReferralShareCard と Edge Function UI 自動連携の話"
date: 2026-03-29
categories: [flutter, supabase, buildinpublic]
tags: [FlutterWeb, Supabase, 個人開発, グロース, EdgeFunction]
---

## はじめに

「自分株式会社」は Notion・Evernote・Slack など 21 競合の機能を 1 つに統合したAIライフマネジメントアプリです。Flutter Web + Supabase で開発しており、今回は **友達招待システム（ReferralShareCard）** と **Edge Function UI 自動連携** という 2 つの機能を同日に実装しました。

グロース施策として「友達を招待したら特典がもらえる」仕組みは定番です。しかし Flutter Web でそれをきれいに実装しようとすると、紹介コード生成・共有 UI・Supabase との連携など意外と考えることが多い。今回の実装ログをまとめます。

---

## ReferralShareCard の設計

`ReferralShareCard` はホーム画面に配置するウィジェットで、以下の役割を持ちます:

- ログイン中ユーザーの **紹介コード** を表示（Supabase の `profiles` テーブルから取得）
- **コピーボタン** でクリップボードにコピー
- **シェアボタン** で招待 URL を SNS へ共有
- 招待経由の新規登録数を表示

Supabase 側では `profiles` テーブルに `referral_code text UNIQUE` と `referred_by uuid` カラムを追加し、新規サインアップ時に `?ref=` パラメータを読み取って `referred_by` をセットする Edge Function (`growth-acquisition-signal`) を用意しました。

---

## Edge Function UI 自動連携パターン

`lib/data/home_tool_catalog.dart` にツールカタログを定義し、各エントリに `functionName`・`route`・`icon` を持たせます。新しい Edge Function を追加したら、このカタログにエントリを 1 行追加するだけで、ホーム画面のカードと専用ルートが自動で生成されます。

今回追加した 3 つの Edge Function UI:

| Edge Function | ページ | 用途 |
|---|---|---|
| `memo-reactions` | `/memo-reactions` | 公開メモの絵文字リアクション一覧 |
| `note-comments` | `/note-comments` | ノートのコメント CRUD |
| `growth-acquisition-signal` | `/growth-acquisition-signal` | 招待経由の獲得シグナル |

---

## 詰まったポイント

### Flutter Web で `?ref=` パラメータを読む

Flutter Web では `Uri.base.queryParameters['ref']` で URL パラメータを取得できます。Firebase Hosting の SPA rewrite ルールを使っている場合もクエリパラメータはそのまま Flutter に渡されます。

### UUID の型ミスマッチ

Supabase の `id` カラムは `uuid` (String) ですが、Dart 側で `int` として扱っていたため実行時エラーが発生しました。flutter analyze では検出できないため、カラム型と Dart の型を明示的に管理する重要性を再認識しました。

---

## まとめ

今回の実装で「友達招待 → 獲得シグナル記録 → 管理者確認 UI」という招待フローが一本つながりました。Edge Function UI の自動連携パターンも確立できたので、今後新しい Edge Function を追加するコストが大幅に下がります。

サービスはこちら → [https://my-web-app-b67f4.web.app/](https://my-web-app-b67f4.web.app/)

\#FlutterWeb \#Supabase \#buildinpublic
