---
layout: post
title: "Supabase Edge Function に多段階承認ワークフローを実装した話"
date: 2026-06-09 18:00:00 +0900
categories: [flutter, supabase, ai, engineering]
tags: [FlutterWeb, Supabase, ClaudeCode, TypeScript, buildinpublic]
---

## はじめに

自分株式会社 (Flutter Web + Supabase) の Social Commerce Hub に、**割引・特典付与の多段階承認ワークフロー**を追加しました。

本番環境: [https://my-web-app-b67f4.web.app/](https://my-web-app-b67f4.web.app/)

## 承認ステップの設計

割引承認には最大 4 ステップを設けています:

| ステップ | 担当 | 発動条件 |
|----------|------|----------|
| Step 1 | Manager | 任意の割引申請 |
| Step 2 | Finance | 割引率が閾値超 or 無償提供あり |
| Step 3 | Legal | 特権付与を含む場合 |
| Step 4 | CEO | 大口割引 |

## 実装: 純粋関数による設計

```typescript
export function buildMultiStepApprovalWorkflow(
  request: DiscountApprovalRequest
): ApprovalWorkflow {
  const steps: ApprovalStep[] = [];
  steps.push({ role: "manager", status: "pending" });
  if (request.discount_rate > FINANCE_THRESHOLD || request.free_offer) {
    steps.push({ role: "finance", status: "pending" });
  }
  if (request.privilege_grants?.length > 0) {
    steps.push({ role: "legal", status: "pending" });
  }
  if (request.discount_rate > CEO_THRESHOLD) {
    steps.push({ role: "ceo", status: "pending" });
  }
  return { steps, current_step_index: 0, status: "held_for_approval" };
}
```

純粋関数設計により Deno Test で 6/6 パスを達成。後方互換フォールバックも実装済み。

## 今週の並行作業

コードに加え、運用ドキュメントも整備:

- **On-call SOP**: SEV1〜3 分類・エスカレーション設計
- **本番監視 Runbook**: 10 本の GHA cron 管理
- **ブランドガイドライン / プレスリリース v1**

## まとめ

ソロ創業者 + AI フリートで「コード・運用体制・ドキュメント」を並走して整備する週でした。

次は Flutter UI と Supabase Realtime によるリアルタイム承認状態反映に取り組みます。

---

\#FlutterWeb \#Supabase \#buildinpublic \#AI駆動開発
