# CI/CD コスト監査 2026-06-20

自動生成: `ci-cd-audit` スケジュールルーチン (WEB版)

---

## サマリ

| 項目 | 値 |
|------|-----|
| 総ワークフロー数 | 106 本 |
| アクティブ cron ワークフロー | 65 本 |
| 直近 24h サンプル実行数 | 21 本 (GHA API 30件ページ内) |
| 2h 毎トリガー ワークフロー | 10 本 (推定 3,600 runs/月) |
| cancel-in-progress: false の schedule WF | 53 本 |
| 重複名称ワークフロー | 1 組 |
| 本実行で自動適用した変更 | 1 ファイル |

**推定 TOP3 コスト源 (ubuntu-latest 換算):**
1. `claude-session-hygiene-cron.yml` — **windows-latest** 11x/日 × ~10min = **1,100 min/月** (Windows係数×2 = 2,200 min相当)
2. 2時間毎 cron 群 10本 — 合計 3,600 runs/月 × 平均 ~0.4min = 約 **1,440 min/月**
3. `deploy-prod.yml` — push/main トリガー、CI + deploy 合計 ~25min × 推定 30 runs/月 = **750 min/月**

---

## ワークフロー別コスト表 (主要 cron)

| ワークフロー名 | 頻度 | 平均時間 | トリガー | 備考 |
|---------------|------|---------|---------|------|
| claude-session-hygiene-cron | 11x/日 | ~10 min | schedule | **windows-latest** (高コスト) |
| health-monitor | 12x/日 | ~0.5 min | schedule | cancel-in-progress: true ✅ |
| infra-health-check | 12x/日 | ~0.5 min | schedule | cancel-in-progress: false ← **本実行で修正** |
| cs-check | 12x/日 | ~0.4 min | schedule | cancel-in-progress: false |
| ai-university-update | 12x/日 | ~1.0 min | schedule | cancel-in-progress: false |
| notion-sync | 12x/日 | ~3.1 min | schedule | false が意図的 (Notion rate limit) |
| wbs-ai-review | 12x/日 | ~0.2 min | schedule | cancel-in-progress: false |
| horse-racing-update | 12x/日 | ~3.7 min | schedule | cancel-in-progress: false |
| mcp-audit-anomaly-cron | 12x/日 | ~0.2 min | schedule | false が意図的 (セキュリティ操作あり) |
| issue-to-wbs | 12x/日 | ~0.2 min | schedule | cancel-in-progress: false |
| memory-search-sync | 12x/日 | ~0.2 min | schedule | cancel-in-progress: false |
| feature-review | 6x/日 | ~0.9 min | schedule | cancel-in-progress: false |
| wiki-compile-cron | 1x/日 | — | schedule | cancel-in-progress: false |
| daily-report | 1x/日 | — | schedule | cancel-in-progress: false |
| blog-draft | 1x/日 | — | schedule | cancel-in-progress: false |

---

## 検出した冗長フロー (A–G)

### A) 重複トリガー / 同一名称ワークフロー — 優先度: HIGH

**`ai-tool-watch.yml`** と **`ai-tool-changelog-watch.yml`** がどちらも `name: AI Tool Changelog Watch` を使用。
- GitHub Actions UI で区別不能。実行ログが混在してコスト追跡が困難。
- `ai-tool-watch.yml`: 毎日 06:15 JST (デイリーwatch)
- `ai-tool-changelog-watch.yml`: 毎月1日 09:00 JST (月次changelog)
- **提案**: `ai-tool-changelog-watch.yml` の `name:` を `AI Tool Monthly Changelog Watch` に変更。

### B) cancel-in-progress: false が大量設定 — 優先度: MED

53 本の schedule ワークフローで `cancel-in-progress: false`。  
`false` が **意図的** なものは以下のみ:
- `notion-sync.yml`: Notion rate limit (3 req/sec) のため順次実行必須 → **変更禁止**
- `mcp-audit-anomaly-cron.yml`: セキュリティ操作 (client suspend) を含む → **変更禁止**
- deploy 系 3 本: 既存規約で `false` 固定 → **変更禁止**

残り 48 本は理由なく `false`。**本実行では 1 本を修正** (infra-health-check)。  
次回以降の監査で追加修正を推奨: `wbs-ai-review`, `cs-check`, `ai-university-update`, `issue-to-wbs`, `memory-search-sync`。

### C) paths フィルタ未設定の push トリガー — 優先度: LOW

- `windows-companion-build.yml`: `push: tags: windows-companion-v*` のタグ push 専用。tags トリガーは docs 変更の影響を受けないため問題なし (false alarm)。

### D) 過剰な cron 頻度 — 優先度: MED

**2時間毎 (毎月 360 runs) のワークフロー 10 本:**
```
ai-university-update    0 */2 * * *
cs-check                7 */2 * * *
health-monitor          0 */2 * * *   ← cancel: true 済
horse-racing-update     0 */2 * * *
infra-health-check     37 */2 * * *   ← 本実行で cancel 修正
issue-to-wbs           25 */2 * * *
mcp-audit-anomaly-cron  7 */2 * * *
memory-search-sync     12 */2 * * *
notion-sync            10 */2 * * *
wbs-ai-review          20 */2 * * *
```
- `horse-racing-update` (3.7 min/run): 2h → 4h 化で月 **133 min 削減**。
- `notion-sync` (3.1 min/run): Notion rate limit のため 2h 維持が妥当。
- 他の軽量 cron (0.2-1.0 min) は 2h が適切。

**11x/日 Windows ワークフロー:**
- `claude-session-hygiene-cron.yml`: windows-latest で `0 21,23,1,3,5,7,9,11,13,15,17 * * *` (11x/日)。
  コメントに "60分→120分" 削減済みとあり、更なる削減 (4h 毎) は業務インパクト要確認。
  windows-latest は ubuntu-latest の 2 倍コスト。ubuntu-latest への移行が不可能か確認推奨。

### E) cache 未使用 / 最適化機会 — 優先度: LOW

`deploy-dev.yml`: `subosito/flutter-action@v2` に `cache: true` 設定済み ✅  
`deploy-staging.yml`: 同上 ✅  
`deploy-prod.yml`: カスタム `setup-flutter-sdk` action を使用。pub cache は `ci.yml` で `actions/cache@v5` を設定済み ✅

### F) 失敗率が高いワークフロー — 優先度: HIGH

- `minimal-e2e-gate`: サンプル 1 実行で **failure 100%**。直近の実行で問題あり。
  → 手動確認を推奨。

### G) 廃止候補 (90日以上ほぼ活用なし / 目的重複) — 優先度: LOW

| ワークフロー | 理由 |
|-------------|------|
| `cron-batch.yml` | DISABLED コメントあり、schedule 無効化済。`workflow_dispatch` のみ。`SUPABASE_SERVICE_ROLE_KEY` 設定後に復活予定だが数ヶ月放置。 |
| `notebooklm-video-pipeline.yml` | 動画生成パイプライン (非日常的)。実行頻度が低ければ廃止候補。 |

**重複の疑いがある日次 cron ペア:**
- `wbs-user-notify.yml` と `wbs-user-tasks-notify.yml` が同時刻 `0 0 * * *`。内容重複の可能性。
- `self-devin-schedule.yml` と `notebooklm-user-tasks-sync.yml` が同時刻 `30 0 * * *`。

---

## 改善提案 (推定削減効果付き)

| 優先度 | 提案 | 推定削減効果 |
|--------|------|------------|
| HIGH | `minimal-e2e-gate` の失敗原因調査・修正 | 再実行コスト 削減 |
| HIGH | `ai-tool-watch.yml` と `ai-tool-changelog-watch.yml` の `name:` 重複解消 | 管理コスト改善 |
| MED | `horse-racing-update`: `0 */2 * * *` → `0 */4 * * *` | ~133 min/月削減 |
| MED | `claude-session-hygiene-cron`: windows-latest → ubuntu-latest 化の検討 | ~1,100 min/月の Windows 割増分を半減 |
| MED | `cs-check`, `wbs-ai-review`, `issue-to-wbs`, `memory-search-sync`: cancel-in-progress を true に | キュー積み防止 |
| MED | `wbs-user-notify.yml` と `wbs-user-tasks-notify.yml` の統合可否確認 | ~360 runs/月削減 |
| LOW | `cron-batch.yml`: secrets 設定か廃止 (現在は workflow_dispatch のみ) | 管理コスト改善 |
| LOW | `self-devin-schedule.yml` と `notebooklm-user-tasks-sync.yml` のスタガリング (`30 0` → `0 1`) | 同時起動防止 |

---

## 自動適用した変更 (本実行で main へ入れた分)

### infra-health-check.yml: cancel-in-progress: false → true

- **変更内容**: `concurrency.cancel-in-progress` を `false` から `true` に変更
- **理由**: インフラ監視 (HTTP status チェック + JSON ファイル書き込み) のみで、セキュリティ操作・DB mutation なし。idempotent。キャンセルされても次回 run が最新状態を上書き。2h スケジュール × 手動トリガーが重複した際のキュー積み防止。
- **推定効果**: 月 ~30 回の手動 dispatch 重複を防止 (積み上がり時間 ~15 min/月削減)
- **PR**: ci/auto-optimize-20260620 ブランチで CI 確認後 squash merge

---

## 削除候補 (根拠付き・即削除しない)

| ファイル | 根拠 | リスク |
|---------|------|--------|
| `cron-batch.yml` | DISABLED コメント、schedule 無効、数ヶ月 workflow_dispatch のみ | 低 (secrets 設定後に復活する場合は維持) |
| `blog-backfill-from-apis.yml` | "one-shot" という名称。スケジュールなし、workflow_dispatch のみ | 低 |
| `fix-supabase-network.yml` | 単発修正系、再実行頻度要確認 | 中 |
