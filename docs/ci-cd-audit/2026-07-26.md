# CI/CD コスト監査 2026-07-26

<!-- generated-by: ci-cd-cost-optimizer / schedule run -->

## サマリ

| 項目 | 値 |
|-----|---|
| 総ワークフロー数 | 116 本 |
| スケジュール実行 (cron あり) | 77 本 |
| 推定 billable min/月 (sub-daily cron 合計) | 約 6,200 ubuntu-min/月 |
| TOP3 コスト源 | notion-sync / cs-check / health-monitor (各 360 runs/月) |
| 本実行で自動適用した変更 | 2 ファイル (deploy-dev.yml / deploy-staging.yml pub cache 追加) |

## ワークフロー別コスト表 (sub-daily cron / 月間実行回数上位)

| ワークフロー | 頻度 | 推定 runs/月 | cancel-in-progress | トリガー |
|------------|------|------------|-------------------|---------|
| notion-sync.yml | 2h | 360 | false (Notion rate limit 対策) | schedule |
| memory-search-sync.yml | 2h | 360 | true ✓ | schedule |
| mcp-audit-anomaly-cron.yml | 2h | 360 | conditional (PR=true / schedule=false) | schedule+PR |
| health-monitor.yml | 2h | 360 | true ✓ | schedule |
| cs-check.yml | 2h | 360 | true ✓ | schedule |
| x-post-metrics-optimizer.yml | 3h + daily | ~248 | true ✓ | schedule |
| wbs-ai-review.yml | 4h | 180 | true ✓ | schedule |
| schedule-resilience-watch.yml | 4h | 180 | true ✓ | schedule |
| horse-racing-update.yml | 4h | 180 | **false** | schedule |
| feature-review.yml | 4h | 180 | true ✓ | schedule |
| edge-function-audit.yml | 4h | 180 | true ✓ | schedule |
| ai-university-update.yml | 4h | 180 | false (意図的) | schedule |
| issue-to-wbs.yml | 6h | 120 | true ✓ | schedule+issue events |
| tools-hub-mcp-smoke.yml | 12h | 60 | true ✓ (2026-07-24 適用済) | schedule |
| blog-publish-orphan-cleanup.yml | 12h | 60 | true ✓ | schedule |

## 検出した冗長フロー

### [high] E: deploy-dev.yml / deploy-staging.yml — pub キャッシュ未設定 ← **本実行で自動適用**
- `subosito/flutter-action@v2` の `cache: true` は Flutter SDK のキャッシュのみ。
- `~/.pub-cache` (pub パッケージ) は毎回フルダウンロード = `flutter pub get` で 30〜60 秒消費。
- `actions/cache@v6` 追加で cache hit 時は約 3〜5 秒に短縮。
- deploy-dev + deploy-staging 合計で推定 **90〜270 ubuntu-min/月** 削減。

### [high] B: horse-racing-update.yml — cancel-in-progress: false (4h 間隔 / timeout 45min)
- 4時間ごと実行 (180 runs/月)・timeout-minutes: 45。
- 各ステップが時刻条件でガードされており途中キャンセルで不完全書込リスクあり。
- → **Issue 提案のみ** (ホワイトリスト除外)。設計レビュー後に判断。

### [med] D: x-post-metrics-optimizer.yml — 3h 間隔が継続中
- 2026-07-06 監査で 3h→6h 変更後、何らかの理由で 3h に戻っている。
- さらに daily 19:17 UTC cron を追加で持つ二重構成 (8 + 1 runs/日 = ~248 runs/月)。
- 2026-07-24 監査で 3h→4h 提案済み・未適用。3h→4h で -60 runs/月。
- 二重 cron の意図を確認後に整理推奨。

### [med] G: issue-wbs-maintenance.yml — Legacy entry point (廃止候補継続)
- "Legacy entry point" コメントあり / `issue-to-wbs.yml` に統合済。
- 前回監査から未対応。dispatch のみのため即削除可能だが人手確認推奨。

### [low] G: 廃止候補 5 ファイル (前回踏襲)
- `local-election-snapshot-queue.yml` / `x-ai-tool-tracker-queue.yml`
- `x-growth-data-report-post.yml` / `x-household-tracker-post.yml`
- `monthly-asset-report.yml`
- 90 日以上 schedule 実行なし → 削除検討。

## 改善提案

| 提案 | 優先度 | 推定削減効果 | 安全性 |
|-----|--------|-----------|--------|
| `deploy-dev.yml` pub cache 追加 | high | ~45〜135 ubuntu-min/月 | **適用済み (本実行)** |
| `deploy-staging.yml` pub cache 追加 | high | ~45〜135 ubuntu-min/月 | **適用済み (本実行)** |
| `horse-racing-update.yml`: cancel-in-progress → true | high | 最大 45 min × 重複回数 | 要設計確認 |
| `x-post-metrics-optimizer.yml`: 3h→4h + 二重 cron 整理 | med | -60 runs/月以上 | 要意図確認 |
| `issue-wbs-maintenance.yml`: 削除 | low | 軽微 | 要確認 |

## 自動適用した変更 (本実行)

### deploy-dev.yml: pub cache (actions/cache@v6) 追加
- `Setup Flutter` と `Install dependencies` の間に `~/.pub-cache` キャッシュステップを挿入。
- key: `${{ runner.os }}-pub-${{ hashFiles('**/pubspec.lock') }}`
- 推定削減: cache hit 時に `flutter pub get` が 30〜60s → 3〜5s (約 25〜55 秒/run 短縮)。

### deploy-staging.yml: pub cache (actions/cache@v6) 追加
- deploy-dev.yml と同一の追加。staging 環境でも同様の効果。

## 削除候補 (根拠付き・即削除しない)

| ファイル | 根拠 | アクション |
|---------|------|---------|
| `issue-wbs-maintenance.yml` | "Legacy entry point" / 統合済 | 人手確認後に削除 |
| `local-election-snapshot-queue.yml` | 長期 schedule 実行なし (要確認) | 90日未実行確認後に削除 |
| `x-ai-tool-tracker-queue.yml` | 同上 | 同上 |
| `x-growth-data-report-post.yml` | 同上 | 同上 |
| `x-household-tracker-post.yml` | 同上 | 同上 |
| `monthly-asset-report.yml` | 同上 | 同上 |

## 累積削減実績 (過去監査 + 本実行)

| 日付 | 削減内容 | 推定削減 |
|------|---------|---------|
| 2026-06-10 | notion/memory/cs-check 毎時→2h | 1,080 runs/月 |
| 2026-06-15 | health-monitor 毎時→2h + infra-health-check 統合 | 720 runs/月 |
| 2026-06-21 | horse-racing 2h→4h | 180 runs/月 |
| 2026-06-22 | ai-university 2h→4h | 180 runs/月 |
| 2026-07-03 | issue-to-wbs 4h→6h | ~120 runs/月 |
| 2026-07-04 | wbs-ai-review 2h→4h | 180 runs/月 |
| 2026-07-06 | claude-hygiene + x-metrics 適用 | ~240 min/月 |
| 2026-07-17 | infra-health-check → health-monitor 統合 | -360 runs/月 |
| 2026-07-20 | tools-hub-mcp-smoke 6h→12h / blog-orphan 6h→12h | -120 runs/月 |
| 2026-07-24 | tools-hub-mcp-smoke cancel-in-progress: true | ~数 run/月節約 |
| **2026-07-26** | **deploy-dev + deploy-staging pub cache 追加** | **~90〜270 ubuntu-min/月** |

---
*生成: github-actions (scheduled) / 2026-07-26*
