# CI/CD コスト監査 2026-07-16

<!-- generated-by: claude-schedule ci-cd-audit -->

## サマリ

| 項目 | 値 |
|-----|---|
| 総ワークフロー数 | 115 本 |
| schedule トリガー | 約 15 本 (daily/2h/4h/weekly) |
| push トリガー | 5 本 (ci / deploy-dev / deploy-staging / claude-agent-review / minimal-e2e-gate) |
| PR トリガー | 7 本 |
| 推定 schedule 実行数/月 | 約 1,600 runs (2h×360×3 + 4h×180 + daily×15×30 + weekly×4×4 等) |
| TOP3 コスト源 | health-monitor + infra-health-check (各 360 runs/月), mcp-audit-anomaly-cron (360 runs/月) |
| 本実行で自動適用した変更 | 1 本 (schedule-resilience-watch.yml cancel-in-progress: true) |

## ワークフロー別コスト表 (主要 schedule)

| ワークフロー | 頻度 | 平均時間 | 失敗率推定 | トリガー |
|------------|------|---------|-----------|---------|
| health-monitor | 0 \*/2 UTC (2h毎) | ~3 min | 低 | schedule |
| infra-health-check | 37 \*/2 UTC (2h毎) | ~3 min | 低 | schedule |
| mcp-audit-anomaly-cron | 7 \*/2 UTC (2h毎) | ~8 min | 低 | schedule + PR |
| schedule-resilience-watch | 22 \*/4 UTC (4h毎) | ~5 min | 低 | schedule |
| daily-report | 30 22 UTC (daily) | ~15 min | 中 | schedule |
| competitor-monitoring | 0 22 UTC (daily) | ~20 min | 低 | schedule |
| quota-monitor | 0 0 UTC (daily) | ~10 min | 低 | schedule |
| ai-tool-watch | 15 21 UTC (daily) | ~10 min | 低 | schedule |
| wiki-compile-cron | 0 17 UTC (daily) | ~10 min | 低 | schedule |
| session-residuals-sync | 30 17 UTC (daily) | ~5 min | 低 | schedule |
| worktree-cleanup-cron | 0 16 UTC (weekly) | ~10 min | 低 | schedule **windows-latest** |
| knowledge-vault-lint | 0 2 UTC (週1 月) | ~10 min | 低 | schedule |
| dependency-audit | 0 23 UTC (週1 日) | ~15 min | 低 | schedule |
| e2e-smoke | 35 22 UTC (週1 月) | ~10 min | 低 | schedule |

## 検出した冗長フロー (A-G・優先度)

### A) 重複トリガー — インフラ監視 2本が重複チェック (high)

`health-monitor.yml` (`0 */2`) と `infra-health-check.yml` (`37 */2`) が37分間隔で同一インフラを監視している。

| チェック項目 | health-monitor | infra-health-check |
|------------|---------------|-------------------|
| Firebase Hosting | ✅ | ✅ **重複** |
| get-home-dashboard | ✅ | ✅ **重複** |
| guitar-recording-studio | ✅ | ✅ **重複** |
| health-check EF | ✅ | ✅ **重複** |
| support tickets | ✅ | — |
| schedule-hub / tools-hub / admin-hub | — | ✅ |

**推定コスト重複**: 各 360 runs/月 × ~3 min = **2,160 ubuntu-min/月** が実質二重計上。
出力先も異なる (`docs/health-status.json` vs `docs/infra-status.json`) ため統合には設計変更が必要。

### A) 重複トリガー — 競合監視 2本が重複チェック (med)

- `daily-report.yml` (22:30 UTC): 7社固定チェック
- `competitor-monitoring.yml` (22:00 UTC): 50+社動的チェック

前者は日次レポートの一部であり独立 workflow ではないが、30分間隔で重複する competitor 可用性チェックを実施。

### B) cancel-in-progress 未設定 — schedule-resilience-watch (high) → **本実行で自動適用**

`schedule-resilience-watch.yml` (4h毎, 180 runs/月) が `cancel-in-progress: false`。
GitHub API 遅延でスタックした場合、次の4h実行がキューに積まれる。
非破壊的なモニタリング専用ワークフローであるため true に変更。

### C) paths フィルタ — 問題なし

push トリガー全5本に包括的な `paths-ignore` が設定済み (docs/, memory/, .claude/, *.md 等)。

### D) 過剰 cron 頻度 — 0:00 UTC 集中 (med)

以下 6 本が `0 0 * * *` に集中し GHA queue を圧迫する可能性:

| ワークフロー | cron |
|------------|------|
| quota-monitor | `0 0 * * *` |
| codex-backlog-check | `0 0 * * *`? |
| ai-university-reminder | `0 0 * * *`? |
| wbs-user-notify | `0 0 * * *`? |
| wbs-user-tasks-notify | `0 0 * * *`? |
| weekly-sns-draft | 要確認 |

Issue #3656 で既報。

### E) cache 未使用 (low)

- `worktree-cleanup-cron.yml`: `actions/setup-python@v6` に `cache: 'pip'` 未設定。
  週1回なので影響は軽微だが追加可能。

### F) 失敗率 — cron-batch.yml (med)

コメントで `# DISABLED` と明示。`SUPABASE_SERVICE_ROLE_KEY / GEMINI_API_KEY` 未設定で毎回エラー。
schedule 自体は削除済みだが、workflow_dispatch のみ残存 (Issue #3841, #3656 で既報)。

### G) 削除候補 (med)

| ワークフロー | 根拠 |
|------------|------|
| `hotfix-stripe-url.yml` | paths: ['.github/workflows/hotfix-stripe-url.yml'] のみで実質 dead。hotfix 完了後も残存。 |
| `cron-batch.yml` | schedule DISABLED、batch_analysis.py 長期エラー中 |
| `fix-supabase-network.yml` | 名称から一時 hotfix 系、要確認 |

## 改善提案

### 1. インフラ監視 2本の統合 [high・Issue追記]

`health-monitor` と `infra-health-check` を1本に統合し `docs/infra-status.json` に全情報を集約。
**推定削減**: 360 runs/月 × ~3 min = 1,080 ubuntu-min/月 (推定 $1.62/月)。
破壊的変更のため Issue 提案のみ。

### 2. schedule-resilience-watch cancel-in-progress: true [high・本実行自動適用済]

4h周期で GitHub API 遅延によるスタック → queue 積み防止。可逆変更。

### 3. hotfix-stripe-url.yml 削除 [med・Issue追記]

Issue #3841 Proposal #5 と同一。Stripe URL タイポは既に修正済みのため、workflow ファイル自体を削除。
**推定削減**: 0 runs/月 (既に実質 dead) → ファイル削除でノイズ除去。

### 4. 0:00 UTC cron オフセット分散 [med・Issue追記]

`quota-monitor` 等を `5 0 * * *`, `10 0 * * *` 等に 5〜10 分ずつずらし、GHA queue 圧迫を低減。

### 5. worktree-cleanup-cron.yml windows-latest → ubuntu-latest 検討 [low・Issue追記]

windows-latest は billing 2× 相当。Python スクリプト自体は OS 非依存であれば ubuntu で代替可能。
Windows 固有の worktree 挙動テストが目的なら変更不可。要確認。

## 自動適用した変更 (本実行 2026-07-16)

| ファイル | 変更内容 | 推定削減効果 |
|---------|---------|-----------|
| `.github/workflows/schedule-resilience-watch.yml` | `cancel-in-progress: false → true` | キュー積み防止 (4h毎 180 runs/月) |

## 削除候補 (根拠付き・即削除しない)

| ファイル | 根拠 | 推定削減 |
|---------|------|---------|
| `hotfix-stripe-url.yml` | paths 限定で実質 dead。Stripe 修正完了済み | 0 runs/月 (ノイズ除去) |
| `cron-batch.yml` | schedule DISABLED。長期エラー放置 | 0 runs/月 (dispatch のみ) |
| `fix-supabase-network.yml` | 一時 hotfix 系か要確認 | TBD |

## 累積削減実績 (過去監査)

| 日付 | 内容 | 効果 |
|------|----|------|
| 2026-06-10 | notion/memory/cs-check 毎時→2h | ~1,080 runs/月削減 |
| 2026-06-15 | health-monitor 毎時→2h | ~360 runs/月 |
| 2026-06-22 | ai-university 2h→4h | ~180 runs/月 |
| 2026-07-03 | issue-to-wbs 4h→6h | ~690 min/月 |
| 2026-07-04 | wbs-ai-review 2h→4h | ~540 min/月 |
| 2026-07-06 | claude-hygiene + x-metrics 削減 | ~528 ubuntu換算min/月 |
| **2026-07-16** | **schedule-resilience-watch cancel-in-progress fix** | **キュー積み防止** |

---

*自動生成: Claude Schedule ci-cd-audit 2026-07-16*

---

## 追補: ユーザー指示による followup 実施 (2026-07-17)

ユーザー指示により Proposal #1〜#5 を実行に移した。

| Proposal | 対応 | PR / 結果 |
|----------|------|-----------|
| #1 監視統合 | health-monitor.yml へ infra-health-check.yml を統合 (EF 6件監視 + 両 JSON 書き出し + 両 task_id 記録)。参照 3 箇所 (resilience watcher / ISSUE_TEMPLATE / README) も更新 | PR #4072 (**360 runs/月 ≈ 1,080 ubuntu-min/月削減**) |
| #2 hotfix-stripe-url.yml 削除 | typo (b6f7f4) が schedule-hub/index.ts で修正済みであることを確認の上削除 | PR #4071 |
| #3 cron-batch.yml 削除 | schedule 無効化済み + 慢性エラーのため削除 (batch_analysis.py は残置) | PR #4071 |
| #4 00:00 UTC cron 分散 | 8本 (daily 5 + weekly Mon 3) を 3/11/19/27/35/43/51/57 分に分散。09:00 JST 帯セマンティクス維持 | PR #4071 |
| #5 worktree-cleanup runner | **評価の結果 windows-latest を維持**: スクリプトは OS 非依存だが、本番実行環境がローカル Windows セッションであり、windows-latest での週次検証がこの WF の唯一の存在意義。コストは ~50 billed-min/月と僅少 | 変更なし |

### PR #4065 (schedule-resilience-watch) の CI 失敗解消

- 原因1: PR body に Minimal E2E Gate の 3 コミットメント文言が欠落 → gate script の canonical passing block を body に追記
- 原因2: body 中の「deploy」文字列が High-risk gate の text pattern に該当 → High-Risk-Ultrareview-Exception block を追記
- High-risk gate は `edited` イベントで再実行されないため close → reopen で両 gate を再発火
- 再実行後: High-risk gate ✅ / Minimal E2E は GitHub API 503 (一過性障害) で deterministic CI 確認が失敗 → CI 完了後に rerun で解消
