# CI/CD コスト監査 2026-06-05

## サマリ

| 指標 | 値 |
|---|---|
| 総ワークフロー数 | **104** |
| hourly schedule | **12** (内 30分毎: health-monitor) |
| concurrency 未設定 | **15** ワークフロー |
| cache 使用率 | **<1%** (1/104) |
| paths フィルタ設定済み | 21 ワークフロー |

**TOP3 推定コスト源:**
1. `feature-review` — 毎時・Playwright+Chromium インストール (~5-15 min/run × 24/day)
2. `health-monitor` — 毎30分・最高頻度 cron (48 run/day)
3. `ci` — Flutter SDK を毎回 git clone (cache なし、~2-3 min/run のオーバーヘッド)

---

## ワークフロー別コスト表 (schedule 系抜粋)

| ワークフロー | 頻度 | 推定時間 | cancel-in-prog | concurrency | 備考 |
|---|---|---|---|---|---|
| feature-review | 毎時 (24/day) | ~5-15 min | false | ✅ | Playwright heavy |
| health-monitor | **毎30分** (48/day) | ~1-2 min | false | ✅ | 最高頻度 |
| memory-search-sync | 毎時 | ~3-5 min | — | **❌ 未設定** | Gemini API 呼出し |
| wbs-ai-review | 毎時 | ~2-5 min | false | ✅ | hourly |
| notion-sync | 毎時 | ~2-3 min | false | ✅ | rate limit 対策 |
| cs-check | 毎時 | ~1 min | false | ✅ | |
| infra-health-check | 毎時 | ~1 min | false | ✅ | |
| horse-racing-update | 毎時 | ~1-2 min | false | ✅ | |
| mcp-audit-anomaly-cron | 毎時 | ~1 min | false | ✅ | |
| schedule-resilience-watch | 毎時 | ~1 min | false | ✅ | |
| edge-function-audit | 毎時 | ~2 min | false | ✅ | |
| issue-to-wbs | 毎時 | ~2 min | **true** | ✅ | 唯一 true |
| codex-backlog-check | 毎日 | ~3-5 min | — | **❌ 未設定** | |
| kg-indexer-nightly | 毎日 | ~10-20 min | — | **❌ 未設定** | |
| blog-publish-orphan-cleanup | 毎6時間 | ~2-5 min | — | **❌ 未設定** | git push to main |
| ci | PR/push | ~10-15 min | PR時のみ true | ✅ | Flutter SDK clone |

---

## 検出した冗長フロー (A–G)

### [HIGH / E] Flutter SDK・pub-cache キャッシュ未設定
- **現状**: `setup-flutter-sdk/action.yml` が毎回 `git clone --depth 1 flutter/flutter.git` (~2-3分)
  - `$RUNNER_TEMP` は実行間で揮発 → action 内の `if [ ! -x ... ]` チェックは無効
- **pub-cache**: `flutter pub get` も毎回フルダウンロード (~30-60秒)
- **推定削減**:
  - pub-cache: 30-60秒/run × PR~15回/日 ≈ **7-15分/日**
  - Flutter SDK cache: 2-3分/run削減 (actions/cache で対応可)

### [MED / B] memory-search-sync.yml — concurrency 未設定 (毎時実行)
- 昨日の監査 (Issue #3108) で適用予定だったが現在も **未設定**
- 毎時実行が重なると複数インスタンスが Gemini API を同時呼出すリスク
- **推定削減**: 並行実行事故防止 + Gemini API 余分コスト削減

### [MED / B] concurrency 未設定 (schedule 系・積み上がりリスク)
| ワークフロー | 頻度 | リスク |
|---|---|---|
| codex-backlog-check | 毎日 | 低 |
| kg-indexer-nightly | 毎日 | 中 (20min×重複) |
| ai-bench-source-recheck | 不定期+daily | 低 |
| daily-report-freshness-monitor | 毎日 | 低 |
| blog-publish-orphan-cleanup | 毎6時間 | **中** (git push to main) |
| wbs-staleness-audit | 毎日 | 低 |

### [MED / D] hourly 過剰 cron — 実益が薄いもの
| ワークフロー | 現在 | 推奨 | 推定削減 |
|---|---|---|---|
| feature-review | 毎時 | 毎4時間 | **75%** (Playwright heavy) |
| schedule-resilience-watch | 毎時 | 毎4時間 | 75% |
| edge-function-audit | 毎時 | 毎4時間 | 75% |
| wbs-ai-review | 毎時 | 毎2時間 | 50% |
| infra-health-check | 毎時 | 毎2時間 | 50% |
| health-monitor | **毎30分** | 毎時 | **50%** |

### [LOW / F] tools-hub-mcp-smoke.yml — 100% failure (昨日 Issue #3108 参照)
- schedule 毎6時間で 100% failure が継続。billable 時間の純損失。

### [LOW / G] 廃止候補
| ファイル | 根拠 |
|---|---|
| `cron-batch.yml` | ファイル冒頭 `# DISABLED` コメント、schedule 無効 |
| `tools-hub-mcp-smoke.yml` | 100% failure 継続中 |

---

## 改善提案 (削減効果付き)

| 優先度 | 対象 | 内容 | 推定削減効果 |
|---|---|---|---|
| HIGH | `setup-flutter-sdk/action.yml` | actions/cache@v4 で Flutter SDK キャッシュ | **2-3 min/run** |
| HIGH | `ci.yml` | `~/.pub-cache` キャッシュ追加 | 30-60秒/run |
| MED | `memory-search-sync.yml` | concurrency + cancel-in-progress: true | 並行実行事故防止 |
| MED | `feature-review.yml` | cron 毎時 → 毎4時間 | **75%削減** |
| MED | `health-monitor.yml` | cron 毎30分 → 毎時 | 50%削減 |
| MED | `tools-hub-mcp-smoke.yml` | schedule 無効化 or 修正 | 4 run/day削減 |
| LOW | `codex-backlog-check.yml` 他 | concurrency 追加 (cancel: true) | 安全性向上 |
| LOW | `cron-batch.yml` | 削除 (DISABLED済み) | 管理コスト削減 |

---

## 自動適用した変更 (本実行で main へ入れた分)

1. **`memory-search-sync.yml`**: `concurrency` + `cancel-in-progress: true` 追加
   - 毎時実行の並行積み上がり防止、Gemini API 二重呼出し回避
2. **`ci.yml`**: `~/.pub-cache` キャッシュ (`actions/cache@v4`) 追加
   - 推定 30-60秒/run削減 × PR ~15回/日 ≈ **7-15分/日削減**

---

## 削除候補 (即削除しない・Issue 提案のみ)

| ファイル | 根拠 | 推奨アクション |
|---|---|---|
| `cron-batch.yml` | # DISABLED 明記、schedule なし | 人手確認後 削除 |
| `tools-hub-mcp-smoke.yml` | 100% failure継続、billable 損失 | ログ調査 → 修正 or disable |

---

*監査スクリプト: CI/CD コスト最適化エージェント (claude-sonnet-4-6) — 2026-06-05 自動生成*
