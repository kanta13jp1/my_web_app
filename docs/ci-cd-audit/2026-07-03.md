# CI/CD コスト監査 2026-07-03

> 自動生成: Claude Schedule ci-cd-audit 2026-07-03

## サマリ

| 指標 | 値 |
|------|----|
| 総ワークフロー数 | 109 |
| schedule-based | 69 (63%) |
| 毎2h以内の高頻度 cron | 7 本 |
| 毎3h cron | 1 本 |
| 毎4-6h cron | 9 本 |
| concurrency 未設定 | 2 本 (x-post-metrics-optimizer / hotfix-stripe-url) |
| windows-latest 使用 (高コスト) | 5 本 |
| 重複ワークフロー | 2 ペア確認済 |

### TOP3 コスト源 (推定)

1. **claude-session-hygiene-cron.yml** — `windows-latest` × 8x/日 = 240 runs/月 × ~2x コスト倍率
2. **7本の2h cron** — cs-check / health-monitor / infra-health-check / mcp-audit-anomaly / memory-search-sync / notion-sync / wbs-ai-review = 合計84 runs/日 = 2,520 runs/月
3. **issue-to-wbs.yml** — 平均 11.5 分 × 6 runs/日 = 69 分/日

---

## ワークフロー別コスト表 (高頻度のみ)

| ワークフロー | 頻度 | 推定時間 | キャンセル率 | トリガー | concurrency |
|-------------|------|---------|------------|---------|------------|
| claude-session-hygiene-cron | 8x/日 (windows) | ~5min | 0% | schedule | ✓ |
| cs-check | 12x/日 (2h) | ~0.3min | 0% | schedule | ✓ cancel=true |
| health-monitor | 12x/日 (2h) | ~0.4min | 0% | schedule | ✓ cancel=true |
| infra-health-check | 12x/日 (2h) | ~0.4min | 0% | schedule | ✓ cancel=true |
| mcp-audit-anomaly-cron | 12x/日 (2h) | ~3min | 0% | schedule+PR | ✓ cancel=false |
| memory-search-sync | 12x/日 (2h) | ~0.2min | 0% | schedule | ✓ cancel=true |
| notion-sync | 12x/日 (2h) | ~3.2min | 0% | schedule | ✓ cancel=false |
| wbs-ai-review | 12x/日 (2h) | ~0.2min | 0% | schedule | ✓ cancel=true |
| x-post-metrics-optimizer | 8x/日 (3h) | ~0.1min | 0% | schedule | ✗ **→本実行で追加** |
| issue-to-wbs | 6x/日 (4h) + issues | ~11.5min | 33% | schedule+issues | ✓ cancel=true |
| feature-review | 6x/日 (4h) | ~0.9min | 0% | schedule | ✓ cancel=true |
| horse-racing-update | 6x/日 (4h) | ~5.8min | 0% | schedule | ✓ cancel=false |
| release-readiness | 1x/日 | ~10min | 0% | schedule+workflow_run | ✓ cancel=false |

---

## 検出した冗長フロー (A–G 分類)

### A — 重複トリガー・重複機能
**[priority: high]** `wbs-user-notify.yml` + `wbs-user-tasks-notify.yml`  
- 両ファイルとも `cron: "0 0 * * *"` (毎日 09:00 JST)、両方が `tools-hub:wbs.notify_user_tasks` を呼び出し、Slack 通知を送信
- **完全な機能重複** = 毎朝ユーザーに二重通知が届いている
- `wbs-user-tasks-notify.yml` には public anon key がハードコード (セキュリティ懸念)
- **→ Issue 提案: `wbs-user-tasks-notify.yml` を削除**

**[priority: med]** `cs-check.yml` + `health-monitor.yml` + `infra-health-check.yml`  
- 3本がほぼ同一インフラ (Firebase + Supabase EF) をチェック、それぞれ2h毎で合計36 runs/日
- `daily-report.yml` も competitor check と AI大学 reminder を二重実行
- **→ Issue 提案: 統合検討 (慎重に進める)**

### B — concurrency 未設定
**[priority: med]** `x-post-metrics-optimizer.yml`  
- **→ 本実行で自動適用: concurrency 追加 ✅**

**[priority: low]** `hotfix-stripe-url.yml`  
- self-referencing workflow (自ファイル push でのみ起動)、実質 no-op の可能性  
- **→ Issue 提案: ワークフロー自体の廃止候補に追加**

### D — 過剰な cron 頻度
**[priority: high]** `claude-session-hygiene-cron.yml` — 8x/日 × `windows-latest`  
- 既に 30min → 60min → 120min → 180min と 3 段階削減済  
- `scripts/session_hygiene_check.py` は Python スクリプト。Windows 依存を確認後に ubuntu-latest 移行で 50% コスト削減可能  
- **→ Issue 提案: ubuntu 移行調査 or 4x/日へ追加削減**

**[priority: med]** `issue-to-wbs.yml` — "4h safety net" を 6h に削減  
- コメント明記 "4h safety net; issue events handle real-time sync"  
- avg 11.5 min × 2 runs/日削減 = **690 min/月削減**  
- **→ 本実行で自動適用: cron `*/4` → `*/6` ✅**

**[priority: low]** `release-readiness.yml` — 毎日 `flutter build web --release` 実行  
- schedule trigger で毎日フル Flutter Web ビルドが走る (deploy 後の workflow_run とは別に)  
- **→ Issue 提案: schedule trigger から flutter build を条件分岐で除外**

### E — cache 未使用
**[priority: med]** `mcp-audit-anomaly-cron.yml` — Deno module cache なし  
- `denoland/setup-deno@v2` を 2 jobs で使用、`~/.cache/deno` キャッシュなし  
- 12x/日 × 2 jobs × ~30s = 推定 **360 min/月**削減余地

**[priority: low]** `cdp-benchmark-update.yml` — npm cache なし  
- `actions/setup-node@v6` に `cache: npm` 未設定。週次実行のため影響小  

**[priority: low]** `horse-racing-update.yml` — pip cache なし  
- `actions/setup-python@v6` に `cache: "pip"` 未設定。4h毎実行

### G — 廃止候補
| ファイル | 理由 | 優先度 |
|---------|------|--------|
| `cron-batch.yml` | schedule DISABLED / secrets 未設定で毎回 skip | high |
| `hotfix-stripe-url.yml` | self-referencing で実質 no-op の可能性 | med |
| `issue-wbs-maintenance.yml` | `issue-to-wbs.yml` のラッパーのみ、直接 dispatch 可能 | low |
| `wbs-user-tasks-notify.yml` | `wbs-user-notify.yml` と完全重複 | high |

---

## 改善提案 (優先度順)

| # | 対象 | 変更内容 | 推定削減効果 | 状態 |
|---|------|---------|------------|------|
| 1 | `x-post-metrics-optimizer.yml` | concurrency 追加 | キュー積み防止 | **自動適用済 ✅** |
| 2 | `issue-to-wbs.yml` | cron 4h → 6h | ~690 min/月 | **自動適用済 ✅** |
| 3 | `wbs-user-tasks-notify.yml` | ファイル削除 (wbs-user-notify.yml と完全重複) | 30 runs/月 + 二重通知解消 | Issue 提案 |
| 4 | `claude-session-hygiene-cron.yml` | ubuntu-latest 移行 or 4x/日 | ~2,400 min/月削減相当 | Issue 提案 |
| 5 | `mcp-audit-anomaly-cron.yml` | Deno module cache 追加 | ~360 min/月 | Issue 提案 |
| 6 | `cron-batch.yml` | 廃止 | — | Issue #3656 追記済 (前回) |
| 7 | `release-readiness.yml` | schedule 時の flutter build 条件分岐 | ~300 min/月 | Issue 提案 |
| 8 | `cs-check`/`health-monitor`/`infra-health-check` | 統合検討 | ~1,440 min/月 | Issue 提案 (慎重) |

---

## 自動適用した変更 (本実行)

### 1. `.github/workflows/x-post-metrics-optimizer.yml` — concurrency 追加
```yaml
concurrency:
  group: x-post-metrics-optimizer
  cancel-in-progress: true  # 2026-07-03 ci-audit: 3h cron × 8x/日のキュー積み防止
```

### 2. `.github/workflows/issue-to-wbs.yml` — cron 頻度削減
```yaml
# before
- cron: "25 */4 * * *"  # 4h safety net; issue events handle real-time sync
# after
- cron: "25 */6 * * *"  # 6h safety net; issue events handle real-time sync (2026-07-03 ci-audit: 4h→6h, 690min/月削減)
```

**PR**: ci/auto-optimize-20260703-1414 (#3760)

---

## 削除候補詳細

| ファイル | 廃止根拠 | リスク | 状態 |
|---------|---------|--------|------|
| `cron-batch.yml` | schedule DISABLED / batch_analysis.py エラー | 低 | 前回提案 (未対応) |
| `wbs-user-tasks-notify.yml` | `wbs-user-notify.yml` と同一機能、hardcoded key あり | 中 (通知設定確認要) | 今回新規提案 |
| `hotfix-stripe-url.yml` | self-referencing、Stripe URL fix は既に適用済の可能性 | 低 | 今回新規提案 |
| `issue-wbs-maintenance.yml` | `issue-to-wbs.yml` の単純ラッパー | 低 | 今回新規提案 |

---

## windows-latest 使用一覧 (コスト 2x)

| ファイル | 頻度 | 移行難易度 |
|---------|------|-----------|
| `claude-session-hygiene-cron.yml` | 8x/日 | 中 (PowerShell → bash 変換要) |
| `dev-cache-cleanup-cron.yml` | 週次 | 低 (Python スクリプト) |
| `docs-rotate-cron.yml` | 月次 | 低 (Python スクリプト) |
| `worktree-cleanup-cron.yml` | 週次 | 低 (Python スクリプト) |
| `windows-companion-build.yml` | オンデマンド | 不可 (Windows アプリビルド) |

---

## 累積削減効果 (2026-06-04〜2026-07-03)

| 期間 | 主要施策 | 推定削減 |
|------|---------|---------|
| 06-04〜06-25 | cache/concurrency/cron 最適化 11件 | ~1,000+ runs/月 |
| 06-25 | deploy-prod.yml pub cache | ~2-3 min/run |
| **07-03** | x-post concurrency + issue-to-wbs cron 6h | ~690 min/月 + キュー防止 |
| 次回候補 | wbs重複削除 + ubuntu移行 + Deno cache | ~3,000+ min/月 |

---

*CI/CD コスト監査 自動生成 — Claude Schedule 2026-07-03*
