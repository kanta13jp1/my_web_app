# CI/CD コスト監査 2026-07-27

<!-- generated-by: ci-cd-cost-optimizer / schedule run -->

## サマリ

| 項目 | 値 |
|-----|---|
| 総ワークフロー数 | 116 本 |
| スケジュール実行 (cron あり) | 77 本 |
| 推定 billable min/月 (sub-daily cron 合計) | 約 6,100 ubuntu-min/月 |
| TOP3 コスト源 | notion-sync / cs-check / health-monitor (各 360 runs/月) |
| 本実行で自動適用した変更 | 2 ファイル (minimal-e2e-gate.yml Playwright cache 追加 / notebooklm-issue-crosscheck.yml cancel-in-progress: true) |

## ワークフロー別コスト表 (PR 実行 + sub-daily cron 上位)

| ワークフロー | 頻度 | 推定 runs/月 | cancel-in-progress | トリガー |
|------------|------|------------|-------------------|---------|
| notion-sync.yml | 2h | 360 | false | schedule |
| health-monitor.yml | 2h | 360 | true ✓ | schedule |
| cs-check.yml | 2h | 360 | true ✓ | schedule |
| memory-search-sync.yml | 2h | 360 | true ✓ | schedule |
| mcp-audit-anomaly-cron.yml | 2h | 360 | conditional | schedule+PR |
| wbs-ai-review.yml | 4h | 180 | true ✓ | schedule |
| horse-racing-update.yml | 4h | 180 | **false** | schedule |
| ai-university-update.yml | 4h | 180 | false (意図的) | schedule |
| minimal-e2e-gate.yml | PR ごと | PR 数次第 | conditional ✓ | pull_request |
| ci.yml | PR/push ごと | PR 数次第 | conditional ✓ | PR/push |

## 検出した冗長フロー

### [high] E: minimal-e2e-gate.yml / e2e-smoke.yml — Playwright Chromium 毎回フルダウンロード ← **本実行で自動適用 (minimal-e2e-gate.yml)**
- `npx playwright install --with-deps chromium` でブラウザバイナリ (~200MB) を毎実行ダウンロード。
- `minimal-e2e-gate.yml` は PR がアプリコードを含む場合に毎回実行 (`public-e2e-stability` job)。
- `e2e-smoke.yml` は週 1 回 (毎月曜) + workflow_dispatch。
- `actions/cache@v6` で `~/.cache/ms-playwright` をキャッシュすれば cache hit 時に DL をスキップ。
- 推定削減: `minimal-e2e-gate.yml` で cache hit の場合 **約 1〜2 min/run** 短縮。
- `e2e-smoke.yml` も同様に対応可能だが、週 1 回のため効果は軽微。→ Issue 提案のみ。

### [med] B: notebooklm-issue-crosscheck.yml — cancel-in-progress: false (daily cron) ← **本実行で自動適用**
- 日次スケジュール実行 (04:00 JST)、timeout-minutes: 10。
- `cancel-in-progress: false` のため workflow_dispatch + schedule が重なった場合にキュー積みリスク。
- notebooklm-py (ブラウザ自動化) のセッションが長引いた場合に後続 run が待機状態となる。
- Issue 作成には dedup ロジックあり → キャンセル後の次回 run でも問題なく補完される。
- → `cancel-in-progress: true` に変更 (低リスク)。推定削減: まれなキュー積み 0〜1 run/月。

### [high] B: horse-racing-update.yml — cancel-in-progress: false 継続 (前回監査踏襲)
- 4時間ごと実行 (180 runs/月)・timeout-minutes: 45。途中キャンセルで不完全書込リスクあり。
- **Issue 提案のみ** (設計確認後に判断)。前回から未対応。

### [med] D: x-post-metrics-optimizer.yml — 二重 cron 継続 (前回監査踏襲)
- 3h 間隔 + daily 19:17 UTC の二重 cron 構成 (推定 ~248 runs/月)。
- 前回 2026-07-26 監査で指摘済み・未対応。

### [low] A: 競合モニタリング重複
- `daily-report.yml` が 7 社の可用性チェックを実施。
- `competitor-monitoring.yml` が別途 daily で 50社可用性チェック + AI 戦略レポートを生成。
- 目的は異なる (daily-report=運営KPI向け / competitor-monitoring=戦略分析向け) が、
  可用性チェック部分は重複。`daily-report.yml` 側の competitor チェックは簡素化可能。
- 推定削減効果: 軽微 (~1〜2 min/日)。→ Issue 提案のみ。

### [low] G: 廃止候補 (前回踏襲)
- `issue-wbs-maintenance.yml` — "Legacy entry point" / 人手確認推奨。
- `local-election-snapshot-queue.yml` / `x-ai-tool-tracker-queue.yml` など長期未稼働候補。

## 改善提案

| 提案 | 優先度 | 推定削減効果 | 安全性 |
|-----|--------|-----------|--------|
| `minimal-e2e-gate.yml` Playwright cache 追加 | high | ~1〜2 min/run (PR ごと) | **適用済み (本実行)** |
| `notebooklm-issue-crosscheck.yml` cancel-in-progress: true | med | まれなキュー積み防止 | **適用済み (本実行)** |
| `horse-racing-update.yml` cancel-in-progress: true | high | 最大 45 min × 重複回数 | 要設計確認 |
| `x-post-metrics-optimizer.yml` 二重 cron 整理 | med | -60 runs/月以上 | 要意図確認 |
| `e2e-smoke.yml` Playwright cache 追加 | low | ~1〜2 min/月 (週 1 回) | 低リスク |
| `daily-report.yml` 競合チェック簡素化 | low | ~1〜2 min/日 | 要設計確認 |

## 自動適用した変更 (本実行)

### minimal-e2e-gate.yml: Playwright ブラウザキャッシュ追加 (actions/cache@v6)
- `public-e2e-stability` job の `Setup Node` と `Install Chromium` の間に cache ステップを挿入。
- key: `${{ runner.os }}-playwright-chromium-${{ hashFiles('package-lock.json') }}`
- restore-keys: `${{ runner.os }}-playwright-chromium-`
- docs_check が true の場合はキャッシュも不要なので `if` 条件でスキップ。
- cache hit 時: `npx playwright install --with-deps chromium` は DL をスキップしてシステム依存パッケージのみインストール (数秒)。
- 推定削減: cache hit の場合 約 1〜2 min/run。月 10 PR で 10〜20 min 削減。

### notebooklm-issue-crosscheck.yml: cancel-in-progress: false → true
- `concurrency.cancel-in-progress` を `false` から `true` に変更。
- 理由: 日次 cron で workflow_dispatch と重なった場合のキュー積み防止。
- dedup ロジックにより次回 run で補完されるため安全。

## 削除候補 (根拠付き・即削除しない)

| ファイル | 根拠 | アクション |
|---------|------|---------|
| `issue-wbs-maintenance.yml` | "Legacy entry point" / `issue-to-wbs.yml` 統合済 | 人手確認後に削除 |
| `local-election-snapshot-queue.yml` | 長期 schedule 実行なし (要確認) | 90日未実行確認後に削除 |
| `x-ai-tool-tracker-queue.yml` | 同上 | 同上 |
| `x-growth-data-report-post.yml` | 同上 | 同上 |
| `x-household-tracker-post.yml` | 同上 | 同上 |
| `monthly-asset-report.yml` | 同上 | 同上 |

## 累積削減実績 (過去監査 + 本実行)

| 日付 | 削減内容 | 推定削減 |
|------|---------|---------|
| 2026-06-10 | notion/memory/cs-check 毎時→2h | 1,080 runs/月 |
| 2026-06-15 | health-monitor 毎時→2h + infra-health-check 統合 | 720 runs/月 |
| 2026-06-21 | horse-racing 2h→4h | 180 runs/月 |
| 2026-06-22 | ai-university 2h→4h | 180 runs/月 |
| 2026-07-03 | issue-to-wbs 4h→6h | ~120 runs/月 |
| 2026-07-04 | wbs-ai-review 2h→4h | 180 runs/月 |
| 2026-07-06 | claude-hygiene + x-metrics 適用 | ~240 min/月 |
| 2026-07-17 | infra-health-check → health-monitor 統合 | -360 runs/月 |
| 2026-07-20 | tools-hub-mcp-smoke 6h→12h / blog-orphan 6h→12h | -120 runs/月 |
| 2026-07-24 | tools-hub-mcp-smoke cancel-in-progress: true | ~数 run/月節約 |
| 2026-07-26 | deploy-dev + deploy-staging pub cache 追加 | ~90〜270 ubuntu-min/月 |
| **2026-07-27** | **minimal-e2e-gate Playwright cache + notebooklm cancel-in-progress** | **~10〜20 ubuntu-min/月** |

---
*生成: github-actions (scheduled) / 2026-07-27*
