# CI/CD コスト監査 2026-07-30

自動生成: `claude-schedule ci-cd-audit` / 117 ワークフロー対象

---

## サマリ

| 項目 | 値 |
|------|----|
| 総ワークフロー数 | 117 |
| うち schedule 持ち | 77 |
| schedule で cancel-in-progress: false | 53 |
| Deno ジョブでキャッシュなし (高頻度) | 1 (mcp-audit-anomaly-cron) |
| 本実行で自動適用 | 2 ファイル |
| 推定削減効果 | ~360–720 Ubuntu-min/月 |

---

## ワークフロー別コスト概要 (最近の runs より推定)

| ワークフロー名 | 頻度 | 平均時間 | 失敗率 | トリガー |
|--------------|------|---------|--------|---------|
| Deploy to Production | push (main) | ~20.5 min | 0% | push |
| Minimal E2E Gate | PR | ~9.9 min | 0% | pull_request |
| CI | PR | ~9.8 min | 0% | pull_request |
| GA Launch Readiness Gate | workflow_run | ~6.0 min | 0% | workflow_run |
| Release Readiness Gate | workflow_run | ~4.7 min | 0% | workflow_run |
| mcp-audit-anomaly-cron | 2h cron | ~8 min | 0% | schedule |
| horse-racing-update | 4h cron | ~5 min | 0% | schedule |
| health-monitor | 2h cron | ~2 min | 0% | schedule |
| tools-hub MCP Smoke | 12h cron | <1 min | 0%* | schedule |

\* 過去 runs で push:3 / 100% 失敗が確認されたが、yml に push トリガーなし → GitHub API 集計の misclassification の可能性あり。実害なし。

---

## 検出した冗長フロー (A–G)

### E) [high] cache 未使用 — mcp-audit-anomaly-cron.yml
- `script-test` / `detect` 2 ジョブとも `denoland/setup-deno@v2` を使うが Deno モジュールキャッシュなし
- 2h cron = 12 runs/日 = 360 runs/月 × 2 ジョブ = 720 回のフルダウンロード
- **推定削減: ~30–60 sec/ジョブ × 720 = 360–720 Ubuntu-min/月**
- **→ 本実行で自動適用 (actions/cache@v6 追加)**

### B) [med] cancel-in-progress: false — daily-anomaly-scan.yml
- 毎日 18:00 UTC に実行。手動 dispatch と schedule 重複時にキュー積みリスク
- ただし `ANOMALY_SCAN_CRON_ENABLED` フラグが false の間は早期 exit (≈10 sec) — 実害小
- scan は冪等 (Supabase POST / idempotent) → cancel 安全
- **→ 本実行で自動適用 (cancel-in-progress: true)**

### B) [med] cancel-in-progress: false — 53本の schedule ワークフロー (自動変更対象外)
特に以下は手動でのレビューを推奨:
- `youtube-analysis.yml` (毎日 02:00 UTC, 純粋分析, false のまま)
- `daily-report.yml` (毎日 22:30 UTC, false のまま)
- `landing-experiment-report.yml` (毎日 00:15 UTC, false のまま)
- `notebooklm-requirements-to-issues.yml` (毎日 22:15 UTC, Issue 作成あり, false のまま)

### D) [low] 高頻度 cron が残存
- `notion-sync.yml` (10 */2 * * *): 2h ごと。コメントに Notion rate limit 理由あり → 変更しない
- `mcp-audit-anomaly-cron.yml` (7 */2 * * *): 2h ごと。セキュリティ監査のため維持が妥当
- `health-monitor.yml` (0 */2 * * *): 2h ごと。cancel-in-progress: true 済 ✓
- `cs-check.yml` (7 */2 * * *): 2h ごと。cancel-in-progress: true 済 ✓

### G) [low] 廃止候補 (即削除せず Issue 確認推奨)
- `x-first-user-launch-candidate.yml`: actions/checkout@v6 / upload-artifact@v6 と古いバージョンを使用。目的が過去の一時施策の可能性。
- `fix-supabase-network.yml`: "fix-" prefix は hotfix 系の一時ファイル示唆。恒久運用か確認推奨。

---

## 改善提案

| 優先度 | 対象 | 提案 | 推定削減効果 |
|--------|------|------|-------------|
| high | mcp-audit-anomaly-cron.yml | Deno modules cache 追加 | ~360–720 min/月 |
| med | 53本の schedule WF | cancel-in-progress: true 追加 (各自判断) | キュー積み防止 |
| med | youtube-analysis / daily-report 等 | cancel-in-progress: true | 重複実行防止 |
| low | x-first-user-launch-candidate.yml | actions/v6→v7 へ更新 or 削除確認 | バージョン統一 |
| low | fix-supabase-network.yml | 恒久 WF か確認 / 不要なら削除 | 管理コスト削減 |

---

## 自動適用した変更 (本実行)

### 1. mcp-audit-anomaly-cron.yml — Deno modules cache 追加
```yaml
# script-test / detect 各ジョブに追加:
- name: Cache Deno modules
  uses: actions/cache@v6
  with:
    path: ~/.cache/deno
    key: deno-${{ hashFiles('deno.lock') }}
    restore-keys: deno-
```
推定削減: **360–720 Ubuntu-min/月**

### 2. daily-anomaly-scan.yml — cancel-in-progress: true
```yaml
concurrency:
  group: daily-anomaly-scan
  cancel-in-progress: true  # 2026-07-30 ci-audit
```
推定削減: 重複実行時の最大 10 min × 数回/月のキュー積み解消

---

## 削除候補 (即削除しない / 根拠付き)

| ファイル | 根拠 | 確認事項 |
|---------|------|---------|
| `x-first-user-launch-candidate.yml` | v6 アクション使用 / "launch candidate" は一時的目的示唆 | 恒久的に必要かオーナー確認 |
| `fix-supabase-network.yml` | "fix-" prefix は hotfix 系 | 恒久運用なら名前変更、不要なら削除 |

---

## 累積削減実績

| 日付 | 内容 | 効果 |
|------|------|------|
| 2026-06-04〜24 | キャッシュ/concurrency/cron 11 件 | ~1000+ runs/月削減 |
| 2026-06-25 | deploy-prod.yml pub cache | ~2-3 min/run |
| 2026-07-06 | claude-hygiene + x-metrics 削減 | ~528 ubuntu-min/月 |
| **2026-07-30** | **mcp-audit Deno cache + daily-anomaly cancel** | **~360–720 min/月** |

---

*自動生成: Claude Schedule ci-cd-audit 2026-07-30*
