# CI/CD コスト監査 2026-07-08

> 自動生成: Claude Schedule ci-cd-audit / 実行時刻 2026-07-08 14:xx UTC

---

## サマリ

| 指標 | 値 |
|------|----|
| 総ワークフロー数 | 111 本 |
| schedule (cron) ワークフロー | 71 本 |
| 推定 schedule 実行回数 | ~116 runs/日 / ~3,480 runs/月 |
| push/PR トリガー | 17 本 |
| cancel-in-progress 未設定 (non-deploy) | 5 本 |
| 高頻度 cron (≦4h 間隔) | 13 本 |
| 高失敗率ワークフロー (≥33%) | 4 本 (過去 30 runs より) |
| **本実行で自動適用** | **2 ファイル** |

---

## ワークフロー別コスト表 (高頻度 TOP13)

| ワークフロー | cron | runs/日 | runs/月 | cancel-in-progress | 備考 |
|------------|------|---------|---------|-------------------|------|
| cs-check | `7 */2 * * *` | 12 | 360 | true | Firebase+Supabase チェック含む |
| health-monitor | `0 */2 * * *` | 12 | 360 | true | docs/health-status.json 書き出し |
| infra-health-check | `37 */2 * * *` | 12 | 360 | true | docs/infra-status.json 書き出し |
| mcp-audit-anomaly-cron | `7 */2 * * *` | 12 | 360 | **false** ⚠️ |  |
| memory-search-sync | `12 */2 * * *` | 12 | 360 | true |  |
| notion-sync | `10 */2 * * *` | 12 | 360 | **false** (rate limit) | Notion API 逐次制約 |
| x-post-metrics-optimizer | `17 */3 * * *` | 8 | 240 | true | 2026-07-06 に削減済 |
| ai-university-update | `0 */4 * * *` | 6 | 180 | true |  |
| edge-function-audit | `47 */4 * * *` | 6 | 180 | true |  |
| feature-review | `0 */4 * * *` | 6 | 180 | true |  |
| horse-racing-update | `0 */4 * * *` | 6 | 180 | false (逐次) |  |
| schedule-resilience-watch | `22 */4 * * *` | 6 | 180 | true |  |
| wbs-ai-review | `20 */4 * * *` | 6 | 180 | true | 2026-07-04 に 2h→4h 済 |

---

## 検出した冗長フロー (A–G)

### A) 機能重複トリガー [med]
`health-monitor.yml` / `infra-health-check.yml` / `cs-check.yml` (Step 5) が
それぞれ毎 2h に Firebase + Supabase EF を並行チェック。
- health-monitor → `docs/health-status.json` (4 EF)
- infra-health-check → `docs/infra-status.json` (6 EF)
- cs-check Step 5 → 独自チェック
推定重複: ~1,080 curl 呼び出し/月が重複実行。

### B) cancel-in-progress: false で積み上がり [high → 自動適用]
| ワークフロー | トリガー | リスク |
|------------|---------|--------|
| `roadmap-append-only-guard.yml` | push+PR (paths フィルタ済) | PR 積み上がり |
| `mcp-container-isolation-smoke.yml` | PR+workflow_dispatch (paths フィルタ済) | 手動再実行競合 |

**→ 本実行で cancel-in-progress: true に自動変更 (詳細: §自動適用)**

また以下は人手判断:
| ワークフロー | 理由 |
|------------|------|
| `mcp-audit-anomaly-cron.yml` | schedule+PR 混在、前回 #3841 でも指摘 |
| `ga-readiness-gate.yml` | schedule+workflow_run 混在 (cancel すると cascade 問題可能性) |
| `wbs-role-harness.yml` | workflow_dispatch のみ実質 → cancel 不要だが false を明示設定推奨 |

### C) paths フィルタなし [low — 誤検知なし]
`windows-companion-build.yml` / `mobile-release-build.yml` は push on tags のみ → 問題なし。

### D) 過剰 cron 頻度 [low — 過去監査で対応済]
最短 2h 間隔が底。さらなる削減は A) の統合議論に含む。

### E) cache 未使用 [low]
メイン `ci.yml` は `actions/cache@v6` で `~/.pub-cache` をキャッシュ済 ✅。
`deploy-prod.yml` は 2026-06-25 に適用済 ✅。
全主要ビルドワークフローでキャッシュ設定を確認 — 新規不足なし。

### F) 高失敗率 [med]
| ワークフロー | 失敗率 | サンプル数 | コメント |
|------------|--------|----------|---------|
| Blog News Production Smoke | 100% | 1 | workflow_run トリガー |
| Minimal E2E Gate | 50% | 2 | PR トリガー |
| AI Agent PR Review | 50% | 2 | PR トリガー |
| GA Launch Readiness Gate | 33% | 3 | schedule+workflow_run |
> ※ サンプル数が少ないため傾向の参考値。継続監視推奨。

### G) 廃止候補 [med — 人手判断のみ]
前回 #3841 からの引き続き:
- `cron-batch.yml` — schedule DISABLED、secrets 未設定で常時エラー
- `issue-wbs-maintenance.yml` — "Legacy entry point" コメントあり
- `hotfix-stripe-url.yml` / `fix-supabase-network.yml` — 一時 hotfix 系

---

## 自動適用した変更 (本実行)

### 変更 1: `roadmap-append-only-guard.yml`
```diff
 concurrency:
   group: roadmap-append-only-guard-${{ github.ref }}
-  cancel-in-progress: false
+  cancel-in-progress: true
```
- **根拠**: push+PR トリガーだが paths フィルタ済で軽量スクリプトのみ実行。false は保守的デフォルトで意図的でない可能性が高い。
- **推定効果**: 同一 PR への連続 push で旧ラン自動キャンセル → 浪費 ubuntu-min を削減。

### 変更 2: `mcp-container-isolation-smoke.yml`
```diff
 concurrency:
   group: mcp-container-isolation-smoke-${{ github.ref }}
-  cancel-in-progress: false
+  cancel-in-progress: true
```
- **根拠**: PR + workflow_dispatch のみ。MCP 設定検証スクリプトを実行するだけで外部状態変更なし。
- **推定効果**: workflow_dispatch の二重実行や PR 連続 push でのキュー積み防止。

---

## 改善提案 (人手判断)

### 🔴 HIGH: `mcp-audit-anomaly-cron.yml` — cancel-in-progress: true 追加
前回 #3841 から未着手。schedule+PR 混在だが PR トリガーのみ cancel で十分:
```yaml
concurrency:
  group: mcp-audit-anomaly
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}
```

### 🟡 MED: インフラヘルスチェック統合 (A)
`health-monitor` + `infra-health-check` + `cs-check Step 5` の curl チェックを
共通 JSON (例: `docs/infra-unified-status.json`) に一本化し、2 本のクロン→1本に統合。
推定削減: ~360 runs/月 + checkout オーバーヘッド ~12 min/日。
**破壊的変更のため Issue 提案のみ。**

### 🟡 MED: 廃止ワークフロー削除 (G)
`cron-batch.yml` / `issue-wbs-maintenance.yml` / `hotfix-stripe-url.yml` / `fix-supabase-network.yml`
を削除すれば yml 管理コスト削減。削除前に残存ユースケースの確認必須。

### 🟢 LOW: `Blog News Production Smoke` 100% 失敗の原因調査
`workflow_run` で blog-publish 完了後にトリガーされているが全失敗中。
実質的に意味をなしていない可能性 → ログ確認+修正 or 無効化。

---

## 累積削減実績 (過去監査 + 本日)

| 日付 | 内容 | 削減効果 |
|------|------|---------|
| 2026-06-10 | notion/memory/cs-check 毎時→2h | ~1,080 runs/月 |
| 2026-06-15 | health-monitor 毎時→2h | ~360 runs/月 |
| 2026-06-22 | ai-university 2h→4h | ~180 runs/月 |
| 2026-07-03 | issue-to-wbs 4h→6h | ~120 runs/月 |
| 2026-07-04 | wbs-ai-review 2h→4h | ~90 runs/月 |
| 2026-07-06 | claude-hygiene 8→4 runs/日, x-metrics 3h→6h | ~240 runs/月 |
| **2026-07-08** | **roadmap-guard + mcp-isolation cancel-in-progress: true** | **PR キュー積み防止** |
