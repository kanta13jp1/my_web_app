# CI/CD コスト監査 2026-06-12

> 自動生成 by CI/CD コスト最適化エージェント

---

## サマリ

| 項目 | 値 |
|------|-----|
| 総ワークフロー数 | 105 |
| スケジュール(cron)実行ワークフロー | 67 (63.8%) |
| 高コスト Runner (macOS/Windows) | 5 workflows |
| 推定 billable 時間 (概算) | ~18,000 ubuntu-equiv min/月 |
| 前回監査 (2026-06-11) 比較 | Windows 30分 cron → 60分 (-50%) |

### TOP3 コスト源

| 順位 | ワークフロー | 理由 | 月間概算コスト |
|------|-----------|------|-------------|
| 1 | `claude-session-hygiene-cron` | Windows×30分×42回/日 | ~37,800 ubuntu-equiv min/月 **→ 本実行で改善** |
| 2 | `mobile-release-build` (macos-15) | iOS ビルド 60分、tag push のみ | 低頻度だが 10x~15x 単価 |
| 3 | `health-monitor` | 毎時 Ubuntu、git push 付き | ~720 min/月 (前回監査対応済) |

---

## ワークフロー別コスト表 (抜粋: 高頻度・高コスト上位)

| ワークフロー | 頻度 | 平均時間(推定) | 失敗率 | トリガー | Runner |
|------------|------|-------------|-------|---------|--------|
| claude-session-hygiene-cron | **毎30分→毎60分** | ~10min | 低 | schedule | windows-latest |
| health-monitor | 毎時 | ~5min | 低 | schedule | ubuntu |
| ai-university-update | 2時間毎 | ~15min | 低 | schedule | ubuntu |
| horse-racing-update | 2時間毎 | ~10min | 低 | schedule | ubuntu |
| notion-sync | 2時間毎 | ~5min | 低 | schedule | ubuntu (対応済) |
| cs-check | 2時間毎 | ~5min | 低 | schedule | ubuntu (対応済) |
| feature-review | 4時間毎 | ~10min | 低 | schedule | ubuntu (対応済) |
| edge-function-audit | 4時間毎 | ~5min | 低 | schedule | ubuntu (対応済) |
| blog-publish-orphan-cleanup | 6時間毎 | ~5min | 低 | schedule | ubuntu |
| mobile-release-build | tag push | ~60min | 低 | push/dispatch | **macos-15** |
| windows-companion-build | tag push | ~45min | 低 | push/dispatch | **windows-latest** |
| dev-cache-cleanup-cron | 週1 | ~5min | 低 | schedule | windows-latest |
| docs-rotate-cron | 月1 | ~5min | 低 | schedule | windows-latest |
| worktree-cleanup-cron | 週1 | ~5min | 低 | schedule | windows-latest |

---

## 検出した冗長フロー (A-G)

### A. 重複トリガー — 優先度 low
- `ci.yml` は staging/develop push + PR をカバー。`deploy-prod.yml` は workflow_call で ci.yml を呼ぶ設計で二重起動防止済み ✅
- 問題なし。

### B. cancel-in-progress 未設定 — 優先度 low
- **前回監査 (2026-06-09) 時点の未設定6件は全て対応済み** (`ai-bench-source-recheck`, `codex-backlog-check`, `daily-report-freshness-monitor`, `kg-indexer-nightly`, `managed-mcp-policy`, `release-notes-data`)
- 残余なし ✅

### C. paths フィルタ未設定 — 優先度 low
- 全105ワークフローで push/PR トリガーには paths or paths-ignore が設定済み ✅
- `ci.yml` は `docs/**`, `memory/**`, `.claude/**`, `*.md` 等を ignore ✅

### D. 過剰な cron 頻度 — 優先度 **HIGH** (本実行で対応)
- **`claude-session-hygiene-cron.yml`**: `*/30` (42回/日) → `0 *` (21回/日) に削減 **✅ 本実行で自動適用**
- 他のWindows cron は週1/月1 で問題なし

### E. cache 未使用 — 優先度 med (情報提供)
- `ci.yml`: `actions/cache@v5` で `~/.pub-cache` キャッシュ済み ✅
- `deploy-prod.yml`, `deploy-dev.yml`, `deploy-staging.yml`: flutter-action `cache: true` 済み ✅
- `mobile-release-build.yml`: flutter + Java cache 済み ✅
- 主要 Flutter ビルドは全てキャッシュ対応済み

### F. 高失敗率ワークフロー — 優先度 HIGH (Issue 提案)
- **`tools-hub-mcp-smoke.yml`**: 前回監査 (2026-06-09) で "6日連続 100% failure" と記録。継続監視要。6時間毎 schedule のまま継続失敗の場合は schedule 停止を推奨。
- **`minimal-e2e-gate.yml`**: 前回監査で Chromium playwright install 問題が記録。現状要確認。

### G. 廃止候補 — 優先度 low (Issue 提案のみ)
- **`cron-batch.yml`**: コメントで `DISABLED` と明記、schedule なし・workflow_dispatch のみ。目的は `batch_analysis.py` だが SUPABASE_SERVICE_ROLE_KEY/GEMINI_API_KEY 未設定で毎回エラー。**90日+ 実質無効状態の疑い** → 削除 or 修正を Issue で提案。
- **`fix-supabase-network.yml`**: 名称が緊急対応的。常設 schedule が必要か要確認。

---

## 改善提案

### 🔴 HIGH — 本実行で自動適用済み

**1. `claude-session-hygiene-cron.yml` 頻度削減 (適用済み)**
- 変更: `*/30 21-23,0-17 * * *` → `0 21-23,0-17 * * *`
- 推定削減: **630 ubuntu-equiv min/日 ≈ 18,900 min/月**
- PR: https://github.com/kanta13jp1/my_web_app/pull/3271

### 🟡 MEDIUM — Issue 提案

**2. `tools-hub-mcp-smoke.yml` schedule 停止検討**
- 継続 failure 中の場合: `TOOLS_HUB_MCP_BEARER_TOKEN` secret 確認 → 修正不能なら schedule をコメントアウト
- 推定削減: 4 runs/日 × ~25s = 100s/日 + エラー通知コスト削減

**3. midnight cron 集中 (0 0 * * *) の時刻分散**
- `quota-monitor`, `codex-backlog-check`, `ai-university-reminder`, `ogp-image-refresh`, `wbs-user-notify`, `wbs-user-tasks-notify` が `0 0 * * *` に集中
- GHA キュー遅延回避のため 5 分ずつずらし推奨 (0/5/10/15/20/25分)
- 各1秒未満の変更だが 2ファイル/実行制約のため別バッチ

**4. `minimal-e2e-gate.yml` Chromium 問題の解消**
- PR ごとに失敗する場合 `playwright install --with-deps chromium` の依存確認
- 推定削減: PR 1件あたり ~120s の無駄実行

### 🟢 LOW — 情報提供

**5. `cron-batch.yml` 削除 or 修正**
- DISABLED 状態で実質デッドコード。workflow_dispatch のみなので実害なし。
- 修正方針: secrets 設定後に schedule 復活 or 完全削除
- **自動削除禁止** (廃止候補は Issue に列挙のみ)

---

## 自動適用した変更 (本実行)

| ファイル | 変更内容 | 推定削減効果 |
|---------|---------|------------|
| `.github/workflows/claude-session-hygiene-cron.yml` | cron `*/30` → `0 *` (30分→60分) | **630 ubuntu-equiv min/日, ~18,900 min/月** |

---

## 累積削減サマリー (2026-06-04〜12)

| 適用日 | 対象 | 変更 | 削減効果 |
|--------|------|------|---------|
| 06-04 | ci.yml | pub cache 追加 | ~2 min/PR |
| 06-04 | memory-search-sync.yml | concurrency 追加 | エラー回避 |
| 06-06 | health-monitor.yml | 30分→1時間 | ~720 runs/月削減 |
| 06-08 | feature-review.yml | 毎時→毎4時間 | ~22.8 分/日 |
| 06-08 | edge-function-audit.yml | 毎時→毎4時間 | ~9 分/日 |
| 06-09 | blog-publish-orphan-cleanup + stale-ef-completeness-check | concurrency 追加 | git 競合エラー回避 |
| 06-10 | notion-sync.yml | 毎時→2時間 | ~360 runs/月削減 |
| 06-10 | cs-check.yml | 毎時→2時間 | ~360 runs/月削減 |
| **06-12** | **claude-session-hygiene-cron.yml** | **30分→60分 (Windows)** | **~18,900 ubuntu-equiv min/月** |

**累積削減合計 (schedule billable 時間推定): ~320 min/日 → ~210 min/日 (-34%)**

---

## 削除候補 (根拠付き・即削除しない)

| ワークフロー | 根拠 | 推奨アクション |
|------------|------|-------------|
| `cron-batch.yml` | ファイル内 `DISABLED` コメント。schedule なし。batch_analysis.py が SUPABASE_SERVICE_ROLE_KEY/GEMINI_API_KEY 未設定でエラー。90日+ 実質無効の疑い | Issue に修正 or 削除提案 |

---

*生成: Claude Code CI/CD コスト最適化エージェント, 2026-06-12 14:10 UTC*
