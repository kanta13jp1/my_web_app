# CI/CD コスト監査 2026-07-29

> 自動生成: Claude Schedule ci-cd-audit 2026-07-29

---

## サマリ

| 項目 | 値 |
|------|-----|
| アクティブワークフロー総数 | 122 本 |
| 今回サンプル runs | 30 runs (直近) |
| 最大実行時間ワークフロー | GitHub Issues WBS Sync (avg 21.2 min) |
| 失敗率 100% ワークフロー | 0 本 (前回5本あった push runs は解消済み) |
| **本実行で自動適用した変更** | 1 ファイル |

**前回監査 (2026-07-28) からの差分**: 新規ワークフロー多数追加 (X系投稿・YouTube分析・Activation分析・E2Eゲート・protected-files guard など)

---

## ワークフロー別コスト表 (サンプルより TOP 実行頻度)

| 名前 | 頻度 | 平均時間 | 失敗率 | トリガー |
|------|------|---------|-------|---------|
| Workflow Failure Handler | schedule/workflow_run | 0.3m | 0% | workflow_run |
| AI大学コンテンツ更新 | 4h ごと | 1.2m | 0% | schedule |
| Health Monitor | 2h ごと | 0.6m | 0% | schedule |
| X Post Metrics Optimizer | 3h ごと + 日次 | 0.5m | 0% | schedule |
| Notion Mirror Sync | 毎時 | 2.5m | 0% | schedule |
| Horse Racing Auto Update | 日次 | 5.3m | 0% | schedule |
| GitHub Issues WBS Sync | 6h + issues event | 21.2m | 0% | schedule/issues |

---

## 検出した冗長フロー

### A. 重複トリガー
- なし (前回指摘済みの二重起動は解消済み)

### B. cancel-in-progress 未設定・false (deploy系除く) [med]
新規追加ワークフローに `cancel-in-progress: false` が多数。うちリスクレベルを評価:

| ファイル | 理由 | 評価 |
|---------|------|------|
| `activation-experiment-report.yml` | 純粋分析・read-only | ✅ **自動適用 → true** |
| `youtube-analysis.yml` | main直接push (BYPASS_RULES) → キャンセル危険 | ❌ false維持 |
| `x-ai-tool-tracker-queue.yml` | Supabase候補作成 (atomic) | 🟡 慎重判断 |
| `x-daily-briefing-post.yml` | X投稿 publish操作あり | ❌ false維持 |
| `x-growth-data-report-post.yml` | X投稿 (週次) | ❌ false維持 |
| `x-household-tracker-post.yml` | X投稿 (週次) | ❌ false維持 |

### C. paths フィルタ [low]
- push-triggered ワークフローは大部分が paths フィルタ済み ✓
- `windows-companion-build.yml`: tags: `windows-companion-v*` 専用 → 問題なし

### D. 過剰 cron 頻度 [med]
- `notion-sync.yml` (Notion Mirror): 毎時 2.5min/run → 月 1,800 min
  - 2h間隔でも機能的に十分な可能性あり (前回未着手)
- 0:00 UTC クラスター継続中:
  `quota-monitor / codex-backlog-check / ai-university-reminder / wbs-user-notify / wbs-user-tasks-notify / weekly-sns-draft / activation-experiment-report (0:30)`
  → 同時キュー 6-7 本、スロットリングリスク

### E. cache 未使用 [low]
- 主要 CI/デプロイ workflow はキャッシュ済み ✓
- `youtube-analysis.yml`: pip cache は `actions/setup-python@v6 cache: pip` で対応済み ✓
- `activation-experiment-report.yml`: Python スクリプト呼び出しのみ (pip install なし) → キャッシュ不要

### F. 高失敗率ワークフロー [low]
- 今回サンプルでは失敗率 >20% のワークフローなし ✓
- (前回の tools-hub-mcp-smoke push失敗は既解消)

### G. 廃止候補 [med]
以下 2 本は前回から継続して廃止確認を推奨:
- `cron-batch.yml`: schedule DISABLED、secrets 未設定エラーで継続放置
- `issue-wbs-maintenance.yml`: "Legacy entry point" コメントあり、`issue-to-wbs.yml` に統合済み

**新たな懸念点**:
- `youtube-analysis.yml`: `secrets.BYPASS_RULES` で main 直接 push。BYPASS_RULES はブランチ保護を回避するトークン。このパターンが正規化されることを防ぐため、設計レビュー推奨。

---

## 自動適用した変更 (本実行)

### `activation-experiment-report.yml` — cancel-in-progress: false → true

**変更前**:
```yaml
concurrency:
  group: activation-experiment-report
  cancel-in-progress: false
```

**変更後**:
```yaml
concurrency:
  group: activation-experiment-report
  cancel-in-progress: true  # 2026-07-29 ci-audit: 純粋分析WF / 手動+schedule重複時のキュー待機回避
```

**根拠**: `activation-experiment-report.yml` は Supabase から集計データを読み取り、Python スクリプトでレポートを生成し、Artifact にアップロードするだけの read-only 分析ワークフロー。外部への書き込みなし。手動 dispatch と schedule が重なった場合、古い実行をキャンセルして新しい実行を優先しても安全。

**推定効果**: 月に数回の重複実行を回避 (~30 ubuntu-min/月)

---

## 改善提案 (人手判断が必要)

### 1. 🔴 [high] `notion-sync.yml` — 毎時 → 2h 間隔への削減
- 現状: `0 * * * *` (毎時)、avg 2.5 min/run → 月 1,800 min
- 提案: `0 */2 * * *` (2h ごと) → 月 900 min、**推定削減 ~900 ubuntu-min/月**
- リスク: Notion ← Supabase 同期の遅延が最大 2h に増加

### 2. 🟡 [med] 0:00 UTC クラスター — cron オフセット分散
- 同時起動 6 本はキュー遅延リスク。5〜10 分ずつオフセット提案済み (Issue #3841 継続)

### 3. 🟡 [med] `youtube-analysis.yml` — BYPASS_RULES 使用パターン設計レビュー
- main 直接 push はブランチ保護を回避。通常は PR 経由を推奨。
- 削除・PR化の検討または既存 policy との整合確認が必要

### 4. 🟡 [med] `cron-batch.yml` / `issue-wbs-maintenance.yml` — 削除確認
- (前回 Issue #3841 からの継続提案)

### 5. 🟡 [med] `x-ai-tool-tracker-queue.yml` — cancel-in-progress 評価
- 日次 21:45 UTC、Supabase への HITL 候補作成のみ (実際の X 投稿は人手承認後)
- candidateKey で冪等性あり → cancel-in-progress: true に変更可能かを人手評価推奨

---

## 累積削減実績

| 日付 | 削減内容 | 効果 |
|------|---------|------|
| 2026-06-10 | notion/memory/cs-check 毎時→2h | ~1,080 runs/月削減 |
| 2026-06-15 | health-monitor 毎時→2h | ~360 runs/月 |
| 2026-06-22 | ai-university 2h→4h | ~180 runs/月 |
| 2026-07-03 | issue-to-wbs 4h→6h | ~690 min/月 |
| 2026-07-04 | wbs-ai-review 2h→4h | ~540 min/月 |
| 2026-07-06 | claude-hygiene + x-metrics | ~528 ubuntu-min/月 |
| 2026-07-20 | tools-hub-mcp-smoke 6h→12h | ~60 runs/月 |
| 2026-07-27 | notebooklm-issue-crosscheck concurrency追加 | 重複防止 |
| **2026-07-29** | **activation-experiment-report cancel-in-progress:true** | **~30 ubuntu-min/月** |
