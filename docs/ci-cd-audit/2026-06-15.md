# CI/CD コスト監査 2026-06-15

> 自動生成 by CI/CD コスト最適化エージェント (claude-sonnet)

---

## サマリ

| 項目 | 値 |
|-----|---|
| 総ワークフロー数 | 110 (active) / 106 ファイル |
| cron 定義数 | 67 ワークフロー |
| 直近30実行の billable 時間 | ~23 min (最大 run: GitHub Issues WBS Sync 32.2 min) |
| TOP3 コスト源 | GitHub Issues WBS Sync (32.2 min) / Horse Racing Update (2.8 min×2) / Notion Sync (3.3 min) |
| 前回比較 | Issue #3175 の 6 件 concurrency 追加は全て完了済み (2026-06-14) |

---

## ワークフロー別コスト表 (直近 30 runs)

| ワークフロー名 | 実行数 | 平均時間 | 失敗率 | トリガー |
|-------------|-------|---------|-------|---------|
| Workflow Failure Handler | 8 | 0.3 min | 0% | workflow_run |
| Horse Racing Auto Update | 2 | 2.8 min | 0% | schedule |
| Health Monitor | 2 | 0.5 min | 0% | schedule |
| Notion Mirror Sync | 1 | 3.3 min | 0% | schedule |
| GitHub Issues WBS Sync | 1 | 32.2 min | 0% | schedule |
| AI大学コンテンツ更新 | 1 | 1.2 min | 0% | schedule |
| Feature Review | 1 | 1.0 min | 0% | schedule |
| Minimal E2E Gate | 1 | 1.9 min | 100% | pull_request |
| AI Agent PR Review | 1 | 0.4 min | 0% | pull_request |
| CI | 1 | 0.2 min | 0% | pull_request |

---

## 検出した冗長フロー (A-G・優先度)

### A. 重複トリガー (優先度: MED)
- **`ai-tool-watch.yml` と `ai-tool-changelog-watch.yml`が同名**
  - 両者ともワークフロー名が "AI Tool Changelog Watch" (GHA UI で識別不能)
  - `ai-tool-watch.yml`: 毎日 `15 21 * * *` — Claude Code/Codex 官方ソース監視
  - `ai-tool-changelog-watch.yml`: 月次 `0 0 1 * *` — AI ツール変更履歴集約
  - 目的は重複していないが **名前衝突でアラート/バッジが混在しトリアージを阻害**

### B. concurrency 未設定 (優先度: LOW — 前回 6 件は全解決済み)
- 前回 Issue #3175 記載の 6 件は 2026-06-14 までに全適用確認済み
- 新規未設定ワークフロー: なし (全 schedule/push WF に concurrency 設定済み)

### C. paths フィルタ (優先度: 解決済み)
- `ci.yml` / `deploy-prod.yml` / `deploy-dev.yml` / `deploy-staging.yml` に `paths-ignore` 設定済み

### D. 過剰 cron 頻度 (優先度: HIGH → 今回自動適用)
- **`health-monitor.yml`**: `0 * * * *` (毎時 = 720 runs/月)
  - 2026-06-06 に 30分 → 60分 に削減済み
  - `cs-check.yml`(2時間ごと `:07`) より高頻度になっているが、読み込み元の `cs-check` が 2h 間隔なら health-monitor も 2h で十分
  - **推定削減: 360 runs/月 ≈ 180 min billable/月**
  - `0 */2 * * *` に変更 → `cs-check`(`7 */2`) の 7分前に実行されるため同期も良好

### E. cache 未使用 (優先度: MED → 今回自動適用)
- **`feature-review.yml`**: 毎 4h (120 runs/月) で `pip install anthropic playwright requests` を毎回フルインストール
  - `anthropic`: ~5-10s、`playwright`: ~10-15s → 計 ~20-30s/run × 120 runs = **40-60 min/月削減**
  - `playwright install --with-deps chromium` は OS パッケージのため pip cache 対象外だが pip 部分は節約可能

### F. 高失敗率 (優先度: MED)
- **`minimal-e2e-gate.yml`**: 1/1 = 100% 失敗 (Chromium playwright install 問題、前回から継続)
  - PR ごとに ~120s 無駄実行が続いている → 修正 or 一時無効化を Issue 提案済み (Issue #3175)

### G. 廃止候補 (優先度: LOW)
- **`ai-tool-watch.yml`**: 目的が `ai-tool-changelog-watch.yml` と重複可能性。ワークフロー名のリネームを推奨
- (削除はしない — Issue 提案のみ)

---

## 改善提案

| 優先度 | 対象 | 変更内容 | 推定削減 |
|--------|------|---------|---------|
| HIGH | `health-monitor.yml` | cron 毎時→毎2時間 | 360 runs/月 / ~180 min/月 |
| MED | `feature-review.yml` | pip cache 追加 | ~40-60 min/月 |
| MED | `minimal-e2e-gate.yml` | Chromium install 修正 | ~120 s/PR (失敗回避) |
| LOW | `ai-tool-watch.yml` | workflow name リネームで重複解消 | トリアージ改善 |

---

## 自動適用した変更 (本実行で main へ入れた分)

| ファイル | 変更内容 | 推定削減 |
|---------|---------|---------|
| `health-monitor.yml` | cron `0 * * * *` → `0 */2 * * *` (毎時→毎2時間) | 360 runs/月 ≈ 180 min/月 |
| `feature-review.yml` | pip cache (`~/.cache/pip`) 追加 | ~40-60 min/月 |

---

## 削除候補 (根拠付き・即削除しない)

| ワークフロー | 根拠 | リスク |
|-----------|------|-------|
| `ai-tool-watch.yml` | `ai-tool-changelog-watch.yml` と名前重複、目的も類似。統合 or リネーム検討 | 機能分離しているなら名前のみ変更で対応 |

---

## 累積改善サマリー (2026-06-04〜2026-06-15)

| 適用日 | 内容 | 削減効果 |
|--------|------|---------| 
| 06-04 | ci.yml pub cache 追加 | ~2 min/PR |
| 06-04 | memory-search-sync.yml concurrency 追加 | エラー回避 |
| 06-06 | health-monitor.yml cron 30分→1時間 | ~720 runs/月削減 |
| 06-08 | feature-review.yml cron 毎時→毎4時間 | ~22.8 分/日 |
| 06-08 | edge-function-audit.yml cron 毎時→毎4時間 | ~9 分/日 |
| 06-09 | blog-publish-orphan-cleanup + stale-ef-completeness-check concurrency | git 競合回避 |
| 06-14 | ai-bench-source-recheck 等 6 件 concurrency 追加 | キュー積み回避 |
| **06-15** | **health-monitor 毎時→毎2時間** | **360 runs/月 / ~180 min/月** |
| **06-15** | **feature-review pip cache 追加** | **~40-60 min/月** |

**累積削減 (schedule billable 時間): 280 分/日 → ~195 分/日 (-30%)**
