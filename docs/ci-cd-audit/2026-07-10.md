# CI/CD コスト監査 2026-07-10

> 自動生成: Claude Schedule ci-cd-audit 2026-07-10 (UTC 14:xx)

## サマリ

| 項目 | 値 |
|------|----|
| 総ワークフロー数 | 111 本 |
| cron 有効 | 71 本 |
| 推定 runs/月 合計 (cron のみ) | ~3,100 runs/月 |
| 本実行で自動適用 | **2 ファイル** |
| concurrency 未設定 (適用前) | 1 本 (public-memo-smoke) |
| TOP3 コスト源 | claude-session-hygiene-cron (windows×240/月) / infra-health-check (360/月) / x-post-metrics-optimizer (270/月) |

## ワークフロー別コスト表 (上位 20)

| ワークフロー名 | 頻度 | runs/月 | runner | cancel-in-progress | 備考 |
|--------------|------|---------|--------|---------------------|------|
| infra-health-check | */2h | 360 | ubuntu | true | 2h 間隔は最適済 |
| memory-search-sync | */2h | 360 | ubuntu | true | 2h 間隔は最適済 |
| cs-check | */2h | 360 | ubuntu | true | 2h 間隔は最適済 |
| notion-sync | */2h | 360 | ubuntu | false | Notion rate limit で false 維持 |
| health-monitor | */2h | 360 | ubuntu | true | 2h 間隔は最適済 |
| mcp-audit-anomaly-cron | */2h | 360 | ubuntu | conditional | PR のみ true |
| x-post-metrics-optimizer | */3h+daily | 270 | ubuntu | true | 前回審査: 要削減 |
| claude-session-hygiene-cron | 8x/日 | 240 | **windows** | true | **windows×2倍課金** |
| ai-university-update | */4h | 180 | ubuntu | false | 4h 最適済 |
| edge-function-audit | */4h | 180 | ubuntu | true | 4h 最適済 |
| horse-racing-update | */4h | 180 | ubuntu | false | 日次でも十分か要検討 |
| schedule-resilience-watch | */4h | 180 | ubuntu | false | 要確認 |
| wbs-ai-review | */4h | 180 | ubuntu | true | 4h 最適済 |
| feature-review | */4h | 180 | ubuntu | true | 4h 最適済 |
| tools-hub-mcp-smoke | */6h | 120 | ubuntu | false | 6h 最適済 |
| issue-to-wbs | */6h | 120 | ubuntu | true | 6h 最適済 |
| blog-publish-orphan-cleanup | */6h | 120 | ubuntu | true | 6h 最適済 |
| wbs-user-notify | daily | 30 | ubuntu | false | 重複: wbs-user-tasks-notify と同機能 |
| wbs-user-tasks-notify | daily | 30 | ubuntu | false | 重複: wbs-user-notify と同機能 |
| public-memo-smoke | daily | 30 | ubuntu | **なし** | concurrency 未設定 ← 本実行で追加 |

## 検出した冗長フロー

### A) 重複トリガー
- `wbs-user-notify.yml` + `wbs-user-tasks-notify.yml`: 同一 cron `0 0 * * *` で類似の WBS ユーザータスク通知を両方実行。前回監査 #3656 で指摘済み・未対処。**[med]**

### B) cancel-in-progress 未設定 (積み上がりリスク)
- `public-memo-smoke.yml`: 唯一 concurrency ブロックなし。毎日実行 + 手動実行が重なるとキュー積み。**[low]** ← **本実行で自動適用**

### C) paths フィルタ
- `public-memo-smoke.yml`: paths-ignore なし。docs 変更でも起動するが 5 min タイムアウトなので影響軽微。

### D) 過剰な cron 頻度
- `claude-session-hygiene-cron.yml`: **windows-latest で 8x/日 (240 runs/月)**。windows runner は ubuntu の 2 倍課金。現在の 180 分間隔 (8x/日) を 360 分間隔 (4x/日) に削減余地あり。前回監査 #3841 で提案済みだが未適用。**[high]** ← **本実行で自動適用**
- `x-post-metrics-optimizer.yml`: `*/3 * * * *` fresh tier (8x/日) + daily backfill (1x/日) = 270 runs/月。`*/6` に変更で 30 runs/月 削減可能。**[med]**

### E) cache 未使用
- `public-memo-smoke.yml`: checkout のみ, ビルドなし → cache 不要。

### F) 失敗率
- `Minimal E2E Gate`: 100% 失敗 (1/1 runs)。chrome テストまたは依存関係の問題の可能性。要確認。**[med]**
- `AI Agent PR Review`: 100% 失敗 (1/1 runs)。PR レビューのトリガー問題。要確認。**[low]**

### G) 廃止候補
- `cron-batch.yml`: コメントで DISABLED 明示。schedule 無効・`workflow_dispatch` のみ。secrets 未設定で毎回エラー。削除 or secrets 設定の判断が必要。(前回監査より継続)
- `wbs-user-tasks-notify.yml`: `wbs-user-notify.yml` と機能重複。どちらかを削除推奨。
- `hotfix-stripe-url.yml` / `fix-supabase-network.yml`: hotfix 系一時ワークフロー。恒久運用不要なら削除。

## 改善提案

| # | 優先度 | ワークフロー | 提案 | 推定削減 |
|---|--------|------------|------|---------|
| 1 | **high** | `claude-session-hygiene-cron.yml` | 8x/日 → 4x/日 (windows runner) | ~120 win-min/月 = ~240 ubuntu換算 |
| 2 | **med** | `x-post-metrics-optimizer.yml` | `*/3` → `*/6` fresh tier | ~120 runs/月 |
| 3 | **med** | `wbs-user-notify` + `wbs-user-tasks-notify` | 統合 (一方削除) | ~30 runs/月 + 構造改善 |
| 4 | **med** | `horse-racing-update.yml` | 4h → 12h or daily | ~120 runs/月 |
| 5 | **low** | `public-memo-smoke.yml` | concurrency 追加 | キュー積み防止 |
| 6 | **low** | `cron-batch.yml` | 削除 or secrets 設定後に復活 | 10~20 runs/月 (DISABLED中) |

## 自動適用した変更 (本実行)

| ファイル | 変更内容 | 推定削減効果 |
|---------|---------|------------|
| `public-memo-smoke.yml` | concurrency block 追加 (`cancel-in-progress: true`) | キュー積み防止 |
| `claude-session-hygiene-cron.yml` | cron 8x/日 → 4x/日 (windows-latest コスト削減) | ~120 windows-min/月 ≒ 240 ubuntu換算min |

**合計: ~240 ubuntu換算 min/月削減**

## 削除候補 (根拠付き・即削除しない)

| ワークフロー | 根拠 |
|------------|------|
| `cron-batch.yml` | コメントで DISABLED、secrets 未設定でエラー継続 |
| `wbs-user-tasks-notify.yml` | `wbs-user-notify.yml` と機能重複 |
| `hotfix-stripe-url.yml` | hotfix 一時ワークフロー、恒久運用不要と思われる |
| `fix-supabase-network.yml` | hotfix 一時ワークフロー、恒久運用不要と思われる |

## 累積削減実績 (過去監査)

| 日付 | 削減内容 | 効果 |
|------|---------|------|
| 2026-06-10 | notion/memory/cs-check 毎時→2h | ~1,080 runs/月 |
| 2026-06-15 | health-monitor 毎時→2h | ~360 runs/月 |
| 2026-06-22 | ai-university 2h→4h | ~180 runs/月 |
| 2026-06-25 | deploy-prod pub cache 追加 | ~2-3 min/run |
| 2026-07-03 | issue-to-wbs 4h→6h | ~690 min/月 |
| 2026-07-04 | wbs-ai-review 2h→4h | ~540 min/月 |
| 2026-07-09 | x-post-metrics cancel-in-progress: true | キュー積み防止 |
| **2026-07-10** | **public-memo-smoke concurrency + claude-hygiene 8→4x** | **~240 ubuntu換算 min/月** |
