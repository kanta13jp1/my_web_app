# CI/CD コスト監査 2026-07-04

> 自動生成: Claude Schedule ci-cd-audit
> 前回レポート: `docs/ci-cd-audit/2026-07-03.md` / Issue #3656

---

## サマリ

| 項目 | 値 |
|------|-----|
| 総ワークフロー数 | 109 |
| schedule (cron) 持ち | 69 |
| 非 cron (push/PR/dispatch のみ) | 40 |
| concurrency 未設定 | 1 (`hotfix-stripe-url.yml`) |
| cancel-in-progress: true | 33 |
| cancel-in-progress: false | 75 |
| windows-latest 使用 (billable 2x) | 5 |
| Deno 使用 (キャッシュなし) | 6 |

### 推定 billable 時間 (月間)

| 頻度帯 | 代表ワークフロー | 推定 runs/月 | 推定 min/月 |
|--------|----------------|-------------|------------|
| 2時間毎 (12x/日) | cs-check / health-monitor / infra-health-check / mcp-audit / memory-sync / notion-sync / wbs-ai-review | ~2,520 | ~3,780 |
| 3時間毎 (8x/日) | x-post-metrics-optimizer | ~240 | ~480 |
| 4時間毎 (6x/日) | ai-university-update / edge-function-audit / feature-review / horse-racing / schedule-resilience | ~900 | ~1,350 |
| 6時間毎 (4x/日) | blog-publish-orphan / issue-to-wbs / tools-hub-smoke | ~360 | ~360 |
| 日次 (30x/月) | 約30本 | ~900 | ~900 |
| 週次/月次 | 約20本 | ~80 | ~80 |
| **合計 (推定)** | | **~5,000** | **~6,950 min** |

> windows-latest は billable 2x なので `claude-session-hygiene-cron` (8x/日) = 実質 ~1,440 min/月 相当。

---

## ワークフロー別コスト表 (主要 TOP15)

| ワークフロー | 頻度 | 平均時間 | トリガー | 備考 |
|-------------|------|---------|---------|------|
| `claude-session-hygiene-cron` | 8x/日 (180min毎) | ~5 min | schedule | **windows-latest = 2x** / 既に 30min→180min 最適化済 |
| `cs-check` | 12x/日 | ~1 min | schedule | 2h (06-10 最適化済) |
| `health-monitor` | 12x/日 | ~2 min | schedule | 2h (06-15 最適化済) |
| `infra-health-check` | 12x/日 | ~2 min | schedule | 2h |
| `mcp-audit-anomaly-cron` | 12x/日 | ~1 min | schedule/PR | 2h |
| `memory-search-sync` | 12x/日 | ~2 min | schedule | 2h (06-11 最適化済) |
| `notion-sync` | 12x/日 | ~3 min | schedule | 2h (06-10 最適化済) |
| `wbs-ai-review` | 12x/日 | ~3 min | schedule | **2h → 4h 提案中** |
| `x-post-metrics-optimizer` | 8x/日 | ~2 min | schedule | 3h / concurrency 追加済 (07-03) |
| `competitor-monitoring` | 1x/日 | ~5 min | schedule | daily |
| `wiki-compile-cron` | 1x/日 | ~3 min | schedule | daily |
| `daily-report` | 1x/日 | ~5 min | schedule | daily |
| `ai-university-update` | 6x/日 | ~3 min | schedule | 4h (06-22 最適化済) |
| `edge-function-audit` | 6x/日 | ~2 min | schedule | 4h (06-08 最適化済) |
| `feature-review` | 6x/日 | ~2 min | schedule | 4h (06-08 最適化済) |

---

## 検出した冗長フロー (A-G)

### A) 重複トリガー
- **優先度: low** (現状なし) — push + schedule の二重実行は既存 paths-ignore で対策済。

### B) cancel-in-progress 未設定 (deploy 系除く)
- **優先度: high** — `hotfix-stripe-url.yml` のみ `concurrency:` ブロック自体が存在しない。
  - trigger: `push branches:[main] paths:[.github/workflows/hotfix-stripe-url.yml]`
  - 連続 push で複数実行が積み上がる可能性あり (実頻度低だが設定漏れ)
  - **→ 本実行で自動追加**

### C) paths フィルタなし (push 系)
- **優先度: low** — push トリガー持ち yml の全て (windows-companion-build 含む) に paths または paths-ignore が設定済。改善余地なし。

### D) 過剰 cron 頻度
- **優先度: high** — `wbs-ai-review.yml` が `*/2` (2時間毎) で動作中。
  - WBS タスクの `ai_review_status='requested'` は人間/エージェントが手動セットするため、2h 精度は過剰。
  - 4h に緩和しても最大待機が 2h 増えるだけで実害なし。
  - 削減効果: 180 runs/月 × ~3 min = **540 min/月削減**
  - **→ 本実行で自動変更**
- **優先度: med** — midnight UTC cluster (0 0 * * *): `quota-monitor`, `codex-backlog-check`, `ai-university-reminder`, `wbs-user-notify`, `wbs-user-tasks-notify` の 5 本が同一スロット (09:00 JST) に集中し GHA キュー圧迫の可能性。
  - 対策: 5〜10 分ずつオフセット (例: 5 0, 10 0, 15 0, 20 0)
  - 総実行数は変わらないがキュー待機時間削減
  - **→ Issue 提案**
- **優先度: low** — `x-post-metrics-optimizer` 3h (07-03 に concurrency 追加済)。さらに 6h 化は効果あるが低優先。

### E) キャッシュ未使用
- **優先度: med** — 6本の Deno ワークフローが `denoland/setup-deno@v2` 使用時に `DENO_DIR` キャッシュなし。ただし `mcp_audit_anomaly.ts` に外部 import がなく Deno binary キャッシュで事足りる可能性が高い。要確認後に追加。
- **優先度: low** — Flutter/pub 系: ci.yml / deploy-prod/dev/staging 全て対処済。

### F) 失敗率高ワークフロー
- **優先度: med** — 直近 30 runs の取得分では failure 0件。過去失敗の多いものは 07-03 以前のレポートを参照。

### G) 廃止候補
- **優先度: med** — `cron-batch.yml`: schedule DISABLED / dispatch のみ / secrets 未設定で毎回エラー。削除候補 (前回提案継続)。
- **優先度: med** — `wbs-user-notify.yml` + `wbs-user-tasks-notify.yml`: 両方 `cron: 0 0 * * *` で WBS ユーザー通知を重複実行。wbs-user-tasks-notify に hardcoded ANON key あり (セキュリティ懸念)。統合推奨。
- **優先度: low** — `hotfix-stripe-url.yml`: Stripe URL タイポ修正専用。Issue #3644 が resolve 済なら廃止可。push trigger が self-referencing で実質デッドコード化している可能性。
- **優先度: low** — `issue-wbs-maintenance.yml`: `issue-to-wbs.yml` の単純ラッパーの可能性。重複確認要。

---

## 改善提案

| # | 提案 | 対象 | 推定削減 | カテゴリ |
|---|------|------|---------|---------|
| 1 | **[本実行適用]** `wbs-ai-review` cron 2h → 4h | `wbs-ai-review.yml` | 180 runs = 540 min/月 | D |
| 2 | **[本実行適用]** `hotfix-stripe-url` concurrency 追加 | `hotfix-stripe-url.yml` | キュー積み防止 | B |
| 3 | midnight UTC cluster 5本をオフセット | 5本 | キュー待機削減 | D |
| 4 | `wbs-user-notify` + `wbs-user-tasks-notify` 統合 | 2本 | 30 runs/月 + key 削除 | G |
| 5 | `cron-batch.yml` 削除 | 1本 | エラー削減 | G |
| 6 | Deno 使用 workflow に `DENO_DIR` キャッシュ追加 | 6本 | ~2-3 min/run × 360 runs/月 | E |
| 7 | windows-latest → ubuntu-latest 移行 | `claude-session-hygiene-cron` 等 | 2x → 1x billable (最大 1,440 min/月相当) | — |
| 8 | `hotfix-stripe-url.yml` 廃止 (Issue #3644 完了確認後) | 1本 | 微 | G |

---

## 自動適用した変更 (本実行)

### 1. `wbs-ai-review.yml` — cron 2h → 4h

```yaml
# before
- cron: "20 */2 * * *"  # 2時間ごと 20 分
# after
- cron: "20 */4 * * *"  # 4時間ごと 20 分 (2026-07-04 ci-audit: 2h→4h, 540min/月削減)
```

**削減効果**: 360 runs/月 → 180 runs/月、推定 540 min/月削減

### 2. `hotfix-stripe-url.yml` — concurrency ブロック追加

```yaml
concurrency:
  group: hotfix-stripe-url
  cancel-in-progress: true  # 2026-07-04 ci-audit: push 連続時キュー積み防止
```

---

## 削除候補詳細

| ファイル | 廃止根拠 | リスク | 状態 |
|---------|---------|--------|------|
| `cron-batch.yml` | schedule DISABLED / secrets 未設定で毎回エラー | 低 | 3回目の提案 (未対応) |
| `wbs-user-tasks-notify.yml` | `wbs-user-notify.yml` と機能重複 / hardcoded ANON key | 中 | 前回提案継続 |
| `hotfix-stripe-url.yml` | self-referencing / Issue #3644 完了後は不要 | 低 | 前回提案継続 |
| `issue-wbs-maintenance.yml` | `issue-to-wbs.yml` の重複ラッパー疑い | 低 | 前回提案継続 |

---

## windows-latest 使用一覧 (billable 2x コスト)

| ファイル | 頻度 | 移行難易度 |
|---------|------|-----------|
| `claude-session-hygiene-cron.yml` | 8x/日 (最大コスト源) | 中 (PowerShell → bash) |
| `dev-cache-cleanup-cron.yml` | 週次 | 低 |
| `docs-rotate-cron.yml` | 月次 | 低 |
| `worktree-cleanup-cron.yml` | 週次 | 低 |
| `windows-companion-build.yml` | タグ時のみ | 不可 (Windowsビルド必須) |

---

## 累積削減効果 (2026-06-04〜2026-07-04)

| 期間 | 主要施策 | 推定削減 |
|------|---------|---------|
| 06-04〜06-25 | cache/concurrency/cron 最適化 11件 | ~1,000+ runs/月 |
| 06-25 | deploy-prod.yml pub cache | ~2-3 min/run |
| 07-03 | x-post concurrency + issue-to-wbs 4h→6h | ~690 min/月 + キュー防止 |
| **07-04** | wbs-ai-review 2h→4h + hotfix concurrency | **~540 min/月削減** |
| 次回候補 | midnight cluster オフセット + Deno cache + ubuntu移行 | ~3,000+ min/月 |

---

*CI/CD コスト監査 自動生成 — Claude Schedule 2026-07-04*
