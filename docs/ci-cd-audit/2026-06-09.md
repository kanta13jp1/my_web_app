# CI/CD コスト監査 2026-06-09

> 生成: CI/CD コスト最適化エージェント (自動実行 2026-06-09 14:06 UTC)

## サマリ

| 項目 | 値 |
|------|----||
| 総ワークフロー数 | **105** (前回 108 → 削除/統合 3 件) |
| うち hourly+ schedule | **4** ワークフロー (health-monitor は 06-06 に 30 分→60 分適用済) |
| concurrency 未設定 (deploy 除く) | **8** ワークフロー (前回 13 → 本実行 -2 で **6** へ) |
| 推定 schedule billable 時間/日 | **≈ 220 分/日** (前回 280 分から -60 分改善) |
| 本実行で自動削減 (見込み) | **≈ 5 分/月** (git 競合エラー回避 + 並行実行積み上がり防止) |

## TOP3 コスト源 (schedule 推定消費時間/日)

| ランク | ワークフロー | 頻度 | 推定時間/day |
|--------|-------------|------|------------|
| 1 | Horse Racing Auto Update | 毎時 (720 回/月・timeout 45 min) | **~90 分** |
| 2 | Notion Mirror Sync (hourly) | 毎時 (720 回/月) | **~60 分** |
| 3 | CS Check | 毎時 (720 回/月) | **~40 分** |

## ワークフロー別コスト表 (schedule / push 主要のみ)

| ワークフロー名 | 頻度 | 平均時間 (推定) | 失敗率 (前回比) | トリガー |
|--------------|------|---------------|--------------|--------|
| Horse Racing Auto Update | 毎時 `0 * * * *` | ~8 min | 0% | schedule |
| Notion Mirror Sync | 毎時 `10 * * * *` | ~3 min | 0% | schedule |
| CS Check | 毎時 `7 * * * *` | ~2 min | 0% | schedule |
| Health Monitor | 毎時 `0 * * * *` | ~2 min | 0% | schedule (06-06: 30分→1時間済) |
| claude-session-hygiene-cron | `*/30 21-23,0-17` (~40回/日) | ~1 min | 0% | schedule |
| memory-search-sync | 毎時 `12 * * * *` | ~1 min | 0% | schedule (cancel-in-progress:true 済) |
| tools-hub-mcp-smoke | 6時間毎 | ~25 s | **100%** ❌ | schedule |
| minimal-e2e-gate | PR ごと | ~2 min | **100%** ❌ | pull_request |
| feature-review | 毎4時間 | ~75 s | 0% | schedule (06-08 削減済) |
| edge-function-audit | 毎4時間 | ~30 s | 0% | schedule (06-08 削減済) |
| wbs-ai-review | 毎2時間 `20 */2 * * *` | ~10 s | 0% | schedule |
| infra-health-check | 毎2時間 `37 */2 * * *` | ~30 s | 0% | schedule |

## 前回 (2026-06-04〜08) からの差分

| 前回提案 | 状態 |
|---------|------|
| feature-review.yml cron 毎時→毎4時間 | ✅ 06-08 適用済 |
| edge-function-audit.yml cron 毎時→毎4時間 | ✅ 06-08 適用済 |
| health-monitor.yml cron 30分→1時間 | ✅ 06-06 適用済 |
| ci.yml pub cache 追加 | ✅ 06-04 適用済 |
| memory-search-sync.yml concurrency 追加 | ✅ 06-04 適用済 |
| blog-publish-orphan-cleanup.yml concurrency 追加 | ✅ **本実行 (06-09) で適用** |
| stale-ef-completeness-check.yml concurrency 追加 | ✅ **本実行 (06-09) で適用** |
| tools-hub-mcp-smoke.yml 100% failure 修正 | ❌ 未対応 (継続中) |
| minimal-e2e-gate.yml Chrome セットアップ修正 | ❌ 未対応 (継続中) |
| setup-flutter-sdk actions/cache 追加 | ❌ 未対応 |
| wbs-user-notify + wbs-user-tasks-notify 統合 | ❌ 未対応 |

## 検出した冗長フロー (A-G) — 2026-06-09 現在

### [HIGH-A] 100% failure ワークフロー (継続未対応)

| ワークフロー | 頻度 | 浪費推定 |
|------------|------|---------|
| `tools-hub-mcp-smoke.yml` | 6h ごと (4回/日) | ~100 s/日 |
| `minimal-e2e-gate.yml` | PR ごと | PR 1件に ~120 s |

> ⚠️ `tools-hub-mcp-smoke.yml` は **2026-06-04〜09 で 6 日連続 100% failure**。`TOOLS_HUB_MCP_BEARER_TOKEN` secret の確認、または schedule の一時停止を推奨。

### [HIGH-D] 過剰 cron 頻度 — 残存 hourly ワークフロー

| ワークフロー | 現在 | 推奨 | 推定削減/日 |
|------------|------|------|------------|
| `notion-sync.yml` | 毎時 | 毎2時間 | 12 runs × 3 min = **36 分** |
| `cs-check.yml` | 毎時 | 毎2時間 | 12 runs × 2 min = **24 分** |
| `horse-racing-update.yml` | 毎時 | 変更せず (時刻依存ロジックあり) | — |
| `memory-search-sync.yml` | 毎時 | 毎2時間 | 12 runs × 1 min = **12 分** |

> `horse-racing-update.yml` は step 内で JST 時刻により処理を切り替える設計のため、hourly のまま維持を推奨。

### [med-B] concurrency 未設定 残り 6 件 (deploy 除く)

次回以降に順次適用推奨 (2 件/実行の制約):

```
ai-bench-source-recheck.yml
codex-backlog-check.yml
daily-report-freshness-monitor.yml
kg-indexer-nightly.yml
managed-mcp-policy.yml
release-notes-data.yml
```

### [med-C] paths-ignore 未設定 (push/PR トリガー持ち)

docs のみ変更でもジョブが全実行:

```
claude-agent-review.yml
feedback-issue-resolved.yml
github-issue-fix.yml
issue-to-wbs.yml
minimal-e2e-gate.yml
windows-companion-build.yml
```

### [med-G] 同一 cron 集中 — midnight 渋滞

以下 6 ワークフローが `0 0 * * *` (UTC 00:00) に集中実行:

```
quota-monitor.yml
codex-backlog-check.yml
ai-university-reminder.yml
ogp-image-refresh.yml
wbs-user-notify.yml
wbs-user-tasks-notify.yml
```

推奨: 各々 5〜15 分ずらして `0 0`, `5 0`, `10 0`, `15 0`, `20 0`, `25 0` に分散。

### [low-G] 重複/廃止候補

| ファイル | 根拠 |
|---------|------|
| `wbs-user-notify.yml` + `wbs-user-tasks-notify.yml` | 同一 cron (0 0 * * *)、類似目的 (user task を通知)。統合候補 |
| `ai-tool-watch.yml` + `ai-tool-changelog-watch.yml` | 同種 AI ツール監視。目的重複の可能性 |
| `cron-batch.yml` | workflow_dispatch のみ・schedule なし。整理候補 |

## 改善提案

### 優先度 HIGH (継続未対応)

1. **`tools-hub-mcp-smoke.yml` schedule 停止または修正**  
   6 日連続 100% failure。推定削減: 4 runs/日 × 25 s = **~1.7 分/日** + エラー通知コスト削減

2. **`minimal-e2e-gate.yml` Chrome セットアップ修正**  
   PR ごとに ~120 s 浪費。`playwright install --with-deps chromium` の依存パッケージ確認

3. **`setup-flutter-sdk` + `actions/cache@v4` 追加**  
   毎回フル clone (~2-3 分) → cache 化で deploy ジョブ 2-3 min/回削減 (720 回/月換算で ~1,440-2,160 分)

### 優先度 med (新規提案)

4. **`notion-sync.yml` + `cs-check.yml` hourly → 2時間毎へ削減**  
   推定削減: 合計 ~60 分/日 ≈ **1,800 分/月**

5. **midnight cron 集中 6 件を 5 分ずつ分散**  
   GHA キュー遅延を防ぎ、各ジョブの実際の開始時刻を予測しやすくなる

6. **残り concurrency 未設定 6 件に `cancel-in-progress: true` 追加 (2 件/バッチ)**

7. **`wbs-user-notify.yml` と `wbs-user-tasks-notify.yml` 統合**  
   1 run/日 × ~30 s 削減 + yaml 管理コスト削減

## 自動適用した変更 (本実行 2026-06-09)

| ファイル | 変更内容 | 効果 |
|---------|---------|------|
| `.github/workflows/blog-publish-orphan-cleanup.yml` | `concurrency: group: blog-publish-orphan-cleanup / cancel-in-progress: true` を追加 | 6h ごとの並行実行 → git push main 競合を防止 |
| `.github/workflows/stale-ef-completeness-check.yml` | `concurrency: group: ${{ github.workflow }}-${{ github.ref }} / cancel-in-progress: true` を追加 | push + PR + schedule の積み上がり防止 |

**合計削減見込み: エラー回避 + 並行実行抑制 (direct billable 削減は小さいが git 競合/再実行コストを防止)**

## 削除候補 (根拠付き・即削除しない)

| ファイル | 根拠 | リスク |
|---------|------|-------|
| `cron-batch.yml` | schedule なし、workflow_dispatch のみ | 低 |
| `wbs-user-tasks-notify.yml` | `wbs-user-notify.yml` と同一 cron・類似目的で統合可能 | 中 (統合後に削除) |
