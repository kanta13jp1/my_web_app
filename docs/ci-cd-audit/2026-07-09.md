# CI/CD コスト監査 2026-07-09

> 自動生成: Claude Schedule ci-cd-audit / 実行時刻 2026-07-09 14:xx UTC

---

## サマリ

| 指標 | 値 |
|------|----|
| 総ワークフロー数 | 111 本 |
| schedule (cron) ワークフロー | 71 本 |
| 推定 schedule 実行回数 | ~116 runs/日 / ~3,480 runs/月 |
| 今回新規検出 (cancel=false 残存) | 1 本 (`mcp-audit-anomaly-cron.yml`) |
| **本実行で自動適用** | **1 ファイル** |

---

## ワークフロー別コスト表 (高頻度 TOP13)

| ワークフロー | cron | runs/日 | runs/月 | cancel-in-progress | 備考 |
|------------|------|---------|---------|-------------------|------|
| cs-check | `7 */2 * * *` | 12 | 360 | true | ✅ 設定済 |
| health-monitor | `0 */2 * * *` | 12 | 360 | true | ✅ 設定済 |
| infra-health-check | `37 */2 * * *` | 12 | 360 | true | ✅ 設定済 |
| mcp-audit-anomaly-cron | `7 */2 * * *` | 12 | 360 | **→ PR条件付きtrue** | 🔧 本実行で適用 |
| memory-search-sync | `12 */2 * * *` | 12 | 360 | true | ✅ 設定済 |
| notion-sync | `10 */2 * * *` | 12 | 360 | false (rate limit) | ✅ 意図的 |
| x-post-metrics-optimizer | `17 */3 * * *` | 8 | 240 | true | ✅ 2026-07-06 削減済 |
| ai-university-update | `0 */4 * * *` | 6 | 180 | true | ✅ 設定済 |
| edge-function-audit | `47 */4 * * *` | 6 | 180 | true | ✅ 設定済 |
| feature-review | `0 */4 * * *` | 6 | 180 | true | ✅ 設定済 |
| horse-racing-update | `0 */4 * * *` | 6 | 180 | false (逐次) | ✅ 意図的 |
| schedule-resilience-watch | `22 */4 * * *` | 6 | 180 | false | ⚠️ 要確認 |
| wbs-ai-review | `20 */4 * * *` | 6 | 180 | true | ✅ 2026-07-04 削減済 |

---

## 検出した冗長フロー (A–G)

### A) 機能重複トリガー [med — 前回継続]
`health-monitor.yml` / `infra-health-check.yml` / `cs-check.yml` が
それぞれ 2h ごとに Firebase + Supabase EF を並行チェック。
推定重複: ~1,080 curl 呼び出し/月が重複実行。
→ 人手による統合判断が必要。前回 #3841 で提案済み。

### B) cancel-in-progress 設定不備 [high → 本実行で自動適用]
| ワークフロー | 変更内容 |
|------------|---------|
| `mcp-audit-anomaly-cron.yml` | `false` → `${{ github.event_name == 'pull_request' }}` |

前回レポート (#3841, 2026-07-08) で HIGH として未適用だった箇所を今回自動適用。
- schedule トリガー: cancel=false を維持 (セキュリティ監査ギャップ防止)
- PR トリガー: cancel=true (PR 連続 push 時のキュー積み防止)
- timeout 8-10 min × 2h 間隔 → 実運用上のキャンセル発生なし

### C) paths フィルタ [low — 問題なし]
push トリガーを持つ全ワークフローが `paths:` または `paths-ignore:` 設定済みを確認 ✅

### D) 過剰 cron 頻度 [low — 過去監査で対応済]
最短 2h 間隔が底。さらなる削減は A) の統合議論に含む。

### E) cache 未使用 [low — 新規候補なし]
`ci.yml` / `deploy-prod.yml` / `deploy-staging.yml` / `deploy-dev.yml` は全てキャッシュ設定済み ✅
Python cron ワークフローの多くは `pip install` なし → cache 効果なし。

### F) 高失敗率 [med]
| ワークフロー | 失敗率 | サンプル数 | コメント |
|------------|--------|----------|---------|
| mcp-audit-anomaly-cron | 50% | 2 | 小サンプル; SUPABASE_URL_PROD / SRK 疎通確認推奨 |
| CS Check | 50% | 2 | 小サンプル |
> ※ 直近 30 runs のみ取得できたため傾向の参考値。継続監視推奨。

### G) 廃止候補 [med — 人手判断のみ]
前回 #3841 から引き続き未着手:
- `cron-batch.yml` — schedule DISABLED、secrets 未設定で常時エラー → 削除 or secrets 設定の二択
- `issue-wbs-maintenance.yml` — "Legacy entry point" コメントあり → `issue-to-wbs.yml` 統合済み
- `hotfix-stripe-url.yml` / `fix-supabase-network.yml` — 一時 hotfix 系 → 残存ユースケース確認後に削除推奨

---

## 自動適用した変更 (本実行)

### 変更 1: `mcp-audit-anomaly-cron.yml` — cancel-in-progress 条件付き true

```diff
 concurrency:
   group: mcp-audit-anomaly
-  cancel-in-progress: false
+  cancel-in-progress: ${{ github.event_name == 'pull_request' }}
```

- **根拠**: 前回 #3841 提案 HIGH 未着手。schedule (2h 間隔) は false 維持 (セキュリティ監査ギャップ防止)、PR 発火時のみ true で旧ランをキャンセル。
- **推定効果**: PR 開発中の連続 push によるキュー積み防止。schedule での実運用キャンセル発生なし (timeout 10min × interval 2h)。

---

## 改善提案 (人手判断 — 継続)

### 🟡 MED: `schedule-resilience-watch.yml` — cancel-in-progress 確認
`cancel-in-progress: false`、4h 間隔、timeout 10min。`permissions: actions: write` で
他ワークフローを retry するため、意図的な false かもしれないが未コメント。
- 次回監査での確認対象として列挙。

### 🟡 MED: インフラヘルスチェック統合 (継続提案)
`health-monitor` + `infra-health-check` の curl チェックを一本化。
推定削減: ~360 runs/月 + checkout オーバーヘッド ~12 min/日。

### 🟡 MED: 廃止ワークフロー削除 (G)
`cron-batch.yml` / `issue-wbs-maintenance.yml` / `hotfix-stripe-url.yml` / `fix-supabase-network.yml`
削除確認後に Issue #3841 へ追記推奨。

---

## 累積削減実績 (過去監査 + 本日)

| 日付 | 内容 | 削減効果 |
|------|------|---------|
| 2026-06-10 | notion/memory/cs-check 毎時→2h | ~1,080 runs/月 |
| 2026-06-15 | health-monitor 毎時→2h | ~360 runs/月 |
| 2026-06-22 | ai-university 2h→4h | ~180 runs/月 |
| 2026-07-03 | issue-to-wbs 4h→6h | ~120 runs/月 |
| 2026-07-04 | wbs-ai-review 2h→4h | ~90 runs/月 |
| 2026-07-06 | claude-hygiene 8→4 runs/日, x-metrics 3h→6h | ~240 runs/月 |
| 2026-07-08 | roadmap-guard + mcp-isolation cancel-in-progress: true | PR キュー積み防止 |
| **2026-07-09** | **mcp-audit-anomaly-cron cancel PR-conditional true** | **PR キュー積み防止** |
