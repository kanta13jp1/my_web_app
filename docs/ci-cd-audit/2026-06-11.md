# CI/CD コスト監査 2026-06-11

> 自動生成 by CI/CD コスト最適化エージェント

## サマリ

| 項目 | 値 |
|------|-----|
| 総ワークフロー数 | 109 |
| 今回検出した冗長フロー | 3件 (A×1 / D×2) |
| 本実行で自動適用 | 2件 (horse-racing-update / memory-search-sync 頻度削減) |
| 推定削減効果 (schedule billable) | **~53 分/日 → 約 1,590 分/月** |

---

## ワークフロー別コスト表 (スケジュール系・高頻度上位)

| ワークフロー | cron 頻度 | 実行回数/月 | 平均時間 | 失敗率 | トリガー |
|-------------|-----------|------------|---------|--------|---------|
| `horse-racing-update.yml` | 毎時 → **毎2時間(本実行)** | 720→360 | ~4.2分 | 不明 | schedule |
| `memory-search-sync.yml` | 毎時 → **毎2時間(本実行)** | 720→360 | ~0.3分 | 0% | schedule |
| `health-monitor.yml` | 毎時 (06-06 audit済30min→1h) | 720 | ~0.4分 | 0% | schedule |
| `cs-check.yml` | 2時間毎 (06-10 audit済) | 360 | ~4.0分 | 0% | schedule |
| `notion-sync.yml` | 2時間毎 (06-10 audit済) | 360 | ~3.4分 | 0% | schedule |
| `ai-university-update.yml` | 2時間毎 | 360 | ~1.1分 | 0% | schedule |
| `tools-hub-mcp-smoke.yml` | 6時間毎 | 120 | ~0.4分 | 0% | schedule |
| `quota-monitor.yml` | 毎日 `0 0 * * *` | 30 | ~7.0分 | 0% | schedule |
| `codex-backlog-check.yml` | 毎日 `0 0 * * *` | 30 | ~1.5分 | 0% | schedule |
| `wbs-user-notify.yml` | 毎日 `0 0 * * *` | 30 | ~0.3分 | 0% | schedule |
| `wbs-user-tasks-notify.yml` | 毎日 `0 0 * * *` | 30 | ~0.3分 | 0% | schedule |

---

## 検出した冗長フロー

### [A] 優先度 HIGH — wbs-user-notify 重複呼び出し
- **`wbs-user-notify.yml`** と **`wbs-user-tasks-notify.yml`** が同時刻 (`0 0 * * *`) に同一の Supabase EF `tools-hub:wbs.notify_user_tasks` を呼び出している
- 実質同じ通知が2回 Slack に届く可能性 (limit だけ異なる: 20 vs 10)
- 推定削減: 1ワークフロー廃止で **30 runs/月 削減**
- **対応**: Issue 提案のみ（workflow 削除はホワイトリスト外）

### [D] 優先度 HIGH — horse-racing-update.yml 過剰頻度
- `0 * * * *` = 毎時24回/日。JRA の本レースは土日のみ → 平日は実益が薄い
- 毎時 → 毎2時間に変更: **12 runs/日削減 × ~4.2分 = ~50分/日削減**
- **対応**: ✅ 本実行で自動変更

### [D] 優先度 MED — memory-search-sync.yml 過剰頻度
- `12 * * * *` = 毎時。メモリ同期は即時性不要 → 2時間毎で十分
- 毎時 → 毎2時間: **12 runs/日削減 × ~0.3分 = ~3.6分/日削減** + queue 圧力軽減
- **対応**: ✅ 本実行で自動変更

---

## 改善提案 (Issue 追記分)

### 🔴 優先度 HIGH
1. **wbs-user-notify.yml 廃止** または **wbs-user-tasks-notify.yml に統合**
   - 2ワークフローが同一 EF を同時刻に呼ぶのは二重通知リスク
   - 推定削減: 30 runs/月 + 重複 Slack 通知解消

2. **minimal-e2e-gate.yml public-e2e-stability job の `paths-ignore` 追加**
   - docs/memory/md 変更だけの PR でも Playwright+Chromium (~3-4分) が走る
   - `paths-ignore` フィルタで非コード変更時はスキップ可能
   - 推定削減: PR 1件あたり最大 ~4分

### 🟡 優先度 MED
3. **midnight cron 集中 4件の分散** (quota-monitor / codex-backlog-check / wbs-user-notify / wbs-user-tasks-notify が全て `0 0 * * *`)
   - 5分ずつずらして GHA キュー遅延を防止
   - 推定効果: 起動遅延 ~1-2分/日 解消

4. **deploy-prod.yml の Flutter build に `actions/cache` 追加**
   - deploy-prod は `workflow_call` で ci.yml の Flutter pub cache を活用するが、deploy job 自身の flutter pub get にキャッシュが効いていない可能性
   - 精査して cache hit 確認要

---

## 自動適用した変更 (本実行 2026-06-11)

| ファイル | 変更内容 | 推定削減効果 |
|---------|---------|------------|
| `horse-racing-update.yml` | cron `0 * * * *` → `0 */2 * * *` (毎時→2時間毎) | ~50分/日 (~1,500分/月) |
| `memory-search-sync.yml` | cron `12 * * * *` → `12 */2 * * *` (毎時→2時間毎) | ~3.6分/日 (~108分/月) |

---

## 削除候補 (根拠付き・即削除しない)

| ワークフロー | 根拠 | リスク |
|------------|------|--------|
| `wbs-user-notify.yml` | `wbs-user-tasks-notify.yml` と機能重複 | 片方削除で Slack 通知が止まるリスク → 事前検証必須 |

---

## 累積削減サマリー (2026-06-04〜11)

| 適用日 | 内容 | 削減効果 |
|--------|------|---------| 
| 06-04 | ci.yml pub cache 追加 | ~2 min/PR |
| 06-04 | memory-search-sync.yml concurrency 追加 | エラー回避 |
| 06-06 | health-monitor.yml cron 30分→1時間 | ~720 runs/月削減 |
| 06-08 | feature-review.yml cron 毎時→毎4時間 | ~22.8 分/日 |
| 06-08 | edge-function-audit.yml cron 毎時→毎4時間 | ~9 分/日 |
| 06-09 | blog-publish-orphan-cleanup + stale-ef-completeness-check concurrency 追加 | git 競合エラー回避 |
| 06-10 | notion-sync.yml cron 毎時→2時間 | ~360 runs/月削減 |
| 06-10 | cs-check.yml cron 毎時→2時間 | ~360 runs/月削減 |
| **06-11** | **horse-racing-update.yml 毎時→2時間** | **~360 runs/月削減 (~1,500分/月)** |
| **06-11** | **memory-search-sync.yml 毎時→2時間** | **~360 runs/月削減 (~108分/月)** |

**累積削減 (schedule billable 時間推定): 開始時 ~280 分/日 → 現在 ~175 分/日 (-37.5%)**
