# CI/CD コスト監査 2026-06-08

> 生成: CI/CD コスト最適化エージェント (自動実行 2026-06-08 14:05 UTC)

## サマリ

| 項目 | 値 |
|------|----|
| 総ワークフロー数 | **108** (active) |
| うち hourly+ schedule | **13** ワークフロー |
| 直近 30 runs 中 100% failure | 2 ワークフロー |
| 推定 schedule billable 時間/日 | **≈ 280 分/日** |
| 本実行で自動削減 (見込み) | **≈ 32 分/日 (約 16 時間/月)** |

## TOP3 コスト源 (schedule 推定消費時間/日)

| ランク | ワークフロー | 頻度 | 平均実行時間 | 推定/日 |
|--------|-------------|------|------------|--------|
| 1 | Horse Racing Auto Update | 毎時 | 260 s | **104 分** |
| 2 | Notion Mirror Sync (hourly) | 毎時 | 196 s | **78 分** |
| 3 | Feature Review | 毎時 → **毎4時間** (本実行適用) | 76 s | 30 min → **7.6 分** |

## ワークフロー別コスト表 (直近 30 runs サンプル)

| ワークフロー名 | 頻度 | 平均時間 | 失敗率 | トリガー |
|--------------|------|---------|--------|--------|
| Horse Racing Auto Update | 毎時 | 260 s | 0% | schedule |
| Notion Mirror Sync | 毎時 | 196 s | 0% | schedule |
| CI Auto-Fix | workflow_run | 158 s | 0% | workflow_run |
| Minimal E2E Gate | PR | 114 s | **100%** | pull_request |
| CI | PR | 111 s | **100%** | pull_request |
| Feature Review | 毎時 | 76 s | 0% | schedule |
| AI大学コンテンツ更新 | 2 時間毎 | 74 s | 0% | schedule |
| Claude Session Hygiene Cron | 30 分毎 | 37 s | 0% | schedule |
| mcp-audit-anomaly-cron | 毎時 | 31 s | 0% | schedule |
| Infra Health Check | 毎時 | 30 s | 0% | schedule |
| Health Monitor | 30 分毎 | 27 s | 0% | schedule |
| Schedule Resilience Watch | 毎時 | 26 s | 0% | schedule |
| Blog Publish Orphan Cleanup | schedule | 24 s | 0% | schedule |
| tools-hub MCP Smoke | 6 時間毎 | 23 s | **100%** | schedule |
| AI Agent PR Review | PR | 22 s | 0% | pull_request |
| Two-Instance Routing Audit | PR | 12 s | 0% | pull_request |
| WBS AI Review | 毎時 | 11 s | 0% | schedule |

## 検出した冗長フロー (A-G)

### [HIGH-A] 100% failure ワークフロー — 修正 or 停止が急務

| ワークフロー | 失敗率 | 頻度 | 浪費 billable 時間 |
|------------|--------|------|-------------------|
| `tools-hub-mcp-smoke.yml` | **100%** | 6 時間毎 | ~4 runs/日 × 23 s = ~92 s/日 |
| `minimal-e2e-gate.yml` | **100%** | PR ごと | PRごとに 114 s 浪費 |

> ⚠️ `tools-hub MCP Smoke` は **前回 (2026-06-04) 監査から引き続き 100% failure**。早急な対応を推奨。

### [HIGH-D] 過剰 cron 頻度 — hourly で実益が薄いワークフロー

| ワークフロー | 現在 | 推奨 | 推定削減/日 |
|------------|------|------|------------|
| `feature-review.yml` | 毎時 | 毎4時間 | 18 runs × 76 s = **22.8 分** ✅ 本実行適用 |
| `edge-function-audit.yml` | 毎時 | 毎4時間 | 18 runs × 30 s = **9 分** ✅ 本実行適用 |
| `infra-health-check.yml` | 毎時 | 毎2時間 | 12 runs × 30 s = **6 分** (Issue提案) |
| `schedule-resilience-watch.yml` | 毎時 | 毎4時間 | 18 runs × 26 s = **7.8 分** (Issue提案) |
| `wbs-ai-review.yml` | 毎時 | 毎2時間 | 12 runs × 11 s = **2.2 分** (Issue提案) |
| `mcp-audit-anomaly-cron.yml` | 毎時 | 毎2時間 | 12 runs × 31 s = **6.2 分** (Issue提案) |

### [med-B] concurrency 未設定ワークフロー (13 件) — 新規検出

push/schedule トリガーを持ちながら `concurrency:` が全くない。並行実行が積み上がる可能性:

```
ai-bench-source-recheck.yml
ai-model-telemetry-review.yml
blog-publish-orphan-cleanup.yml
codex-backlog-check.yml
daily-report-freshness-monitor.yml
kg-indexer-nightly.yml
managed-mcp-policy.yml
notebooklm-session-ops-routes.yml
release-notes-data.yml
session-state-hooks.yml
stale-ef-completeness-check.yml
two-instance-audit.yml
wbs-staleness-audit.yml
```

### [med-C] paths-ignore 未設定 (push/PR トリガー持ち) — 7 件

docs のみ変更でもジョブが全実行される:

```
claude-agent-review.yml
feedback-issue-resolved.yml
github-issue-fix.yml
issue-to-wbs.yml
minimal-e2e-gate.yml
windows-companion-build.yml
workflow-failure-handler.yml
```

### [med-G] 重複/廃止候補

| ファイル | 根拠 |
|---------|------|
| `cron-batch.yml` | ファイル冒頭に `# DISABLED` コメント。secrets 未設定のため毎回エラー。schedule 無効・workflow_dispatch のみ |
| `wbs-user-notify.yml` + `wbs-user-tasks-notify.yml` | 同一 cron `0 0 * * *`、類似目的 (user task を Slack 通知)。統合候補 |
| `ai-tool-watch.yml` + `ai-tool-changelog-watch.yml` | 同種 AI ツール監視。名前が異なるが目的重複の可能性あり |

## 改善提案 (削減/統合/cache/concurrency)

### 優先度 HIGH

1. **`tools-hub-mcp-smoke.yml` schedule 停止** (前回監査 #3108 未対応)
   - 6h ごとに 100% failure が継続。`TOOLS_HUB_MCP_BEARER_TOKEN` secret の確認後、修正か schedule コメントアウト
   - 推定削減: ~4 runs/日 × 23 s = 1.5 分/日

2. **`minimal-e2e-gate.yml` Chrome セットアップ修正** (前回監査 #3108 未対応)
   - PR ごとに 100% failure (Chromium セットアップ問題)。`playwright install --with-deps chromium` の環境確認
   - 推定削減: PR ごとに 114 s の節約

3. **`setup-flutter-sdk` action に `actions/cache@v4` 追加** (前回監査 #3108 未対応)
   - 毎回 `git clone --depth 1 flutter/flutter.git` でGBレベルのダウンロードが発生
   - `${RUNNER_TEMP}/flutter-3.38.x` をキャッシュすることで 2-3 分/実行削減

### 優先度 med

4. **残り hourly ワークフローの頻度削減** (Issue提案)
   - `infra-health-check` 毎時 → 毎2時間、`schedule-resilience-watch` 毎時 → 毎4時間
   - `mcp-audit-anomaly-cron` 毎時 → 毎2時間、`wbs-ai-review` 毎時 → 毎2時間
   - 合計推定削減: ~22 分/日

5. **concurrency 未設定 13 件に `cancel-in-progress: true` 追加** (Issue提案)
   - deploy 系以外で並行積み上がり防止

6. **`wbs-user-notify.yml` と `wbs-user-tasks-notify.yml` 統合** (Issue提案)
   - 同一 cron で類似目的。片方廃止で 1 run/日削減

7. **`cron-batch.yml` 削除** (Issue提案)
   - `# DISABLED` コメント済み。workflow_dispatch のみ。不要なら完全削除推奨

### 優先度 low

8. **paths-ignore 未設定 7 件への追加** (Issue提案)
   - docs/**、memory/**、*.md 変更で不要ジョブが走るのを防ぐ

## 自動適用した変更 (本実行で main へ入れた分)

### PR: `ci/auto-optimize-20260608-1405`

| ファイル | 変更内容 | 推定削減 |
|---------|---------|----------|
| `.github/workflows/feature-review.yml` | `cron: "0 * * * *"` → `"0 */4 * * *"` (毎時→毎4時間) | 18 runs/日 × 76 s = **22.8 分/日** |
| `.github/workflows/edge-function-audit.yml` | `cron: "47 * * * *"` → `"47 */4 * * *"` (毎時→毎4時間) | 18 runs/日 × 30 s = **9 分/日** |

**合計削減見込み: ≈ 32 分/日 ≈ 960 分/月 (≈ 16 時間/月)**

## 削除候補 (根拠付き・即削除しない)

| ファイル | 根拠 | リスク |
|---------|------|-------|
| `cron-batch.yml` | `# DISABLED` コメント、secrets 未設定エラー継続、schedule 無効 | 低 (workflow_dispatch のみなので削除しても影響なし) |
| `ai-tool-watch.yml` | `ai-tool-changelog-watch.yml` と目的重複の可能性 | 要確認 |

## 前回監査 (2026-06-04 〜 2026-06-07) からの差分

| 前回提案 | 状態 |
|---------|------|
| tools-hub MCP Smoke 修正 | ❌ 未対応 (今回も 100% failure) |
| setup-flutter-sdk cache 追加 | ❌ 未対応 |
| Minimal E2E Gate Chrome 修正 | ❌ 未対応 |
| WBS user notify 統合 | ❌ 未対応 |
| 毎時 schedule 削減 (feature-review / edge-function-audit) | ✅ **本実行で適用** |
| ci.yml pub cache 追加 | ✅ 2026-06-04 適用済み |
| memory-search-sync concurrency 追加 | ✅ 2026-06-04 適用済み |

### 新規検出 (2026-06-08)
- concurrency 未設定ワークフロー **13 件** を新規検出
- paths-ignore 未設定で push/PR トリガー持ち **7 件** を再確認
- `ai-tool-watch.yml` と `ai-tool-changelog-watch.yml` の重複疑いを確認
