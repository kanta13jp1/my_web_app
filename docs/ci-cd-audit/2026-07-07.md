# CI/CD コスト監査 2026-07-07

<!-- generated-by: github-actions (ci-cd-audit) -->

## サマリ

| 指標 | 値 |
|-----|---|
| 総ワークフロー数 | 114 (110 yml ファイル) |
| 推定 cron billable 時間/月 | ~200 h (ubuntu 換算) |
| TOP コスト源 | health-monitor + infra-health-check (720 runs/月, 各 ~5min) |
| 今回自動適用 | 2 ファイル |
| 今回削減推定 | ~180 runs/月・~900 ubuntu-min/月 |

> 累積削減実績 (過去監査):
> 2026-06-10: notion/memory/cs-check 毎時→2h (-1,080 runs/月) /
> 2026-06-15: health-monitor 毎時→2h (-360 runs/月) /
> 2026-06-22: ai-university 2h→4h (-180 runs/月) /
> 2026-07-03: issue-to-wbs 4h→6h (-690 min/月) /
> 2026-07-04: wbs-ai-review 2h→4h (-540 min/月) /
> 2026-07-06: claude-hygiene + x-metrics (-528 ubuntu換算min/月) /
> **2026-07-07: mcp-audit-anomaly cancel-in-progress + infra-health-check 2h→4h (-900 min/月)**

---

## ワークフロー別コスト表 (主要 cron)

| ワークフロー | 頻度 | 推定実行時間 | 月間 runs | 推定 min/月 | トリガー |
|------------|------|------------|----------|------------|--------|
| health-monitor | 2h | ~5 min | 360 | ~1,800 | schedule |
| infra-health-check | 2h (→4h 適用) | ~5 min | 360→180 | ~1,800→900 | schedule |
| ai-university-update | 4h | ~15 min | 180 | ~2,700 | schedule |
| cs-check | 2h | ~10 min | 360 | ~3,600 | schedule |
| mcp-audit-anomaly-cron | 2h | ~18 min | 360 | ~6,480 | schedule+PR |
| competitor-monitoring | daily | ~20 min | 30 | ~600 | schedule |
| daily-report | daily | ~15 min | 30 | ~450 | schedule |
| wiki-compile-cron | daily | ~10 min | 30 | ~300 | schedule |
| quota-monitor | daily | ~10 min | 30 | ~300 | schedule |
| ai-tool-watch | daily | ~10 min | 30 | ~300 | schedule |
| e2e-smoke | weekly Mon | ~10 min | 4 | ~40 | schedule |
| knowledge-vault-lint | weekly Mon | ~10 min | 4 | ~40 | schedule |
| docs-rotate-cron | monthly | ~10 min | 1 | ~20 (windows=2x) | schedule |
| deploy-prod | push/main | ~45 min | 可変 | 可変 | push |
| CI | PR/push | ~20 min | 可変 | 可変 | PR+push |

---

## 検出した冗長フロー

### A) 重複トリガー (優先度: med)
- **health-monitor** (2h) と **infra-health-check** (2h) が共に Firebase + Supabase EF を監視。
  - health-monitor: Firebase + get-home-dashboard / guitar-recording-studio / health-check / チケット → docs/health-status.json
  - infra-health-check: Firebase + schedule-hub / tools-hub / admin-hub / guitar-recording-studio / get-home-dashboard / health-check → docs/infra-status.json
  - 両者合計 720 runs/月。Firebase + 3 EF が完全重複。
  - **今回対応**: infra-health-check を 2h→4h に削減 (health-monitor の 2h 監視で基本カバー維持)

### B) cancel-in-progress 未設定問題 (優先度: high — 前回監査未対応)
- **mcp-audit-anomaly-cron**: `cancel-in-progress: false` のまま 2h 間隔 (timeout 18min)。
  - GitHub Actions の実行キュー遅延時に runs が積み上がり、同じ anomaly 検出が 2〜3 回連続実行される。
  - **今回対応**: `cancel-in-progress: true` に変更 (最新状態のみ評価すれば十分)

### C) paths フィルタ関連 (優先度: low)
- push 系ワークフロー (CI / deploy-prod / deploy-dev / deploy-staging) はすべて `paths-ignore` 設定済 ✅
- cron 系は性質上不要

### D) 過剰 cron 頻度 (優先度: med)
- **infra-health-check**: `37 */2 * * *` (360 runs/月) → **今回 `37 */4 * * *` に削減** (180 runs/月削減)
- **残検討**: docs-rotate-cron / worktree-cleanup-cron が `windows-latest` 使用。Windows は 2x billing。月 1〜4 run なので影響小だが ubuntu 移行で 50% 削減可能 (Issue 提案)

### E) cache 未使用 (優先度: low)
- cron 系 Python 使用ワークフロー (`ai-tool-watch` / `wiki-compile-cron` / `session-residuals-sync` / `knowledge-vault-lint`) は `cache: 'pip'` 未設定だが、追加 pip install を行わないため実質影響なし
- Flutter / npm / pub の主要 cache はすべて設定済 ✅

### F) 実質無効ワークフロー (優先度: med)
- **cron-batch.yml**: ファイル冒頭に `DISABLED: batch_analysis.py が毎回エラー` コメント。schedule 削除済で `workflow_dispatch` のみ残存。実質デッドコード。

### G) 廃止候補 (優先度: med — 人手判断)
1. **cron-batch.yml**: DISABLED コメント付き。secrets 設定か削除の判断が必要 (前回からキャリーオーバー)
2. **docs-rotate-cron.yml** + **worktree-cleanup-cron.yml**: `windows-latest` 使用 (2x billing)。`ubuntu-latest` への変更で月 10〜40 min 削減可能。
3. **infra-health-check.yml** 長期的統合: health-monitor との統合で checkout オーバーヘッドを削減可能 (破壊的変更のため Issue 提案のみ)
4. **hotfix-stripe-url.yml** / **fix-supabase-network.yml**: hotfix 系の一時ワークフロー。恒久運用不要なら削除推奨 (前回からキャリーオーバー)

---

## 改善提案 (Issue 提案のみ)

| # | 優先度 | 対象 | 提案 | 推定削減効果 |
|---|--------|------|------|------------|
| 1 | med | `docs-rotate-cron.yml` | windows-latest → ubuntu-latest | ~10 min/月 (windows 2x billing 解消) |
| 2 | med | `worktree-cleanup-cron.yml` | windows-latest → ubuntu-latest | ~20 min/月 |
| 3 | med | `cron-batch.yml` | DISABLED → schedule 削除 or 完全廃止 | active issue 解消 |
| 4 | low | `health-monitor` + `infra-health-check` | 長期的に統合 1 ワークフロー化 | ~900 min/月 |
| 5 | low | `hotfix-stripe-url.yml` / `fix-supabase-network.yml` | 完了確認後に削除 | 管理コスト削減 |

---

## 自動適用した変更 (本実行)

### 1. `mcp-audit-anomaly-cron.yml` — cancel-in-progress: true 追加

```yaml
# 変更前
concurrency:
  group: mcp-audit-anomaly
  cancel-in-progress: false

# 変更後
concurrency:
  group: mcp-audit-anomaly
  cancel-in-progress: true  # 2026-07-07 ci-audit: 2h cron キュー積み防止
```

**推定削減効果**: キュー積み時の重複実行防止。anomaly 検出の信頼性向上。

### 2. `infra-health-check.yml` — cron 2h → 4h

```yaml
# 変更前
  - cron: "37 */2 * * *"  # 2時間ごと37分

# 変更後
  - cron: "37 */4 * * *"  # 4時間ごと37分 (2026-07-07 ci-audit)
```

**推定削減効果**: 180 runs/月削減 × ~5 min = **~900 min/月 (15h/月)** 削減  
**安全性根拠**: health-monitor が既に 2h 間隔で Firebase + 3 EF を監視。infra-status.json の WEB版フォールバック用途は 4h 更新で十分。

---

## 削除候補 (根拠付き・即削除しない)

| ワークフロー | 廃止理由 | 確認事項 |
|------------|---------|--------|
| `cron-batch.yml` | DISABLED コメント / schedule 削除済 / secrets 未設定 | batch_analysis.py の今後の使用予定確認 |
| `hotfix-stripe-url.yml` | hotfix 一時対応ワークフロー | Stripe URL hotfix が完了・恒久対応済か確認 |
| `fix-supabase-network.yml` | Network Restrictions 対応一時ワークフロー | 設定恒久化済か確認 |
| `issue-wbs-maintenance.yml` | "Legacy entry point" コメント / issue-to-wbs.yml に統合済 | 利用者がいないか確認後に削除 |
