# 「自分株式会社」SEO根本原因仮説台帳

対象: <https://my-web-app-b67f4.web.app/>

この台帳は、順位変動だけで原因を断定せず、観測済みの事実と推論を分離して探索範囲を段階的に狭めるための記録です。信頼度は現時点の相対評価であり、順位やインデックスを保証するものではありません。

## 現在の症状

- 最終週次計測: 2026-09-02 20:13 JST
- `自分株式会社`: 自然検索96件まで対象ドメイン未検出（順位は推測せず「確認範囲外」）
- Googleのホームページタイトル・スニペット: 旧title/snippetのまま。2026-09-02本番HTMLには旧文言が存在せず、新titleへの更新は未確認
- `/philosophy`: 2026-09-02も`site:`検索で未検出
- Search Console: 対象プロパティ未登録のため、最終クロール日時とインデックス採否理由は未確認

## 原因仮説

| ID | 原因層 | 仮説 | 根拠 | 反証・弱点 | 信頼度 | 直近の検証／実装 | 観測期間と成功指標 | 次の反証確認 |
|---|---|---|---|---|---|---|---|---|
| H1 | 発見・クロール／インデックス | Googleが新しいホームHTMLと`/philosophy`をまだ再取得・選別していない | Google表示は旧title/snippetのまま。`/philosophy`は公開直後から`site:`未検出 | Search Consoleのクロール記録がなく、「未クロール」と「クロール後に不採用」を区別できない | 中 | HTTP 200、index/follow、自己canonical、robots許可、sitemap掲載、ホーム内部リンクを確認 | 次回週次計測まで。新title/snippetまたは`/philosophy`検出で強く支持 | Search Console URL検査を利用できる場合、クロール日時とインデックス理由を確認。再クロール済みで不採用ならH1を弱める |
| H2 | sitemap・発見シグナル | 全URLの`lastmod`をデプロイ日に更新する実装が、ページ固有の更新日シグナルを不正確にしている | 生成器は全URLへ同じ日付を設定。本番sitemap 47 URLも全件同日。理念ページ本番反映後にmainは21コミット進んだが理念関連変更は0件 | sitemapは発見のヒントであり、これ単独で圏外や未インデックスを説明できない | 高（実装欠陥）／低〜中（順位影響） | 2026-08-20: 明示的な`date_modified`があるURLだけ`lastmod`を出力するよう修正 | 1〜2回の無関係デプロイ後も`/philosophy`の`lastmod=2026-08-23`が維持され、未確認URLに日付が生成されない | 2026-09-02の別デプロイ後も`/philosophy`の`lastmod=2026-08-23`が維持され、未確認URLに一律日付が生成されていないことを確認済み |
| H3 | 検索意図・関連性 | 以前は概念検索の情報量が不足していたが、現在は主要意図を満たしており、まず再クロール観測が必要 | 定義、P/L・B/S、6部署、人的資本、30日サイクル、架空の棚卸し例、FAQを独自表現で公開済み | Googleが取得後にどのクエリへ関連付けるかは未確認。一次体験や著者性は競合より弱い | 中 | 2026-08-19に追加内容を本番配信。短期間の再改稿は行わない | 2〜4週間。`/philosophy`の検出、表示クエリ、title/snippet採用を観測 | 再クロール済みでも検索表示されない場合、重複・有用性・著者性を再評価する |
| H4 | 信頼性・エンティティ | 運営主体・著者・一次経験・外部評価が弱く、構造化データで示す主体を利用者が確認できる導線も不足していた | 上位には著者プロフィール、媒体評価、書籍、個人実践、法人導入実績がある。publisher IDの分断は修正済みだが、`sameAs`のGitHubプロフィールは理念ページの初期HTMLにもFlutter起動後画面にも可視リンクがなかった | 公開プロフィールへの導線だけで著者性や外部評価は増えず、圏外を単独で説明できない。架空の実績は追加できない | 中（競合差）／高（可視導線の欠落） | 2026-08-23: 公開GitHub APIとリポジトリ所有を確認し、理念ページの静的HTMLとFlutter画面の両方に同じ公開プロフィールへの可視リンクを追加 | 次回本番配信後にraw HTMLとFlutter起動後画面の双方でリンクを確認。順位影響はH1の判定後に評価 | Search Consoleで再クロール済みでも未表示なら、実在する運営者専用ページ・一次データ・引用可能な公開実績の不足を優先監査する |
| H5 | 外部評価 | 新しいFirebaseサブドメインへの外部言及・参照が少なく、競合ドメインより評価蓄積が弱い | 1ページ目はnote、専門家、Amazon、Diamond、R25など既存媒体中心 | バックリンク／参照ドメインを未計測。推測のみで施策化できない | 低〜中 | 未実装 | Search Consoleまたは信頼できる参照データ取得後 | 自然な言及・共有・引用につながる独自テンプレートや一次データの候補を調査する |

## 2026-08-20に一段深く分かったこと

前回は「明確なクロール遮断がなく、再クロール待ちが最も整合的」と判断した。今回は発見シグナルの生成過程まで追い、sitemapの`lastmod`がページの更新日ではなくデプロイ日で一律更新される実装を特定した。これはGoogleのインデックス採否を直接説明する証拠ではないが、Googleが利用条件として示す「継続的かつ検証可能な正確さ」を満たさないため、独立して修正すべき技術SEO欠陥である。

一方、最大の未確定要因はSearch Consoleデータがなく、「まだ再クロールされていない」のか「再クロール済みだがインデックスに選ばれていない」のかを区別できない点である。したがって、この回では本文を再度増やさず、更新日シグナルの精度だけを改善する。

## 2026-08-22にさらに一段深く分かったこと

前回は発見シグナルの正確さを修正した。今回は本文を再改稿せず、上位ページで差が残っていた著者・運営主体の信頼性を、まず構造化データの接続関係まで追跡した。

ホームHTMLには同名のOrganizationが二つの形で存在していた。`WebSite.publisher`と理念ページの`WebPage.publisher`はID付きの`#organization`を参照する一方、`SoftwareApplication.publisher`だけはIDのない匿名ノードをインライン定義していた。このため、サイト・理念・アプリを公開する主体が同一であることを一つのグラフとして明示できていなかった。Googleは構造化データをページ内容とエンティティの理解に利用し、Organization情報は主体の曖昧さ解消に使うと説明しているため、独立した実装不整合として修正する。

ただし、これは著者プロフィール、一次経験、第三者評価を新しく生む施策ではなく、順位への寄与は未確定である。実在確認できない人物・法人・成果を追加すると信頼性を逆に損なうため、今回は既存のOrganization IDへpublisher参照を統合するだけに留める。これによりH4の「実装上のエンティティ分断」は強く支持されて修正対象となったが、「外部から確認できる著者性・評価の不足」は未解決のまま残る。

### 今回の評価方法

- 成功: ホームと生成後の`/philosophy`で、`SoftwareApplication.publisher`、`WebSite.publisher`、`WebPage.publisher`がすべて`https://my-web-app-b67f4.web.app/#organization`を参照し、そのIDを持つOrganization定義が一つ存在する。
- 失敗の解釈: 統合後も再クロール済みページが検索表示されなければ、この技術的不整合の単独原因説を弱め、H1のインデックス採否とH4の実在する著者・一次情報を優先する。
- 観測期間: 次回の本番配信後に生成HTMLを即時確認し、順位・スニペットは次回週次計測以降に他の変更と分離して観測する。
- ロールバック: `SoftwareApplication.publisher`を以前のインラインOrganizationへ戻す。本文、UX、canonical、サイトマップには影響しない。

## 2026-08-23にさらに一段深く分かったこと

前回はJSON-LD内のpublisher参照を一つのOrganization IDへ統合した。今回はその主体情報が利用者にも確認できるかを、配信HTMLとFlutter起動後の画面に分けて直接観測した。

本番`/philosophy`の初期HTMLには理念本文、編集方針、Organizationの`sameAs`が存在し、Flutter起動後は静的SEO shellが削除されても編集方針と開発実績への内部導線は画面に残った。したがって「理念本文がFlutter起動時に全面消失する」という仮説は弱まった。ただし`sameAs`にあるGitHub/Xプロフィールは、初期HTMLの可視リンクにもFlutter起動後画面にも存在せず、構造化データが示す主体を利用者が外部で確かめる導線がなかった。

Google Search Centralは、構造化データが可視内容を代表し、読者に見えない内容をマークアップしないよう求めている。GitHub公式APIでは`kanta13jp1`が公開Userであり、公開リポジトリ`kanta13jp1/my_web_app`の所有者であることを確認できた。この検証済み情報に限り、`/philosophy`の静的関連リンクとFlutter編集方針へ同じGitHubプロフィール導線を追加する。氏名、法人格、資格、成果、利用者評価は推測・追加しない。

### 今回の評価方法

- 成功: 生成後`/philosophy`のraw HTMLとFlutter起動後画面の双方に、`https://github.com/kanta13jp1`への「GitHub @kanta13jp1」リンクが存在する。
- 反証: 可視導線を追加して再クロールされた後も未表示なら、「JSON-LDと可視主体の不一致」が単独原因である可能性を弱める。
- 観測期間: 本番配信後にraw HTMLとブラウザ描画を即時確認し、検索表示は次回週次計測以降に他の変更と分離して観測する。
- ロールバック: 公開ルート設定とFlutter編集方針からGitHubリンクを削除し、理念ページの`date_modified`を直前の正確な更新日に戻す。canonical、本文の主張、DBには影響しない。

## 変更の評価方法

- 成功: sitemap生成物に現行のcanonical URL一覧（2026-08-22時点で`/shop`を含む48 URL）が残り、`date_modified`を明示した`/philosophy`だけが正確な最終更新日（現時点では`2026-08-23`）を持つ。
- 継続確認: 無関係な本番デプロイ後も理念ページの`lastmod`が変わらない。
- 失敗の解釈: sitemapが正確になっても未検出が続く場合、H2単独原因説は弱まり、H1のクロール実績とH3/H4のインデックス品質を優先して検証する。
- ロールバック: `scripts/generate_sitemap.py`の明示日付マッピングと生成済み`web/sitemap.xml`を直前実装へ戻す。URL一覧やページ本文には影響しない。
## 2026-09-02にさらに一段深く分かったこと

前回までに、検索意図を満たす本文、正確なsitemap更新日、統合されたpublisher、利用者が確認できるGitHub導線を順に整備した。今回の週次実測でも対象は自然検索96件まで未検出で、`/philosophy`は`site:`検索に現れなかった。一方、9月2日の別デプロイ後も`lastmod=2026-08-23`が維持されたため、H2の実装欠陥は修正済みであり、H2単独原因説はさらに弱まった。

ホームのGoogle表示title/snippetは旧内容のままだが、その文言は現在の本番HTMLに存在しない。これは未再取得とGoogle独自のtitle書き換えのどちらでも起こり得る。Search Consoleは対象プロパティ未登録で、最終クロール日時とインデックス不採用理由を取得できないため、H1の「未クロール」と「クロール後に未採用」は今回も区別できない。

さらに、`/philosophy`の初期HTMLにはH1と通常の内部リンクがある一方、Flutter起動後の可視本文ではDOM上の`h1`と`a[href]`を確認できなかった。本文とリンクラベルは残るため「内容が全面消失する」仮説は支持されないが、レンダリング後の見出し・リンク意味論が弱い可能性をH6として分離する。初期HTMLは正常で、Googleのレンダリング結果も未取得のため、現時点では原因と断定しない。

### 追加仮説

| ID | 原因層 | 仮説 | 根拠 | 反証・弱点 | 信頼度 | 直近の検証／実装 | 観測期間と成功指標 | 次の反証確認 |
|---|---|---|---|---|---|---|---|---|
| H6 | metadata・構造化データ／レンダリング | Flutter起動後にネイティブなH1とアンカーが消えるため、Googleのレンダリング後ページ理解・リンク評価が初期HTMLより弱くなる可能性がある | 初期HTMLにはH1と通常の`a[href]`があるが、起動後DOMでは理念本文が可視のまま`h1`と`a[href]`が0件 | Googleは初期HTMLも処理でき、sitemapとホームの初期リンクで発見可能。Search Consoleのレンダリング証拠がなく順位原因とは未確定 | 低〜中 | 2026-09-02: 初期HTMLと起動後DOMを分離観測。コード変更なし | 次回週次まで。Googleの新title採用または`/philosophy`検出で重大度を下げる | Search Console URL検査のライブテスト／取得HTMLで見出し・リンク認識を確認。再取得後も未採用なら、意味論を保つ最小実装を別PRで評価する |

### 仮説の強弱更新

- H1: **中から中〜高へ**。旧Google表示と理念ページ未検出が2週間超継続。ただしSearch Console証拠がなく、未再取得か未採用かは未分離
- H2: **実装欠陥は解消、順位原因として低へ**。別デプロイ後も正確な`lastmod`を維持
- H3: **内容量不足説は弱まる**。対象は上位ページの主要トピックを独自表現で既に網羅
- H4: **観測継続**。可視GitHub導線は8月30日マージ後まだ短く、順位寄与を判定しない
- H5: **未計測のまま**。自然な外部言及・第三者評価の差は残るが、データなしにリンク施策へ進まない
- H6: **新規・低〜中**。まずSearch Consoleのレンダリング証拠で反証し、同一ページの再改稿を避ける
