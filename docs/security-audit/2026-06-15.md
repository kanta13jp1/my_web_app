# 依存パッケージ脆弱性チェック 2026-06-15

> 自動週次タスク `scheduled-tasks/dependency-audit` 実行ログ。前回: 2026-05-25 (~3週前)。

## サマリ

| 項目 | 結果 |
|---|---|
| Flutter advisory-affected (`isCurrentAffectedByAdvisory`) | **0** ✅ |
| Flutter discontinued (`isDiscontinued`) | **0** ✅ |
| Flutter retracted (`isCurrentRetracted`) | **0** ✅ |
| Direct/Dev 要 upgrade (in-range / `upgradable>current`) | 1 件 (`sentry_flutter` minor) |
| Direct/Dev 要 upgrade (major / 制約編集要) | 6 件 |
| Deno EF `std@0.168.0` ソース import 残存 | **0** ✅ |
| Deno EF `std@0.177.0` ソース import 残存 | **0** ✅ |
| Deno EF `std@0.224.0` 使用 | 46 件 (統一済 / 前回 41 → 46) |
| Deno EF `jose@v5.9.6` CVE | **0** ✅ (latest は v6.2.3 = major) |

**今回の自動アップグレード**: **適用なし**。理由は下記 (タスク規定「patch のみ自動更新」に該当する upgrade が無いため)。

---

## Flutter/Dart パッケージ

`flutter pub outdated --json` で全 57 パッケージを確認。

### Advisory / Discontinued / Retracted
- `isCurrentAffectedByAdvisory: true` → **0 件** ✅
- `isDiscontinued: true` → **0 件** ✅
- `isCurrentRetracted: true` → **0 件** ✅

→ **既知脆弱性 advisory に該当する依存は無し。緊急対応不要。**

### Direct/Dev 要 upgrade 一覧

| パッケージ | kind | 現在 | upgradable (制約内) | resolvable | latest | 種類 | 判定 |
|---|---|---|---|---|---|---|---|
| `sentry_flutter` | direct | 9.21.0 | **9.22.0** | 9.22.0 | 9.22.0 | minor (制約内) | 🟡 後述 |
| `flutter_local_notifications` | direct | 21.0.0 | 21.0.0 | 22.0.1 | 22.0.1 | **major** | 見送り |
| `mime` | direct | 1.0.6 | 1.0.6 | 2.0.0 | 2.0.0 | **major** | 見送り |
| `package_info_plus` | direct | 8.3.1 | 8.3.1 | 10.1.0 | 10.1.0 | **major** (8→10) | 見送り |
| `share_plus` | direct | 12.0.2 | 12.0.2 | 13.1.0 | 13.1.0 | **major** | 見送り |
| `latlong2` | direct | 0.9.1 | 0.9.1 | 0.9.1 | 0.10.1 | **0.x breaking** | 見送り |
| `flutter_lints` | dev | 3.0.2 | 3.0.2 | 6.0.0 | 6.0.0 | **major** (3→6) | 見送り |
| `mockito` | dev | 5.6.4 | 5.6.4 | 5.6.4 | 5.7.0 | minor (blocked) | 適用不可 |
| `test` | dev | 1.30.0 | 1.30.0 | 1.30.0 | 1.31.1 | minor (blocked) | 適用不可 |
| `flutter_native_splash` | dev | 2.4.7 | 2.4.7 | 2.4.7 | 2.4.8 | patch (blocked) | 適用不可 |

**自動アップグレードを適用しなかった理由**:

- タスク規定は「**patch バージョンアップなら自動更新してコミット / major はレポート**」。
- `upgradable > current` (= pubspec.yaml 制約を変えずに `pub upgrade` で取り込める in-range upgrade) は **`sentry_flutter` 9.21.0 → 9.22.0 のみ**。ただしこれは **minor** であり「patch」ではない。観測基盤 (Sentry) の minor は挙動変更を伴いうるため、無人実行での自動適用は見送り (= [NO-SCOPE-CREEP] / 観測系は CI gate 通過を確認してから着地)。
- `flutter_native_splash` は唯一の patch (2.4.7 → 2.4.8) だが、依存グラフ制約により `resolvable` が 2.4.7 のまま = **そもそも upgrade 不可** (transitive 制約ブロック)。
- `mockito` / `test` の minor も同様に `resolvable` が現行のまま (Flutter SDK / transitive ブロック) = 適用不可。
- major 6 件 (`flutter_local_notifications` / `mime` / `package_info_plus` / `share_plus` / `latlong2` / `flutter_lints`) は破壊的変更ありうるため、pubspec.yaml 制約編集 + 個別検証が必要 = 本 audit では実施せず、下記「次回推奨」へ。

---

## Supabase Edge Functions (Deno)

### `deno.land/std` 移行状況

| バージョン | 前回 (2026-05-25) | 今回 (2026-06-15) | 状態 |
|---|---|---|---|
| `std@0.168.0` (ソース import) | 0 | **0** | ✅ 維持 |
| `std@0.177.0` (ソース import) | 0 | **0** | ✅ 維持 |
| `std@0.224.0` (ソース import) | 41 | **46** | ✅ 統一済 (新規 EF 5 件も同版) |

`.ts` ソースからの直接 import は全 46 ファイルが `std@0.224.0` に統一済。新規追加された Edge Function / テストも同版を踏襲しており drift なし。

> ⚠️ **軽微 (非セキュリティ)**: `supabase/functions/deno.lock` には `std@0.168.0` のハッシュエントリが **11 件** 残存。これは過去ロックの履歴で、ソース import は 0 件のため実害なし。lockfile 再生成 (`deno cache --reload` 系) で除去可能だが優先度低。

### npm: / jsr: / x: 依存

| 依存 | バージョン | 使用箇所 | 状態 |
|---|---|---|---|
| `npm:@supabase/supabase-js@2` | メジャー固定 (`@2` = v2 内自動最新) | 25 ファイル | ✅ |
| `https://deno.land/x/jose@v5.9.6` | v5 系 pin | `_shared/mcp_auth_guard.ts` 1 箇所 | 🟡 監視 (下記) |

- `esm.sh` / `jsr:` / `cdn.skypack` / `unpkg` 由来の import は **0 件** (供給元は deno.land/std・npm:・deno.land/x のみ = 攻撃面が小さい)。

#### `jose@v5.9.6` 評価 (前回 audit の繰越監視項目)

- **CVE / advisory: なし** ✅ (npm `jose` パッケージに 2026-06 時点で既知の脆弱性 advisory なし)。
- 最新版は **v6.2.3** (= major v6 系)。現行 v5.9.6 は v5 系の安定版で、脆弱性起因の緊急アップグレード理由は無し。
- `jose` は WorkOS JWKS による JWT 検証本体 (= MCP 認可基盤 / `MCP_AUTH_SECURITY_PRINCIPLES.md` 原則 #2/#5/#7/#8 の中核)。v6 への major upgrade は API 破壊変更を伴うため、認可フロー回帰テスト込みで別途評価 (= 緊急性なし・計画的に実施)。

---

## アクション

### ✅ 完了 (今回 audit)
- `flutter pub outdated --json` で全 57 パッケージの advisory / discontinued / retracted / direct・dev upgrade を一括確認 → 脆弱性 0。
- Deno EF 全 `.ts` の `deno.land/std` / `npm:` / `deno.land/x` import 版数 verify (源泉 import の std 統一を再確認)。
- `jose@v5.9.6` の CVE / 最新版チェック (前回繰越監視項目をクローズ = 脆弱性なし確認)。

### 📋 次回推奨 (Codex #1 もしくは計画的 major upgrade lane)

- [ ] **Codex #1 / Win Claude #1**: `sentry_flutter` 9.21.0 → 9.22.0 (in-range minor / CI gate 通過確認の上で着地 / 観測基盤のため smoke 推奨)。
- [ ] **計画的 major lane**: 以下 direct major を個別 PR で破壊的変更確認の上 upgrade —
  - `package_info_plus` 8 → 10 (2 メジャー遅延 / 優先度中)
  - `share_plus` 12 → 13
  - `flutter_local_notifications` 21 → 22
  - `mime` 1 → 2
  - `latlong2` 0.9 → 0.10 (flutter_map と整合確認)
- [ ] **dev major**: `flutter_lints` 3 → 6 (lint rule 大量追加の可能性 / 別 PR で analyze 差分確認)。
- [ ] **保留**: `mockito` 5.7.0 / `test` 1.31.1 / `flutter_native_splash` 2.4.8 は transitive 制約で `resolvable` がブロック中 = Flutter SDK / 上位依存の更新後に再評価。
- [ ] **軽微 housekeeping**: `supabase/functions/deno.lock` の `std@0.168.0` 残存ハッシュ 11 件を lockfile 再生成で除去 (非セキュリティ / 優先度低)。
- [ ] **継続監視**: `jose` v5.9.6 → v6 major (認可回帰テスト込み / 緊急性なし)。

### 🟢 残課題 (前回 audit からの繰越状況)

| 前回 (5/25) 宿題 | 状況 |
|---|---|
| `flutter_secure_storage` 10.2.0 → 10.3.0 patch | ✅ **解消** (今回 outdated 一覧から消失 = 既に解決済 / direct upgrade 不要) |
| `test` (dev) minor upgrade | ⏳ 1.30.0 へ前進したが latest 1.31.1 は依然 transitive ブロック = 引き続き保留 |
| `jose@v5.9.6` 最新版チェック | ✅ **完了** (今回: CVE 0 / v6 major は計画的に) |
| `flutter_lints` 6.x major | ⏳ 未着手 (3.0.2 / 警告なし / 優先度低 = 別 PR) |

---

## 環境メタ

- Flutter / Dart toolchain: `flutter pub outdated --json` 正常実行 (exit 0 / 57 パッケージ解決)。
- Branch: `auto/dependency-audit-20260615` (origin/main `18640d6b2` から fresh worktree / WORKDIR-ISOLATION 準拠)。
- 実行コンテキスト: 起動時 checkout は Codex WIP `codex/issue-1215-hitl` (dirty) のため、レポートは origin/main 由来の隔離 worktree で作成 = Codex 作業へ非干渉。
- Caller: scheduled `dependency-audit` (週次 / autonomous / ユーザー不在)。

> 前回 audit: [`2026-05-25.md`](./2026-05-25.md) / [`2026-04-20.md`](./2026-04-20.md) / [`2026-03-28.md`](./2026-03-28.md)
