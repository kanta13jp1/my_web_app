# 依存パッケージ脆弱性チェック 2026-07-20

> 自動週次タスク `scheduled-tasks/dependency-audit` 実行ログ。前回: 2026-07-13 (~1週前)。
> 実行 tree: `origin/main` 由来 worktree (`52010c6d9`)。

## サマリ

| 項目 | 結果 |
|---|---|
| Flutter advisory-affected | **0** ✅ (継続) |
| Flutter discontinued | **1** ⚠️ **繰越 3 週目** — `flutter_markdown` 0.7.7+1 |
| **🆕 EF `supabase-js` lock 実効版** | **`2.105.1`** — 最新 `2.110.7` / `2.110.4` に**資格情報露出の低減修正**あり (未取込) |
| **🆕 `supabase_flutter` 2.14.2 → 2.16.0** | 2.15.x に **Web の認証パラメータ URL 残留修正** (本番が Flutter Web のため実質セキュリティ) |
| **🆕 `http: any`** | 上限なしの direct 依存 (supply-chain 衛生) |
| Deno EF `std@0.224.0` | 85 import (前回 76 / 100% 統一・`.ts` stale 0) ✅ |
| Deno EF `jose` v5.9.6 | CVE 非該当を**個別 advisory 単位で再検証** (下記) ✅ |

**今回の自動アップグレード**: **適用なし** (report-only)。patch bump に該当する安全な更新が 1 件も無いため。

---

## 🆕 今週の新規: EF `deno.lock` の `supabase-js` が資格情報露出の低減修正を未取込

前回までの audit は EF の **import 形式**(`npm:@supabase/supabase-js@2` = major 固定 / 27 箇所)を追跡していたが、
**lock の実効解決版**は追跡していなかった。今回そこを掘って以下を検出:

| 項目 | 値 |
|---|---|
| source の import | `npm:@supabase/supabase-js@2` (27 箇所 / floating major) |
| `supabase/functions/deno.lock` の実効解決 | **`2.105.1`** |
| upstream 最新 | **`2.110.7`** (2026-07-16) |

`2.110.4` に *「function 呼び出し時に Authorization ヘッダで API key を送るのをやめた」* という
**資格情報の露出低減修正**が含まれる。現行 `2.105.1` は未取込。

source が floating `@2` 指定のため、**lock を再生成すれば追従する**構造 (pubspec 制約変更は不要)。

**自動適用を見送った理由**: `deploy-prod.yml` は main マージで **EF 22 本を自動デプロイ**する。
lock 再生成は 27 箇所の解決を一斉に動かすため、無人週次タスクが本番 EF 全体を自動載せ替えすることになる。
→ 有人セッションでの `deno cache --reload` + lock 更新 PR を推奨 (優先度: 中)。

**あわせて検出した死行**: `deno.lock` に `https://esm.sh/@supabase/supabase-js@2.90.1` と
`deno.land/std@0.168.0` 由来の remote エントリが残るが、**`.ts` source からの import は共に 0 件**。
実害なしだが lock 再生成時に一緒に掃除できる。

---

## 🆕 今週の新規: `supabase_flutter` 2.15.x の Web 認証まわり修正

| 現在 | 最新 | 種類 |
|---|---|---|
| 2.14.2 | 2.16.0 | minor |

本番は **Flutter Web** (<https://my-web-app-b67f4.web.app/>) であり、以下 2 件が該当しうる:

- `2.15.1` — *「token 交換後に web URL から認証パラメータを消去」*
- `2.15.4` — *「認証パラメータ消去時に hash route と重複クエリキーを保持」*

URL 上に認証パラメータが残留する挙動は、履歴 / Referer / 共有リンク経由の**トークン漏洩面**に直結する。
CVE 発番は無いが、**Web 配信の本アプリでは実質セキュリティ修正**として扱うのが妥当。

前回 audit は `supabase_flutter` を「advisory 無し・version drift のみ」と分類していたが、
今回 changelog を精査して上記の Web 固有リスクを特定した。

minor bump かつ認証コードパスに触れるため、無人週次タスクでの自動適用は不適切と判断。
→ 有人セッションでの追従を推奨 (優先度: 中〜高)。

---

## 🆕 今週の新規: `http: any` — 制約なし direct 依存

`pubspec.yaml:56` が `http: any`。上限が無いため、上流が破壊的変更や問題のあるバージョンを
publish した際に**無警告で取り込まれうる**。前回 audit は `http` を「security 中核・advisory 無し」と
分類していたが、**制約の緩さ自体**は未指摘だった。

→ `^1.x` 等の caret 制約への変更を推奨 (優先度: 中 / 別 PR)。

---

## ⚠️ 繰越 3 週目: `flutter_markdown` が discontinued

**本項は 2026-07-06 に新規検出 → 07-13 繰越 → 今週も継続の既知項目**であり、状況に変化なし。

```
$ flutter pub get
flutter_markdown 0.7.7+1 (discontinued replaced by flutter_markdown_plus)
1 package is discontinued.
```

- 影響: **12 ファイル**が `package:flutter_markdown/flutter_markdown.dart` を import (前回から件数変化なし)
- advisory / CVE は無し。**今後セキュリティ修正が来ない**保守リスク
- 移行の形: `pubspec.yaml` 差替え + 12 ファイルの import path 一括置換 (API 互換 fork)
- 判断: 12 ファイル横断のため analyze / render smoke が必須。当環境は analyze OOM 実績あり → **本タスクでは実施しない**

> 検出手法の注 (再掲): `flutter pub outdated` の `isDiscontinued` は latest-pin 済みを取りこぼす。
> 今回も同一 tree で両者を実測し、`pub get` のみが警告を出すことを再確認した
> (`pub outdated` 出力に markdown / discontinued の行は 0 件)。

---

## Flutter/Dart パッケージ

### advisory 該当

`isCurrentAffectedByAdvisory: true` → **0 件** ✅

### 繰越の upgradable (前回から据置)

| パッケージ | 現在 | 最新 | 種類 | 備考 |
|---|---|---|---|---|
| `sentry_flutter` / `sentry` | 9.22.0 | 9.24.0 | minor | 9.23/9.24 に**セキュリティ・PII 修正の記載なし**。急ぎ不要 |

その他 `timezone` / `build_runner` 等の制約内 upgradable は前回レポート参照 (状況据置)。

### `pubspec.lock` が committed 状態と乖離

clean な `origin/main` tree で `flutter pub get` を実行しただけで lock が変化 (12 insertions / 20 deletions):

- `test` 1.26.3 → 1.30.0 / `test_api` 0.7.7 → 0.7.10 / `test_core` 0.6.12 → 0.6.16
- `matcher` 0.12.17 → 0.12.19 / `leak_tracker_testing` 0.11.1 → 0.13.0 / `js 0.7.2` 除去

いずれも **test ツールチェーン (dev) のみ**で production 依存への影響なし。
ただし「commit 済 lock と実際の解決結果が違う」状態は CI 再現性を損なうため別途 lock 再生成 PR を推奨。
**本レポートでは lock を commit しない** (docs-only 維持)。

---

## Supabase Edge Functions (Deno)

### `deno.land/std` 移行状況

| バージョン | 前回 (07-13) | 今回 (07-20) | 状態 |
|---|---|---|---|
| `std@0.224.0` (source) | 76 | **85** | ✅ 統一維持 (EF 増に伴う自然増) |
| `std@0.168.0` / `@0.177.0` (source) | 0 | **0** | ✅ 維持 (lock に死行のみ残存) |

`std@0.224.0` は現行 (JSR `@std` 1.x) から見ると古いが **published advisory は無い**。
移行は保守性の課題であってセキュリティ課題ではない。

### `jose` v5.9.6 — 個別 advisory 単位で非該当を再検証 ✅

前回まで「CVE 該当無・審査済」と記録されていた項目を、今回 **advisory 単位で裏取り**した:

| Advisory | 影響範囲 | v5.9.6 |
|---|---|---|
| GHSA-hhhv-q57g-882q (2024-03) | `≤ 4.15.4` | ❌ 非該当 — **v5.x は圧縮ペイロード対応を完全撤去済**と advisory 本文に明記 |
| GHSA-jv3g-j58f-9mq9 (2022-09) | v2 系 | ❌ 非該当 |
| GHSA-4v4g-726h-xvfv 他 3 件 (2021-04) | v1/v2 系 runtime 別パッケージ | ❌ 非該当 |

最新は `6.2.3` だが v5 → v6 は major bump であり、**セキュリティ上の更新理由は無い**。現状維持で問題なし。

---

## 検証ゲート

本レポートは **docs-only / コード変更ゼロ**のため、`flutter analyze` / `deno lint` は
**実行していない** (検証対象の変更が存在しないため)。コード変更を伴うフォローアップ PR では要実施。

---

## 推奨アクション (優先度順)

| # | 内容 | 優先度 | 種別 | 備考 |
|---|---|---|---|---|
| 1 | `flutter_markdown` → `flutter_markdown_plus` 移行 (12 ファイル) | **高** | 保守 | **繰越 3 週目**。discontinued = 今後 patch 無し |
| 2 | `supabase_flutter` 2.14.2 → 2.16.0 | 中〜高 | 実質セキュリティ | 🆕 Web の認証パラメータ URL 残留修正 |
| 3 | EF `deno.lock` 再生成 (→ supabase-js 2.110.7) | 中 | セキュリティ衛生 | 🆕 API key ヘッダ露出低減。**本番 EF 22 本 自動デプロイに注意** |
| 4 | `http: any` → caret 制約化 | 中 | supply-chain 衛生 | 🆕 無警告での破壊的取込を防止 |
| 5 | `pubspec.lock` 再生成 commit | 低 | CI 再現性 | dev/test ツールチェーンのみ |
| 6 | `deno.lock` の死行掃除 (`std@0.168.0` / `esm.sh supabase-js@2.90.1`) | 低 | 清掃 | #3 と同時実施可 |

## 次回 audit への申し送り

- **EF は import 形式だけでなく `deno.lock` の実効解決版も確認すること。** source が floating
  `npm:...@2` 指定のため、import 形式を見るだけでは「major 固定 ✅」で終わり、
  lock が 5 minor 遅れて資格情報関連の修正を取り逃している事実が見えない (今回の #3)。
- **direct 依存は「advisory の有無」だけでなく「制約の緩さ」も見ること** (今回の `http: any`)。
- ホスト repo が他インスタンスの WIP ブランチにある場合、`origin/main` 由来 worktree で実行すること。
  今回ホスト repo は `codex/issue-1215-hitl` にあり、`docs/security-audit/` の**新しい 4 本
  (06-15 / 06-22 / 07-06 / 07-13) が作業ツリーに存在しなかった**。前回 audit を 2026-05-25 と
  誤認しかけたため、**過去レポートの参照も `git show origin/main:` 経由で行うこと**。
