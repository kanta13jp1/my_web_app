# 依存パッケージ脆弱性チェック 2026-05-25

> 自動週次タスク `scheduled-tasks/dependency-audit` 実行ログ。前回: 2026-04-20 (~5週前)。

## サマリ

| 項目 | 結果 |
|---|---|
| Flutter advisory-affected | **0** ✅ |
| Flutter discontinued | **0** ✅ |
| Direct/Dev 要 upgrade (patch/minor) | 2 件 (`flutter_secure_storage`, `test`) |
| Direct 要 upgrade (major) | 0 件 |
| Deno EF `std@0.168.0` 残存 | **0** ✅ (前回 120件 → 0件 / 完全解消) |
| Deno EF `std@0.177.0` 残存 | **0** ✅ (前回 3件 → 0件 / 完全解消) |
| Deno EF `std@0.224.0` 使用 | 41 件 (統一済) |

**今回の自動アップグレード**: 適用なし。理由は下記。

---

## Flutter/Dart パッケージ

### Direct/Dev 要 upgrade

| パッケージ | 種別 | 現在 | 推奨 | 上限 | 種類 | 自動化 |
|---|---|---|---|---|---|---|
| `flutter_secure_storage` | direct | 10.2.0 | 10.3.0 | 10.3.0 | patch | **見送り** (※下記) |
| `test` (dev) | dev | 1.26.3 | 1.30.0 | 1.31.1 | minor | **見送り** (前回 audit で Flutter SDK 互換性警告) |

**自動化を見送った理由**:
- 現セッション (part 236 / `claude/part-236-defer-handoff`) は **DEFER 状態**。`flutter pub upgrade` は依存グラフ全体に影響するため、scope creep [NO-SCOPE-CREEP] 違反になりうる。
- `test` パッケージは前回 audit (2026-04-20) で「1.30.0 は Flutter SDK と非互換あり」と記録あり。bench/CI への影響範囲未確認のため、上書き禁止。
- 本 patch upgrade は **Codex #1 担当タスク** として委譲 (= [`docs/CODEX_WORKFLOW.md`](../CODEX_WORKFLOW.md) §6 振分 5 質問: 全 NO → Codex)。

### Advisory / Discontinued
- `isCurrentAffectedByAdvisory: true` → 0 件 ✅
- `isDiscontinued: true` → 0 件 ✅

---

## Supabase Edge Functions (Deno)

### `deno.land/std` 移行状況 (重要 — 前回 audit 比)

| バージョン | 前回 (2026-04-20) | 今回 (2026-05-25) | 状態 |
|---|---|---|---|
| `std@0.168.0` | 120 ファイル | **0 ファイル** | ✅ 完全解消 |
| `std@0.177.0` | 3 ファイル | **0 ファイル** | ✅ 完全解消 |
| `std@0.224.0` | (記載なし) | 41 ファイル | ✅ 統一済 |

`deno.lock` には `std@0.168.0` のハッシュエントリが残存するが、これは過去ロックの履歴。`.ts` ソースコードからの直接 import は **0 件**。前回 audit の宿題「Deno std import 一括アップデート」が **誰かの手で完了済** (※ git blame 候補: 直近 5 週間で Win Claude #128-#132 のいずれか / 別途特定要)。

### npm: / jsr: / x: 依存

| 依存 | バージョン | 状態 |
|---|---|---|
| `npm:@supabase/supabase-js@2` | メジャー固定 (^2 自動最新) | ✅ |
| `https://deno.land/x/jose@v5.9.6` | `_shared/mcp_auth_guard.ts` 1 箇所 | ⚠️ 監視推奨 |

`jose@v5.9.6` は WorkOS JWKS による JWT 検証本体 (= MCP 認可基盤)。MCP_AUTH_SECURITY_PRINCIPLES.md 原則 #2/#5/#7/#8 の中核。最新 (v5.x 系) との差分を次回 audit で確認すること。

---

## アクション

### ✅ 完了 (今回 audit)
- `flutter pub outdated --json` で direct/dev/advisory/discontinued を一括確認
- Deno EF `std@0.168.0` / `@0.177.0` 残存ファイル数 verify (前回宿題のクローズ確認)

### 📋 次回推奨 (Codex #1 もしくは VSCode版 → 既に dormant のため Codex #1)

- [ ] **Codex #1**: `flutter_secure_storage` 10.2.0 → 10.3.0 patch upgrade (CI gate 通過必須)
- [ ] **Codex #1**: `test` (dev) 1.26.3 → 1.31.1 minor upgrade — Flutter SDK 互換性事前確認後 (前回 audit 警告を参照)
- [ ] **Win Claude #1**: `deno.land/x/jose@v5.9.6` 最新版チェック (MCP 認可影響度評価込み)

### 🟢 残課題 (前回 audit からの繰越状況)

| 前回 (4/20) 宿題 | 状況 |
|---|---|
| Deno std `@0.168.0` → `@0.224.0` 一括 (Win版) | ✅ **完了** (今回 verify) |
| `device_info_plus`, `share_plus`, `google_fonts` major upgrade | ⏳ 未着手 (本 audit では direct 一覧から消えているため pubspec.yaml 直接指定外 = transitive のみ / 再評価不要) |
| `flutter_lints` 6.x major upgrade | ⏳ 未着手 (現 3.0.2 / 警告なし / 優先度低) |
| `test` 1.31.0 Flutter SDK 互換性確認 | ⏳ 未着手 (上記 Codex タスクへ引き継ぎ) |

---

## 環境メタ

- Flutter SDK: outdated 警告検出済 (`flutter upgrade` 推奨 / 本 audit では実施せず)
- Branch: `auto/dependency-audit-20260525` (origin/main から fresh / WORKDIR-ISOLATION 準拠)
- Caller: scheduled `dependency-audit` (週次 / autonomous)
- Caveman mode: active (本 report 本文は normal で記載 / commit message は normal)

> 前回 audit: [`2026-04-20.md`](./2026-04-20.md) / [`2026-03-28.md`](./2026-03-28.md)
