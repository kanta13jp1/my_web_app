# ブログ下書き 2026-04-20

## タイトル案 (3案)

1. **「衝動買いを撲滅する価格トラッカーをFlutter × Supabaseで無料で作った話」**
2. **「Amazon価格を自動追跡するアプリをFlutter Webに組み込んだ — EF50本制限を守りながら」**
3. **「notebooklmの深夜リサーチで生まれた価格トラッカー機能 — Zero-Cost Price Tracker実装記」**

## 投稿先候補

Zenn / Qiita / note / はてなブログ / X Article / Medium / dev.to / Hashnode / Substack / GitHub Pages / NOTION

---

## 本文下書き (約2200字)

### はじめに

「この商品、もう少し安くなったら買おう」——そう思ってブラウザのブックマークに溜め込んでいる商品リストが、あなたのPCにもあるはずだ。

ところがブックマークには価格情報がない。結局「なんとなく」タイミングを見計らって購入し、翌日にセールになっていたという苦い経験を何度も繰り返してきた。

私が開発している**自分株式会社**（Flutter Web + Supabase の AI 統合ライフマネジメントアプリ）には、すでに「日用品リマインダー（shopping_items）」と「購入後ログ（purchase_sessions）」が存在していた。しかし "購入前の価格追跡" という空白地帯があった。

そこで今回、**NotebookLM の Deep Research** で調査した "Building a Zero-Cost Amazon Price Tracker" の知見を取り込み、**価格トラッカー機能**を実装した。

---

### どんな機能か

- 商品URLを貼るだけで wishlist に登録
- 現在価格を手動ログ（Phase 2 で自動スクレイピング予定）
- 史上最安値・最高値を自動計算
- 目標価格を設定→達成時に通知（notification-center 連携）
- 価格推移グラフ（fl_chart、Phase 2 実装予定）

---

### 実装方法

#### 1. DBスキーマ設計 (Supabase PostgreSQL)

2つのテーブルを新規作成した。

```sql
-- 商品 wishlist
CREATE TABLE tracked_products (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
  product_url text NOT NULL,
  product_name text NOT NULL DEFAULT '',
  store_domain text NOT NULL DEFAULT '',   -- amazon.co.jp など自動抽出
  current_price int,
  target_price int,                        -- この価格以下で通知
  highest_price int,
  lowest_price int,
  notify_on_drop boolean NOT NULL DEFAULT true,
  is_active boolean NOT NULL DEFAULT true,
  is_purchased boolean NOT NULL DEFAULT false,
  UNIQUE (user_id, product_url)            -- 同一URLの重複登録防止
);

-- 価格推移ログ (時系列チャート用)
CREATE TABLE price_logs (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  product_id uuid NOT NULL REFERENCES tracked_products(id) ON DELETE CASCADE,
  price int NOT NULL,
  source text NOT NULL DEFAULT 'manual',  -- 'manual' | 'scraper' | 'api'
  logged_at timestamptz NOT NULL DEFAULT now()
);
```

**Row Level Security (RLS)** を完備し、`auth.uid() = user_id` で自分の商品だけ参照・更新できる。

#### 2. トリガーで最安値・最高値を自動更新

```sql
CREATE OR REPLACE FUNCTION update_tracked_product_prices()
RETURNS TRIGGER AS $$
BEGIN
  UPDATE tracked_products SET
    current_price = NEW.price,
    highest_price = GREATEST(COALESCE(highest_price, NEW.price), NEW.price),
    lowest_price  = LEAST(COALESCE(lowest_price, NEW.price), NEW.price),
    updated_at    = now()
  WHERE id = NEW.product_id;
  RETURN NEW;
END;
$$ LANGUAGE plpgsql;

CREATE TRIGGER trg_price_logs_after_insert
AFTER INSERT ON price_logs
FOR EACH ROW EXECUTE FUNCTION update_tracked_product_prices();
```

`price_logs` に INSERT するだけで `tracked_products` の current/highest/lowest が自動更新される。フロント側で計算ロジックを持つ必要がなく、シンプルに保てる。

#### 3. Edge Function は増やさない — lifestyle-hub に統合

このプロジェクトでは **Edge Function 50本以下** というハードキャップを設けている。新機能を追加するたびに EF を増やすとデプロイ管理が煩雑になるためだ。

今回は既存の `lifestyle-hub` に `price.*` アクションを追加する方針を採った。

```typescript
// supabase/functions/lifestyle-hub/index.ts
case 'price.add':
  return await handlePriceAdd(req, supabase);
case 'price.list':
  return await handlePriceList(req, supabase);
case 'price.log':
  return await handlePriceLog(req, supabase);
case 'price.update':
  return await handlePriceUpdate(req, supabase);
case 'price.delete':
  return await handlePriceDelete(req, supabase);
```

これにより **EF 総数を増やさずに機能を拡張** できた。

#### 4. Flutter の UI 実装

`PriceTrackerPage` では `_AddProductSheet`（モーダルボトムシート）で商品を追加し、カードリスト形式で一覧表示する。

```dart
Future<void> _add() async {
  final added = await showModalBottomSheet<bool>(
    context: context,
    isScrollControlled: true,
    backgroundColor: const Color(0xFF141414),
    shape: const RoundedRectangleBorder(
      borderRadius: BorderRadius.vertical(top: Radius.circular(20)),
    ),
    builder: (_) => const _AddProductSheet(),
  );
  if (added == true) await _load();
}
```

URLから `store_domain` を自動抽出することで、「どのECサイトの商品か」をUI上でバッジ表示できる。

---

### 詰まったポイント

#### (1) 既存テーブルとの棲み分け

`shopping_items`（日用品リマインダー）と `purchase_sessions`（購入後ログ）がすでに存在しており、新テーブルとの役割が混乱しそうだった。

解決策：コメントで三者の違いを明示した。

```
shopping_items    → 日用品リマインダー (価格なし)
purchase_sessions → 購入後ログ (事前wishlistなし)
tracked_products  → 目標価格 + URL + 価格推移を持つ wishlist (本機能)
```

#### (2) EF 50本制限との戦い

新規 EF を作成したくなる誘惑があったが、`lifestyle-hub` の action 分岐で吸収した。このパターンを使うと、概念的に近い機能をまとめてデプロイでき、cold start のコストも下げられる。

#### (3) Philosophy Alignment の自己チェック

プロジェクトでは実装前に「9原則チェック」を行っている。価格トラッカーは全9原則をパスした。特に面白かったのは：

- **CEO感**: ユーザーが目標価格を自分で決める（AI が価格を決めるのではない）
- **資本=時間**: セール到来を自動検知することで "価格チェックのための時間" を0にする
- **KPI=昨日の自分**: 「史上最安値」との比較で、今が "自分史上ベスト" の購入タイミングかを判断できる

---

### まとめ

今回の実装で学んだことをまとめる。

1. **Supabase トリガー** を使うと、集計ロジックをフロントから切り離せて責務が明確になる
2. **Edge Function のハブ化**（action 分岐）は EF 総数を抑えながら機能を増やす有効な戦略
3. **NotebookLM Deep Research** で先行事例を調べてから実装することで、設計ミスが減る
4. **Philosophy Alignment チェック** を実装前に行うことで「競合の劣化コピー」にならない機能を選別できる

次のフェーズでは、Amazon の商品ページを自動スクレイピングする EF（Phase 2）と、目標価格到達時の Push 通知（notification-center 連携）を実装する予定だ。

---

自分株式会社: https://my-web-app-b67f4.web.app/

#FlutterWeb #Supabase #buildinpublic #個人開発 #Flutter #価格トラッカー
