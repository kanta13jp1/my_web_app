# CS チェックノート 2026-04-29-03:00

## 実行サマリー

| 項目 | 結果 |
| --- | --- |
| Edge Function UI 連携 | 20関数すべて登録済み、新規追加なし |
| サポートチケット | 0件（Supabase ネットワーク制限によりキャッシュ参照） |
| GitHub PR レビュー | 10件オープン、コードレビュー実施 |
| Issue 自動修復 | workflow-failure 3件クローズ、1件新規作成 |
| Schedule 健全性 | cs-check 正常 / daily-report 2日間停止を検出 |
| インフラ | sandbox ブロックにより外部チェック不可（キャッシュ参照） |

---

## Step 0: Edge Function UI 連携

ローカル Edge Function 数: **20件**（上限 90 以下のため QUOTA GUARD 問題なし）

全 20 関数が `edge_function_summary_card.dart` に登録済みのため、新規 UI 連携追加なし。

---

## Step 1-2: サポートチケット

`docs/ticket-cache.json` 参照: **0件**

Supabase ネットワーク制限 (403) により API 直接呼び出し不可。

---

## Step 4: GitHub PR レビュー

オープン PR: 10件

### PR #1000 — Home画面を生成画像風の操作盤デザインへ更新
- ブランチ: `codex/codex1-home-hopson-style`
- セキュリティ: ✅ 問題なし（純粋な UI コード、外部入力なし）
- パフォーマンス: `_HopsonHomePainter.shouldRepaint` が正確に実装されており不要な再描画なし
- Lint: CI `in_progress` 中（flutter analyze タイムアウトは環境問題、コードレベルの問題なし）

その他の PR (#947, #922, #913, #901, #886, #874) は CI 全て正常または既レビュー済み。

---

## Step 6: GitHub Issue 自動修復

### クローズした Issue

| Issue | 内容 | 理由 |
| --- | --- | --- |
| #949 | [CI失敗] codex/codex2-backlog-ready-conflict-filter | PR #947 の全チェックが success |
| #930 | [CI失敗] codex/codex1-stale-ef-runtime-view | PR #922 の全チェックが success |
| #996 | [Deploy失敗] Deploy to Production (main) | main が 3d1fa81 以降も複数コミット進行し再デプロイ済み |

### スキップした Issue（修復困難）

- #1002, #1001: CI on `codex/codex1-home-hopson-style` — CI `in_progress` 中、自己回復待ち
- #938: WBS AI Review 失敗 — cron ジョブ、外部サービス問題、手動対応要
- #933: Horse Racing Auto Update 失敗 — cron ジョブ、外部サービス問題
- #932: Blog Publish 失敗 — PS版 専任 (`/blog-publish-cleanup`)
- #931: Blog Draft Register 失敗 — PS版 専任

---

## Step 7: Schedule 健全性

### 7a: 直近コミット

```
c45faf0 自動: CS チェック 2026-04-29-02:00
82a96ce 自動: CS チェック 2026-04-29-01:00
```

cs-check は正常動作中。

### 7b: 日次タスク実行確認

- 最新 daily-report: `docs/daily-reports/2026-04-27.md`（**2日間未生成**）
- Issue #1003 を新規作成して監視

### 7c: cs-notes パターン分析

直近 cs-notes に「エスカレーション」未解決項目なし。ネットワークブロック（sandbox 制限）は継続中で既知問題。

---

## Step 8-9: インフラヘルスチェック

sandbox の全外部 HTTP ブロック (`host_not_allowed`) により直接チェック不可。

`docs/flutter-analyze-cache.json`: `analyze_ok=null` / `status=unknown`（GHA cs-check が更新予定）

---

## エスカレーション

なし

---

## 要対応

- **daily-report 停止** (Issue #1003): `daily-report.yml` の実行ログを確認し、Claude API 呼び出しまたは認証の問題を調査すること
- **Blog ワークフロー失敗** (#932, #931): PS版#2 専任スキル `/blog-publish-cleanup` で対応
