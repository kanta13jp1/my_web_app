# Issue Fix Plan #1257

- Issue: [[追加要望] 入力フォームにおけるプロアクティブなエラー検知と解決策提示機能](https://github.com/kanta13jp1/my_web_app/issues/1257)
- Labels: enhancement,追加要望
- Workflow: `.github/workflows/github-issue-fix.yml`
- CI repair pair: `.github/workflows/ci-auto-fix.yml`
- Run: https://github.com/kanta13jp1/my_web_app/actions/runs/31454281595

## Goal

[追加要望] 入力フォームにおけるプロアクティブなエラー検知と解決策提示機能

## Current Context

```text
﻿## 背景
ユーザーが複雑な設定や入力を行う際、送信後にエラーに気づくことが多く、操作のストレスとなっている。広告管理ツールがポリシー違反を事前に検知し明確な解決策を提示するように、アプリ内でも事前のトラブルシューティングを提供したい。

## 要望
フォームの入力中にバックグラウンドでリアルタイムに検証を行い、エラーや不整合が予測される場合に、具体的な解決策とともにユーザーへ警告する機能を実装したい。

## 受け入れ条件
- フォーム入力時にバックグラウンドで非同期の入力検証が実行されること。
- 進行を妨げる可能性のあるエラーを検知した場合、送信ボタンを押す前に警告メッセージが表示されること。
- 警告には、単なるエラー内容の通知だけでなく、ユーザーがどう修正すべきかの具体的な解決策が含まれていること。
- 提示された解決策を容易にフォームへ反映できる導線があること。

## 実装メモ
主要な入力フィールドに対して入力イベントのデバウンス処理を実装し、検証APIを呼び出して結果をUIに即時反映させる。
---

## NotebookLM Source
- Notebook: 8 Gemini Tips for Organizing Your Space and Life
- Notebook ID: 579bd686-f17e-414a-894f-822f29b5c11e
- URL: https://notebooklm.google.com/notebook/579bd686-f17e-414a-894f-822f29b5c11e
- Generated by: Codex #1 bulk NotebookLM issue registration
- Generated at: 2026-04-30 01:03:58 +09:00

<!-- notebooklm-bulk-source:579bd686-f17e-414a-894f-822f29b5c11e -->


```

## Autonomous Repair Loop

1. Reproduce the smallest failing path for this issue.
2. Apply the minimum safe fix on this branch.
3. Let normal CI run on the draft PR.
4. If CI fails on mechanical issues, `ci-auto-fix.yml` attempts `dart fix --apply` and `deno fmt`.
5. Merge only after CI is green and the issue scope is satisfied.

## Checklist

- [x] Reproduction is clear: invalid email, URL without HTTPS, and invalid budget were only discoverable after submit; the new flow surfaces them while typing.
- [x] Smallest safe fix is implemented: isolated feature with a replaceable async validator, debounce, stale-response protection, and one-click suggestions.
- [x] Analyze/tests/CI are checked
- [x] PR notes explain the change and the remaining risk

## Implementation Notes

- Route: `/proactive-form-check`
- Input remains on-device in the default rule-based validator. A remote policy
  validator can be injected later without coupling it to the UI.
- Submission remains disabled until every required value has a current,
  non-blocking validation result.
- The view model discards out-of-order async results so an older response cannot
  replace feedback for the user's latest input.
- Email suggestions preserve the potentially case-sensitive local part and
  normalize only surrounding whitespace and the domain.
- Heavy validation ran on GitHub-hosted CI while the local machine was above
  the repository's safe memory threshold.

## Subagent Review

- Async-state reviewer (read-only): found a concurrent-submit race and dispose
  guard gaps. Submission is now locked before revalidation, restored in a
  `finally` block, and covered by a regression test.
- UX/accessibility reviewer (read-only): found color/icon-only severity and an
  over-broad live region. Findings now expose severity in text and Semantics,
  while a short live region announces result counts.
- Both reviewers ran without Flutter/Dart/build/browser/Git commands, edited no
  files, and left no cleanup work.
